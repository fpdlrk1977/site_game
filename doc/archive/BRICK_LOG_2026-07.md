# BRICK_LOG 2026-07 — 브릭 시스템 세션 로그 (아카이브)

> 2026-07-28 ~ 07-31 브릭 전환기의 세션 기록 전문. **평소엔 읽지 않는다** — 특정 결정의 경위를 되짚을 때만 찾아본다.
> 반복 함정은 `doc/BRICK_PITFALLS.md`, 현재 상태·다음 할 일은 `doc/BRICK_PROGRESS.md`로 뽑아 두었다.

---

## 세션 로그

### 2026-07-31 — 🧱 ⑬ 재료(무늬) 텍스처 — ✅ 확인 완료

tsc·lint 클린 + 저장 248 → **254/254**(v1 하위호환 + v2 골든) + **신규 무늬 36/36** + 나머지 유지(107/34/27/43).
**✅ 사용자 확인 완료** — 재료를 골라 놓으면 벽돌·나무·잔디 무늬가 보인다.

- **아틀라스 한 장**(`lib/brick/textures.ts`, 4×4칸 · 16×16 픽셀 아트 · 절차적). 재료 8종.
  재질이 안 갈리므로 **draw call이 안 늘어난다** — 인스턴스 속성 `aTile` 하나로 칸만 고른다.
- **저장 v2** — 무늬를 **브릭 레코드가 아니라 팔레트에** 넣었다(엔트리 4→5바이트). 무늬는 색·재질과 거의 항상
  함께 다니므로 조합당 한 번만 적힌다. 레코드에 1바이트를 더했다면 청크당 최대 8KB가 늘었다. **v1은 계속 읽는다.**
- **색은 틴트** — 재료가 자기 색을 갖고 팔레트 색이 곱해진다(흰색 = 본래 색). 마크 잔디 틴트와 같은 구조.
  지형도 흰색 틴트로 바꿨다 — 안 그러면 초록 잔디에 초록이 또 곱해져 시커메진다.

#### 🔴 두 번 헛돌았다 — 둘 다 "마크가 어떻게 하는지"를 안 본 탓

1. **흑백 무늬** — "색 팔레트와 안 싸우게" 흑백 명암으로 만들고 색을 곱했다. → 사용자: *"마인크래프트 같은 이미지가 아닌데."*
   맞다. 마크 텍스처는 **컬러 픽셀 아트**고, 잔디(초록 명암)·벽돌(빨강+회색 줄눈)처럼 **재료마다 고유한 색 관계**가 있다.
   흑백 한 채널로는 담을 수 없다. → 컬러 픽셀 아트로 재작성.
2. **전부 흰색으로 나옴** — 아래 함정 **T-1**(flipY).

> 사용자가 명시했다: **"마인크래프트를 언급하는 건 기능에 있어 마인크래프트를 기준에 둔다는 얘기"**.
> 오늘 그림자(→AO)와 무늬(→컬러) 둘 다 이걸 안 지켜서 돌아갔다.

### 2026-07-31 — 🧱 ⑧~⑫ 배치 (기본색·전체 지우기·WASD·잔디 바닥·꼭지점 회전) — ✅ 확인 완료

tsc·lint 클린 + 배치 21 → **27/27** + 나머지 유지(107/34/25/248/43) + **사보타주 검증**.
**✅ 사용자 확인 완료** — 회전 네 방향 · 드래그 · WASD · 전체 지우기 · 잔디 바닥 전부 정상.

착수 전 네 가지를 물어 방향을 못 박았다(회전=앵커 고정 먼저·세우기 나중 / 블록=재료와 무늬 둘 다 /
프로토타입에선 WASD와 전체 지우기만 / 바닥은 표면만 잔디). 계획서 ⑧~⑭에 적어 두었다.

| | 내용 |
|---|---|
| ⑧ | 기본색 **흰색**, 팔레트에서 흰색 ↔ 빨강 자리 교체(`BRICK_COLORS[0]`이 기본색이라 자동 반영) |
| ⑨ | **전체 지우기** — 도구 패널에. 프로토타입의 중복 버튼은 제거해 **한 곳**으로 |
| ⑩ | **WASD 이동** — 신규 공용 `BrickMover`(프로토타입·에디터·뷰어 공유) |
| ⑪ | 지면 **표면 한 겹만 잔디**, 아래는 흙 → 돌 |
| ⑫ | **회전 = 겨눈 칸(꼭지점) 기준** |

#### ⑫ — 왜 "R이 안 먹는" 것처럼 보였나

앵커가 **항상 AABB 최소 모서리**라 브릭이 늘 `+X/+Z`로만 자랐다. 게다가 `extentOf`는 **회전 0과 2가 같은 값**이라
**1×4 브릭은 R을 눌러도 자리가 두 가지밖에 안 나왔다.** 네 방향이 아니었다.

→ 신규 `pivotOffset(part, rot)`: 기준 칸이 회전과 함께 돈다(rot 0 최소 모서리 · 1 `(ex−1,0)` · 2 최대 모서리 · 3 `(0,ez−1)`).
첫 클릭(`anchorFromFace`)과 드래그(`slideAnchor`)가 **같은 값을 써야** 하므로 `FacePlacement.pivot`에 담아 넘긴다 — 따로 계산하면 반드시 어긋난다.

**테스트는 규칙이 바뀐 만큼 다시 썼다**(통과시키려고 고친 게 아니다):
- 삭제: "파츠 크기가 달라도 첫 칸이 같다" — 이제 **틀린 규칙**이다.
- 일반화: `앵커 == 겨눈 칸` → **`겨눈 칸을 덮는다`**(몰래 옮기지 않는다는 원래 취지는 그대로).
- 신규: **회전 네 가지가 서로 다른 자리** · **겨눈 칸이 네 회전 모두에 포함** · 1×1은 회전 무관(회귀 감시).
- 사보타주(rot 1의 기준점을 0으로) → `서로 다른 자리 3/4`로 실패 확인.

#### 🐛 후속 — 회전해도 가이드가 안 따라오던 문제 (같은 날, 사용자 보고)

*"회전 시 가이드라인의 방향이 이상하고 기준이 되는 작은 모서리를 벗어난다."*

**놓일 자리(`spot`)는 포인터 이벤트에서만 다시 계산되는데 `R`은 포인터 이벤트가 아니다.**
마우스를 안 움직이고 R만 누르면 **앵커는 옛 회전 · 크기는 새 회전**이 되어 고스트가 겨눈 칸을 벗어난다.
예전엔 앵커가 회전과 무관해서 안 드러났고, ⑫로 앵커가 회전에 의존하게 되자 표면화됐다.
→ 마지막으로 겨눈 지점을 기억해 **파츠·회전이 바뀌면 그 지점에서 다시 계산**한다(획이 도는 중엔 평면이 자리를 정하므로 제외).

> **오늘 두 번째 같은 계열이다** — 빛도 "다시 계산은 했는데 화면에 안 갔다"(G-6)였다. 함정 **G-7**로 등록.

### 2026-07-31 — 🐛 ⑤ 후속 — "브릭을 놓으면 그림자가 사라진다" + 구석 그늘(AO) — 브라우저 확인 대기

tsc 클린 + 월드 96 → **102/102** + 나머지 유지(21/248/34/25/43) + **사보타주 검증 2건**.

사용자 보고: **"새로고침하면 그림자가 있는데, 브릭을 아무 곳이나 추가하면 그림자가 사라진다. 모두 사라질 때도 있고 어떤 건 안 사라진다."**
눈으로 찾지 않고 **순수 계층에서 재현**해 원인을 갈랐다 — 결과적으로 결함은 **렌더 갱신 쪽**이었고, 빛 값 자체는 멀쩡했다.

#### 원인 ① 다시 구운 빛이 GPU까지 안 갔다 (진짜 원인)

`recomputeLight()`가 `this.light`만 채우고 **리전 리비전을 안 올렸다.** 렌더러의 `Region`은
`useMemo(..., [world, regionKey, rev, studStyle])`라 `rev`가 그대로면 인스턴스 버퍼를 다시 쓰지 않는다.
→ 140ms 디바운스 뒤 계산된 **올바른 값이 화면에 닿지 않고**, 다른 이유로 리빌드된 리전만 우연히 갱신된다.
"어떤 건 바뀌고 어떤 건 안 바뀐다"가 정확히 이 증상이다. (`recomputeAll()`은 `bumpRegion`을 도는데 `recomputeLight()`만 빠져 있었다.)

#### 원인 ② 그 리빌드는 **틀린 값**으로 굽는다

브릭을 놓으면 `colTop`은 **즉시** 오르는데 빛 지도는 140ms 뒤 갱신된다. 그 사이 그 기둥 아래 칸은
"열린 하늘(1.0)"에서 "그늘"로 분류만 바뀌고 빛 지도엔 항목이 없어 `lightAt`이 `SKY_MIN`(0.16)을 돌려준다.
배치 순간 리빌드되는 리전이 그 값으로 구워지고, 이후 올바른 값이 와도 ①때문에 덮이지 않는다.
→ **①을 고치면 ②의 일시적 오값도 자동으로 덮인다.**

**수정**: `recomputeLight()`를 `bakeLight()`(실제 굽기) + **리전 전량 `bumpRegion`** 으로 나눴다.
굽기 쪽엔 예산 초과로 중간에 빠지는 길이 있어서 **분리하지 않으면 그 경로에서 리비전이 안 올라간다.**

> **비용**: 디바운스 뒤 한 번, 보이는 리전의 인스턴스 버퍼를 다시 쓴다. 큰 월드에서 드래그 끝에
> 끊김이 느껴지면 "값이 변한 칸이 속한 리전만" 올리는 방식으로 좁힐 수 있다(구현이 더 큼).

#### 조사 중 확인한 사실 — 지금 모델엔 **점유 기반 AO가 없었다**

평지에 브릭을 놓고 주변 지형 576개의 꼭짓점 밝기를 전부 비교했더니 **변한 브릭 0개**였다.
열린 하늘 칸은 전부 1.0이라 브릭 옆·모서리가 어두워지지 않는다 — 사용자가 말한 **"바닥에만 그림자"** 가 맞았다.
(보이던 그늘은 땅속 방향으로 어두워지는 그라데이션뿐이었다.)

#### 구석 그늘(AO) 추가 — 밝아지는 곳은 없다

`cornerLight`에 꼭짓점당 **바깥 일곱 칸 중 막힌 칸 수**로 깎는 항을 더했다.

- **기준은 `FLAT`(=3)** — 평평한 면의 꼭짓점은 **항상 같은 평면의 이웃 셋이 막혀 있다.**
  거기서 **더** 막힌 만큼만 깎으므로 평평한 바닥·벽·볼록 모서리는 **1.0 그대로**고, 오목한 안쪽 구석만 어두워진다.
  → **전역 밝기가 흔들리지 않는다.** 이 기준을 안 잡고 `blocked/8`로 깎으면 **바닥 전체가 통째로 어두워진다**(그건 AO가 아니라 그냥 조광).
- 자기 몸은 자기 꼭짓점을 가리지 않는다 — 축마다 안쪽 좌표가 하나씩이라 **자기 칸은 정확히 하나**다.
- 막힌 칸에 **안 판 땅속**도 포함한다 — 실제로 빛을 막으므로 구덩이 벽·바닥에도 그늘이 진다.
- 세기 0.7, 가장 깊은 구석에서 0.6배.

> **왜 마인크래프트식 "면×꼭짓점 24값" AO가 아닌가**: 그건 vec4 속성 6개라 `instanceMatrix`(4칸)와 합쳐
> 속성 한도에 붙는다(⑤ 본 작업에서 이미 내린 결론). 8값 구조를 유지하면서 **오목한 곳만 깎는** 형태로 맞췄다.

#### 테스트 — 둘 다 **고의로 깨서** 잡히는지 확인했다

- 리전 올리기 제거 → `안 오른 리전 4개`로 실패 ✓
- AO 무력화 → `벽에 닿은 바닥 꼭짓점 1.00 → 1.00`으로 실패 ✓

기존 항목 **"깔린 지형의 위쪽 꼭짓점은 최대 밝기"**(무수정 통과)가 AO 기준선이 옳다는 증거도 된다 —
평평한 지형에 AO가 걸렸다면 그 항목이 먼저 깨진다.

**확인 결과**: 1·3 ✅ 확인 완료(사용자). 2는 세기가 약해 안 보였고 → 아래에서 곡선을 고쳤다. 4는 미확인(증상 나오면 보고).

#### 후속 — 밑동 그늘을 마인크래프트처럼 (같은 날)

**사용자가 목표를 분명히 했다**: *"마인크래프트를 보니까 블록을 바닥에 놓으면 접촉하는 면에 강한 접촉 그림자가 지더라."*
→ **그건 방향광 그림자가 아니라 AO다.** 마인크래프트엔 태양 방향도 그림자 맵도 없다.
내가 앞서 "그림자 맵을 켜야 하나"로 몰고 간 건 **질문을 잘못 읽은 것**이다. 필요한 건 이미 만든 AO의 **세기 곡선**이었다.

**정정 하나 더**: 앞서 "번지는 폭 1m는 구조적 한계"라고 적었는데, **마인크래프트도 블록 1개(1m) 폭**이다.
지형 타일이 1m라 **폭은 이미 같았다.** 사용자가 본 "퍼짐"은 폭이 아니라 **선형 응답 때문에 깊은 구석이 새까매진 것**이었다.

| 세기(선형) | 벽 하나(밑동) | 깊은 구석 | 사용자 판정 |
|---|---|---|---|
| 0.7 | 0.82 | 0.30 | 안 보인다 |
| 2.0 | 0.50 | 0.20 | 너무 퍼진다(=얼룩) |
| **√ 0.6** | **0.70** | **0.40** | 채택 |

- **`√` 응답** — 첫 접촉에서 크게 떨어지고 그 뒤는 완만. 선형은 **낮추면 밑동이 안 보이고 올리면 구석이 뭉개지는** 딜레마를 벗어날 수 없다.
- **면 배율 확대**(앞뒤 0.8→0.72 · 좌우 0.6→0.5 · 밑면 0.5→0.35) — 배율은 면마다 상수라 **모서리에서 딱 끊긴다.**
  번짐 없이 입체감을 키우는 진짜 레버는 AO가 아니라 이쪽이다. **윗면 1.0은 유지**(바닥 밝기가 바뀌면 씬 톤이 통째로 흔들린다).
- **둘 다 추가 비용 0.** 반면 방향광+그림자 맵은 실측 **+10.3ms**(프레임 예산의 절반 초과, 카메라 컬링이 안 먹어 삼각형 4배) → **안 한다**(D-1 재확인).

**테스트(+5, 107/107)**: 값이 아니라 **곡선 모양**을 잠근다 — 낙차가 점점 줄어드는지(오목).
선형으로 되돌리면 낙차가 `0.15/0.15/0.15`로 균일해져 실패한다(사보타주로 확인).

#### 🔴 결론 — **AO는 처음부터 정상이었고, 바닥이 어두워 안 보였다** (같은 날, 픽셀 측정으로 확정)

> 아래 "잘림" 절은 **틀린 진단**이다. 기록으로 남겨 둔다 — 같은 함정을 다시 밟지 않기 위해서.

사용자가 마인크래프트 스크린샷을 들고 왔다: *"블록 밑에 접촉 그림자가 확실히 보이잖아."* 우리 화면엔 안 보였다.
**추측을 멈추고 `DEBUG_BAKED_LIGHT`(구운 값을 흑백으로 렌더)를 켜서 스크린샷 픽셀을 직접 읽었다.**

| 측정 | 값 | 뜻 |
|---|---|---|
| 디버그: 열린 바닥 | **153** | 흰색(1.0)이 153 = `1/π` → **환경광이 π로 나눠지고 있다** |
| 디버그: 벽 밑동 | **96 → 126 → 141 → 153** | **AO 정상 동작**(0.64 → 1.0, 계산값과 일치) |
| 색 화면: 바닥 | **61**, 밑동 **46** | 그늘은 있다. **바닥이 너무 어두워** 차이가 15/255뿐 |

→ **AO도 곡선도 멀쩡했다.** 문제는 `BRICK_AMBIENT`였고, 그것도 "너무 높아서"가 아니라 **너무 낮아서**였다.

**세 번 잘못 짚었다.** ①그림자 맵이 필요한 줄 알았다(아니었다 — AO가 맞았다) ②AO 세기·곡선 문제인 줄 알았다(그것도 정상이었다)
③**"환경광 1.6은 잘린다"고 단정하고 1.0으로 낮췄다** — `1/π`를 빼먹은 계산이었고, 화면만 더 어두워져 **그늘이 더 안 보이게 만들었다.**
공통점은 하나다 — **화면 값을 재지 않고 추측했다.** 재기 시작하자 5분 만에 끝났다. → 함정 **D-2b**(π) · **D-2c**(측정법)로 등록.

**수정**: `BRICK_AMBIENT = Math.PI`. 알베도가 1:1로 나와 **고른 색이 그 색 그대로**, 흰 브릭이 진짜 흰색이 되고,
면 배율·AO는 그 위에서 깎기만 하므로 **잘림도 없다.** 갈색 바닥 61 → **107**, 밑동 그늘 46 → **88**(밝은 바닥 위의 그늘이라 잘 보인다).

#### ✅ 마지막 조각 — 그늘의 **공간 프로파일** (같은 날, 확인 완료)

밝기를 고친 뒤에도 사용자: *"과한 blur가 들어간 느낌"*, 그리고 마인크래프트 스크린샷 — **잔디와 벽돌 접촉부에 바짝 붙은 진한 띠**.
세기(0.7↔2.0)를 오가며 두 번 헛돌았던 진짜 이유가 여기 있었다: **문제는 세기가 아니라 떨어지는 모양이었다.**

**원인**: 정점 사이는 래스터라이저가 **직선**으로 섞는다 → 어두운 꼭짓점에서 밝은 꼭짓점까지 **면 전체에 균등하게** 밝아진다.
세기를 올리면 띠가 진해지는 게 아니라 **넓은 얼룩이 진해질 뿐**이다. 모양은 세기로 못 바꾼다.

**수정**: 프래그먼트에서 **부족분에 지수**를 씌운다 — `1 - pow(1 - sky, SHADE_CURVE)` (1.6).
어두운 자리는 그대로 두고 조금만 떨어지면 빠르게 회복한다.

| 벽에서의 거리 | 전(직선) | 후(곡선) |
|---|---|---|
| 접촉면 | 0.70 | **0.56** |
| 반 칸(0.5m) | 0.85 | 0.85 |
| 한 칸(1m) | 1.00 | 1.00 |

- **면 배율을 별도 varying으로 분리**했다(`vBaked = vec2(sky, tone)`). 배율은 **면마다 상수**라 곡선을 씌우면 안 되는데,
  전엔 하늘빛과 곱해져 한 값으로 넘어가고 있었다. 분리하지 않으면 곡선이 면 배율까지 뭉갠다.
- **`AO_STRENGTH`(1.2)와 `SHADE_CURVE`(1.6)는 한 쌍**이다. 곡선이 어두운 값을 조금 들어올리므로 CPU 원값을 그만큼 세게 잡았다.
  **한쪽만 바꾸면 접촉 그늘이 무너진다** — 양쪽 파일 주석에 명시.
- 깊은 구석은 하한(원값 0.25 = 화면 0.37)에서 **포화**시킨다. 새까맣게 뭉개지지 않는다.
- 테스트는 원값 기준이라 **화면보다 어둡게 보인다**(0.40 → 화면 0.56) — 그 사실을 테스트 주석에 적었다.

**✅ 사용자 확인 완료** — *"이정도면 된거 같아."* (더 조일 일이 생기면 `SHADE_CURVE`↑ = 더 좁고 진하게, `AO_STRENGTH`↑ = 전체적으로 진하게)

#### ❌ (틀린 진단, 기록용) "원인은 잘림이다"

곡선을 고친 뒤에도 사용자: *"많이 퍼져 보여. blur가 많이 들어간 느낌."* + ***"우측 패널 Exposure를 움직이면 화면 밝기가 변하는데, 이전 조명 로직이 남은 건 아닌지 확인이 필요하다."*** → **지적이 정확했다.**

- 톤매핑이 **`LinearToneMapping`**(에디터·뷰어 공통). 곡선이 없어 **1.0 초과는 하드 클리핑**.
- 환경광이 **1.6**. 흰 브릭 기준으로 이렇게 나가고 있었다:

| | 계산값 | 화면 |
|---|---|---|
| 윗면 | 1.60 | **1.00** |
| 앞뒤 면(0.72) | 1.15 | **1.00** ← 윗면과 구분 안 됨 |
| 밑동 AO(0.70) | 1.12 | **1.00** ← **AO가 아예 안 보임** |
| 좌우(0.5) / 밑면(0.35) | 0.80 / 0.56 | 0.80 / 0.56 |

밝은 쪽이 통째로 뭉개지다 어느 지점부터 갑자기 그라데이션이 시작되니 **경계 없이 번지는 것처럼** 보인다.
**오늘 넓힌 면 배율(0.8→0.72)도 흰 브릭에선 효과가 0이었다** — 잘려서 윗면과 같은 값이었다.

→ **`BRICK_AMBIENT` 1.6 → 1.0.** 잘림이 사라져 면 배율·AO 전 구간이 표현되고, **고른 색도 그 색 그대로** 나온다(1.6배는 모든 색을 흰색 쪽으로 띄운다).
대신 씬이 전보다 어두워진다 — 밝기는 **배경·안개 색**으로 맞춘다(환경광을 올리면 같은 함정으로 돌아간다). 함정 **D-2b**로 등록.

> **교훈**: AO 세기와 곡선을 두 번 헛돌린 이유는 **출력이 잘리고 있는지 먼저 확인하지 않아서**다.
> "룩이 흐릿하다"에서 제일 먼저 볼 것은 조명 파라미터가 아니라 **밝은 면들이 서로 구분되는가**이다.
> 그리고 이번 원인은 **사용자가 화면에서 짚어 준 단서**(Exposure 슬라이더)로 찾았다.

**미결**: 우측 패널 **Exposure 슬라이더**(구 씬 스키마 `EnvSchema.toneMappingExposure`) — 죽은 코드는 아니고 실제로 동작한다.
다만 Linear라 **1보다 올리면 잘림만 심해진다.** 그대로 둘지/뺄지 미정.

**✅ ⑤ 조명 마무리** — 접촉 그늘·명암·밝기 전부 확인 완료.

**남은 확인(저위험, 증상 나오면)**
1. 드래그로 여러 개 놓았을 때 손 뗀 직후 끊기지 않는지 — 빛 재계산 후 **리전 전량 재업로드** 비용.
   느껴지면 "값이 변한 칸이 속한 리전만" 올리는 방식으로 좁힌다.
2. **미결 — 우측 패널 Exposure 슬라이더**(구 씬 스키마 `EnvSchema.toneMappingExposure`). 죽은 코드는 아니고 실제로 동작한다.
   다만 톤매핑이 Linear라 **1보다 올리면 잘린다**(환경광이 이제 `π`=알베도 1:1이므로 그 위는 전부 잘림). 그대로 둘지/뺄지 미정.

### 2026-07-31 — 💡 ⑤ 부드러운 조명(정점 단위 밝기) — 브라우저 확인 대기

tsc·lint 클린 + 월드 95 → **96/96** + 나머지 유지(21/248/34/25/43) + 컴파일 클린.

면마다 값이 하나였을 땐 **면 전체가 통짜로 같은 밝기**라, 벽 모서리·구덩이 안이 계단처럼 각져 보였다.
이제 브릭의 **꼭짓점 여덟 개** 밝기를 넘기고 정점마다 섞어, 래스터라이저가 면 안을 보간한다.

#### 설계 — 인스턴싱을 그대로 두고 값만 늘렸다

| | 전 | 후 |
|---|---|---|
| 인스턴스 속성 | `aLightA/B` (vec3 ×2 = **6면**) | `aCornerA/B` (vec4 ×2 = **8꼭짓점**) |
| 지오메트리 속성 | — | `aSelA/aSelB` (삼선형 가중치) |
| 면 배율(FACE_TONE) | CPU에서 곱함 | **셰이더**가 월드 법선으로 곱함 |

- **왜 "면 24값(6면×4꼭짓점)"이 아니라 "꼭짓점 8값"인가**: 24값이면 vec4 속성이 6개 필요하고,
  `instanceMatrix`가 이미 4칸을 먹어 **속성 한도(16)에 위험하게 붙는다**. 꼭짓점 8값이면
  **속성 개수가 예전과 같다**(vec4 둘). 대신 한 꼭짓점을 세 면이 공유하므로 미세하게 섞이는데,
  각 면이 **자기 쪽 네 꼭짓점**만 쓰므로 실사용에선 티가 안 난다.
- **왜 one-hot이 아니라 삼선형 가중치인가**: 상자 모서리 정점은 꼭짓점 위라 어느 쪽이든 같지만,
  **돌기·모따기·쐐기**의 정점은 면 한가운데에 있다. one-hot이면 그런 정점이 엉뚱한 값을 받아 얼룩진다.
  삼선형이면 상자에서는 자동으로 one-hot이 되고 나머지는 자연스럽게 섞인다 — **경사·돌기까지 공짜로** 처리된다.
- **★ 면 배율은 반드시 월드 법선으로**: 브릭은 Y축으로 90°씩 도는데 x면(0.6)과 z면(0.8) 배율이 다르다.
  로컬 법선을 쓰면 **회전한 브릭의 옆면 밝기가 뒤바뀐다.** 셰이더에서 `mat3(instanceMatrix) * normal`로 돌려 쓴다.
- **CORNER_MAP은 손으로 안 적고 계산한다** — `FACE_MAP`과 어긋나면 **밝기만 조용히 뒤집힌다**(에러가 안 난다).

#### 연쇄 정리
`world.faceLight`와 `FACE_MAP` 제거(고아). 월드 테스트의 빛 항목을 꼭짓점 기준으로 다시 썼다.
특히 **"걸쳐 놓은 브릭이 아래 타일을 어둡게 만들지 않는다"** 는 항목은 뜻이 조금 달라졌다 —
꼭짓점 단위에선 **덮인 쪽 꼭짓점만 어두워지는 게 정상**(그게 부드러운 조명의 목적)이라, **먼 쪽 꼭짓점이 그대로**인지를 본다.

#### 남은 확인 — 성능
굽는 비용이 면 6개 → 꼭짓점 8개 × 8칸으로 늘었다(브릭당 대략 50 → 64 조회). 계획서 기준은
**"빛 계산이 50ms를 넘으면 되돌린다"** — 지금 실측값은 12ms였다. 브라우저 통계의 `빛 칸 · 계산`으로 확인할 것.

**확인 필요(브라우저)**
1. 벽 모서리·구덩이 안이 **부드럽게 어두워지는지**(계단 무늬가 사라졌는지)
2. 브릭을 **회전(R)** 해서 놓아도 옆면 밝기가 자연스러운지 — 뒤바뀌면 월드 법선 배선이 틀린 것
3. 경사·돌기 표면에 **얼룩**이 없는지
4. 통계의 **`빛 칸 · 계산`(ms)** — 50ms 넘으면 알려줄 것
5. 발광 브릭·반투명은 그대로인지

### 2026-07-31 — 🔺 ④ 경사 파츠 3종 — ✅ 확인 완료(사용자 "잘된다")

tsc·lint 클린 + 신규 지오메트리 22/22 + 월드 83 → **95/95** + 저장 212 → **248/248**(파츠가 늘어 왕복 케이스도 늘었다) + 나머지 유지.

**파츠 갈래에 `경사` 추가** — `1×2 경사` · `2×2 경사` · `1×2 역경사`(처마·계단 밑 마감). `R`로 네 방향.
경사면은 **+Z로 내려간다**(회전 0 기준).

- `PARTS`에 **`shape: 'box' | 'slope' | 'slopeInv'`** 추가. 미지정 = 상자라 **기존 파츠는 전부 무변경**.
- `PART_ORDER`는 **뒤에만** 붙였다. 골든 픽스처 테스트가 옛 바이트열을 그대로 지킨다(248/248).
- **돌기 없음** — 비스듬한 면에 원기둥을 세우면 레고에도 없는 모양이 되고 폴리곤만 는다.
  덕분에 **삼각기둥 8삼각형**으로 상자(12)보다도 가볍다.

#### ★ 핵심은 지오메트리가 아니라 **가시성 규칙**

지금 규칙은 "표면에 닿은 칸이 전부 차 있으면 안 그린다"인데, 경사는 **칸을 차지하면서도 대각선이 뚫려 있다.**
경사를 '막힘'으로 치면 그 너머 브릭이 안 그려지고 **뚫린 쪽으로 구멍**이 보인다.

→ `occludes(part)` 도입: 경사는 **칸은 점유하되(그 자리에 못 놓음) 이웃 면을 감추지 않는다**(non-occluding).
마인크래프트가 계단·반블록에 쓰는 규칙과 같다. 면 몇 개가 더 그려지는 비용은 지붕 한 면 수준이라 무시할 만하다.

#### 🐛 사보타주가 잡은 **테스트 자체의 결함**

`occludes`를 항상 `true`로 바꾸는 사보타주를 넣었는데 **테스트가 그대로 통과했다.**
원인은 코드가 아니라 테스트였다 — 1×1 브릭을 **여섯 면 모두 경사로** 감싸려 했는데,
경사는 1×2라 **서로 겹쳐 배치가 실패**했고(=면이 안 막힘) 가운데가 보이는 이유가 엉뚱했다.
→ 다섯 면은 상자, **한 면만 경사**로 바꾸고 각 배치가 성공했는지도 확인하게 고쳤다. 이제 사보타주가 정확히 그 항목을 깬다(94/95).

> **교훈**: "감싸서 안 보이게 만든다" 같은 테스트는 **감싸기가 실제로 성공했는지**부터 단언해야 한다.
> 배치가 조용히 실패하면 **원하는 결과가 엉뚱한 이유로** 나온다.

#### 🐛 경사면이 텅 비어 보이던 것 (사용자 보고: *"경사면이 비었어"*)

**원인 — 면의 감기 방향(winding)이 뒤집혔다.** 삼각형 변을 `a→b`로 감았더니
**사각면 세 장의 법선이 전부 안쪽**을 향했고, 뒷면 컬링에 잘려 아무것도 안 보였다.
(옆면 삼각형 두 장은 법선을 손으로 박아 넣어서 멀쩡했다 — 그래서 "일부만" 비어 보였다.)

**수정**: 변을 `b→a` 방향으로 감는다.

#### 이걸 못 잡은 테스트를 보강했다 — 그리고 그 보강도 한 번 틀렸다

기존 테스트는 **치수·삼각형 수·높이 프로필**만 봤다. 전부 맞아도 **뒤집힌 면은 그대로 통과**한다.
→ 불변식 추가: **볼록 도형이므로 모든 면의 법선은 바깥을 향한다.**

⚠️ 그런데 처음엔 기준점을 **원점**으로 잡았다가 멀쩡한 면이 3장 걸렸다 —
**쐐기는 경사면이 원점을 정확히 지나서** 내적이 0이 된다.
→ 기준점을 **모든 꼭짓점의 평균**(볼록 결합이라 항상 도형 안)으로 바꿨다.

※ 사보타주(감기를 되돌림)로 **6장 뒤집힘**이 잡히는 것 확인. 테스트 22 → **25/25**.

> **교훈**: 지오메트리 테스트에서 **치수만 재면 절반만 검사한 것**이다.
> 화면에 나타나는 사고의 절반은 **감기 방향**에서 나온다 — 그건 숫자가 아니라 **불변식**으로 잡아야 한다.

**확인 필요(브라우저)**
1. 파츠 패널에 **`경사` 탭** — 1×2 · 2×2 · 역 1×2
2. 경사로 **지붕 만들기** — `R`로 네 방향 돌려 마감
3. **경사 옆·아래에서 구멍이 안 보이는지**(가시성 규칙이 이걸 막는다)
4. 드래그로 경사 여러 개 깔기 · `Ctrl+Z` 한 번에 복구
5. 새로고침 후에도 경사가 그대로 뜨는지(저장 왕복)

### 2026-07-31 — 🧱 §1-b 연속 놓기 + 자리 미세조정 은퇴 — ✅ 확인 완료(사용자 "잘돼")

tsc·lint 클린 + 획 잠금 11 → **17/17** + 스토어 42 → **43/43** + 나머지 유지(21/212/81).

선택 도구를 철회하며 남은 진짜 필요("넓게 깔기")를 **새 개념 없이** 풀었다.

- **놓기도 지우기처럼 획이다** — 누른 채 끌면 지나가는 자리에 계속 놓인다. 한 획 = 되돌리기 한 번.
- **자리 미세조정(`SlidePlane`) 은퇴** — 놓고 끌어서 옮기던 기능.
  > 그건 **배치가 한 칸씩 어긋나던 시절의 보정책**이었다. 그 문제는 고쳤고("겨눈 칸의 옆 칸") 사용자도 확인했다.
  > 존재 이유가 사라진 기능을 남겨 두니 **연속 놓기를 막는 걸림돌**이 돼 있었다.

**`eraseStroke.ts` → `strokeLock.ts`로 승격**(놓기·지우기 공용).

**★ 놓기와 지우기는 판정 대상이 다르다** — 이게 이 항목의 핵심이다.
- 지우기: **커서 밑 브릭**을 본다(갈래·층·거리).
- 놓기: **새 브릭이 앉을 자리**를 본다(층·거리). **갈래는 안 본다.**
  바닥을 훑으면 커서는 곧 **방금 놓은 브릭의 옆면**을 짚게 되는데, 짚은 것의 종류·층으로 판정하면
  **한 칸 놓자마자 획이 끊긴다.** 놓일 칸의 층만 잠그면 이어진다.

**연쇄 정리**: `moveBrick`(스토어) — 미세조정 전용이라 고아가 됐다. 함께 `compact()`도 뺐다.
그건 `moveBrick`이 만들던 `add/del` 쌍을 상쇄하려고 있었는데, **이제 그런 쌍을 만드는 곳이 없다**
(놓기 획은 `add`만, 지우기 획은 `del`만). id 어긋남 방어는 `resolveId`가 계속 맡는다.

**테스트**: 획 잠금에 놓기 6개 추가(같은 층만·위/아래층 차단·도달 거리·**갈래 무시로 이어짐**).
스토어 ③번을 "드래그로 자리 고치기" → **"연속 놓기 한 획"** 으로 교체(6개 놓고 `undo` 한 번, `redo`로 복구).

#### 🐛 연속 놓기가 **엉뚱한 방향으로** 생기던 것 (사용자 보고, 즉시 수정)

> *"이것도 지울 때와 마찬가지로 드래그하면 엉뚱한 방향에서 생겨버리네."*

**원인 — 면(hover) 기준으로 붙였다.** 드래그 중에는 커서가 **방금 놓은 브릭의 옆면**을 짚게 되는데,
그 면은 대개 **카메라를 향한 앞면**이다. `anchorFromFace`가 "그 면의 바깥 칸"에 붙이므로
새 브릭이 **커서 방향이 아니라 내 쪽으로** 붙고, 그게 계속 이어져 화면 앞으로 줄이 생겼다.

**수정 — 면이 아니라 평면.** 첫 클릭에서 잡은 **표면의 평면**에 커서를 투영해 그 칸에 놓는다(신규 `StrokePlane`).
평면은 브릭에 가려지지도, 방향이 바뀌지도 않는다 — 커서가 가리키는 **표면 위 그 자리**가 그대로 나온다.
같은 칸에 매 프레임 다시 시도하지 않도록 이 획에서 시도한 칸을 `Set`으로 기억한다.

> **아이러니**: 바로 앞 단계에서 지운 `SlidePlane`이 **정확히 이 수학**이었다.
> 도구(평면 투영)는 맞았고 쓰임새(브릭 하나를 옮기기)가 틀렸던 것이다. 지울 때 그걸 못 알아봤다.

> **교훈**: **"어디에 붙일까"(단발 클릭)와 "어디를 훑고 있나"(드래그)는 다른 질문이다.**
> 전자는 면 방향이 답이고, 후자는 평면 위 좌표가 답이다. 같은 함수로 풀려다 방향이 틀어졌다.

#### 🐛 벽(측면)에서 줄이 어긋나던 것 (사용자 보고: *"바닥은 깔리지만 측면은 원하는 동작이 안 된다"*)

**원인 — 자유 축의 칸을 그대로 앵커로 썼다.**
벽은 **세로(y)가 자유 축**인데 **브릭 높이가 3칸**이다. 커서가 브릭 중간 높이(y=1,2)를 지나면
앵커가 그 칸으로 잡혀 **한두 칸 뜬 줄**이 놓인다. 바닥에서 티가 안 났던 건
세로가 **잠긴 축**이었고 가로 간격이 우연히 파츠 폭과 맞아떨어졌기 때문이다.

**수정 — `strokeAnchor()`**: 자유 축을 **첫 브릭 기준으로 파츠 크기 단위**로 스냅한다(레고를 격자에 붙이듯).
`anchor[k] = 첫앵커[k] + floor((칸[k] − 첫앵커[k]) / 파츠크기[k]) × 파츠크기[k]`

**테스트 +8 (17 → 25/25)**: 브릭 높이 중간을 짚어도 같은 줄 · 한 켜 위라야 다음 줄 ·
가로도 폭 단위 스냅 · **음수 쪽도 내림으로 격자 유지**(반올림이면 0 근처에서 한 칸 틀어진다) ·
격자 원점은 **첫 브릭**(원점이 아니어도).

> **교훈**: 격자 편집에서 **"커서가 있는 칸"과 "브릭이 앉을 칸"은 다르다.**
> 파츠가 여러 칸을 차지하면 그 차이가 곧 어긋남으로 보인다. 자유 축마다 **파츠 크기로 스냅**해야 한다.

#### 🐛 벽 앞면 드래그가 X가 아니라 Y로 쌓이던 것 (사용자 보고)

> *"브릭 두 개를 쌓고 맨 위 브릭의 측면을 클릭·드래그하면 X방향이 아니라 Y방향으로 추가된다."*

**원인 — 이미 있던 규칙을 안 썼다.** `anchorFromFace`는 면마다 **움직일 수 있는 축**을 이미 계산해서 준다.

```ts
// 수평면(윗면·밑면)이면 XZ 두 방향, 수직면이면 그 벽을 따라 수평 한 방향만
const slide = face.axis === 1 ? [0, 2] : [face.axis === 0 ? 2 : 0];
```

**벽면은 수평 한 축뿐이고 세로는 고정**이다. 그런데 내 획 잠금은 "면 축만 잠그고 나머지 둘은 자유"로 만들어서
**세로까지 열려** 있었다. 커서가 조금만 위아래로 흔들리면(가까이서 보면 브릭 한 켜 = 0.6m라 몇 십 픽셀이다)
그대로 윗줄에 놓였다.

**수정**: `PlaceLock`에 `slide`를 넣고, `strokeAnchor`가 **`slide`에 있는 축만** 움직인다(나머지는 첫 브릭 자리 고정).
→ 벽 앞면 = 그 벽을 따라 **가로 한 줄**, 윗면 = **XZ 평면**. 벽을 위로 쌓으려면 한 켜씩 훑는다(예측 가능).

**테스트 25 → 26/26**: 커서가 위로 흔들려도 세로 불변 · 한참 위를 짚어도 같은 줄 · 가로는 파츠 폭 단위 ·
음수 쪽 내림 · 윗면은 XZ 둘 다 열림.
※ 사보타주("면 축만 빼고 전부 자유"로 되돌림)로 **정확히 그 네 항목**이 깨지는 것 확인.

> **교훈**: 같은 문제를 이미 푼 코드가 옆에 있는데 **새로 규칙을 지어냈다.**
> `slide`는 드래그 미세조정 시절부터 있던 값이고, 미세조정을 지우면서 **그 규칙까지 같이 버린 것**이 원인이다.
> 기능을 걷어낼 때 **그 기능이 들고 있던 규칙이 다른 데도 필요한지** 먼저 봐야 한다.

#### 🐛 옆면 드래그가 끈 방향이 아니라 "앞뒤"로 붙던 것 → **면마다 규칙을 나눴다 (C안)**

> 사용자: *"브릭 우측면을 클릭·드래그하면 오른쪽이 아니라 정면 기준 뒤로 추가된다."*

**원인**: 규칙이 **"클릭한 면이 이루는 평면을 칠한다"** 하나였다. 우측면(법선 X)을 클릭하면 평면은 **YZ**이고,
그 위에서 움직일 수 있는 축은 **Z(앞뒤)** 뿐이다. 그래서 오른쪽으로 끌어도 앞뒤로 줄이 생긴다.
윗면에서는 두 모델(면 칠하기 / 끈 방향)의 결과가 같아서 문제가 안 보였고, **옆면에서 갈라졌다.**

**수정 — 면의 성격에 따라 뜻을 나눈다.**

| 클릭한 면 | 드래그 |
|---|---|
| **수평면**(윗면·밑면) | 그 **평면을 칠한다** — 바닥 한 층을 한 획에 |
| **수직면**(옆면) | **끈 방향으로 한 줄** — 오른쪽·위·뒤 전부 |

- 수직면은 **카메라를 마주 보는 평면**(`CameraPlane`)으로 커서를 잰다. 면의 평면으로는 그 위 두 축밖에 못 재는데,
  "끈 방향"을 알려면 **면 밖으로 나가는 축(법선)** 까지 필요하다.
- **축은 첫 움직임에서 한 번만 고르고 고정**한다(`dominantAxis`). 매 프레임 다시 고르면 손이 떨릴 때 줄이 방향을 바꾼다.
- 한 걸음은 **파츠 크기**(2×4면 2칸 또는 4칸), **절반 넘게 가야 한 걸음**(`axisSteps`, 반올림) —
  미세한 떨림에 줄이 늘었다 줄었다 하지 않는다. 중간 칸도 전부 채워 **빨리 끌어도 줄이 안 끊긴다.**

**테스트 26 → 34/34** (줄 긋기 8개: 축 고르기 · 부호 · 반올림 경계).

#### 결정 — 쌓기는 옆면이 담당 (보류 항목)

> 사용자 질문: *"윗면 클릭 후 위로 드래그하면 쌓여야 하나?"* → **아니다. 같은 층으로 퍼진다.**

윗면 드래그에서 "위"는 원근상 **멀리**를 뜻하므로, 쌓기까지 맡기면 **한 동작에 두 뜻**이 된다.
쌓기는 **옆면 클릭 + 위로 드래그**(한 번에 기둥)나 **윗면 반복 클릭**(한 개씩)으로 한다.

> **보류(사용자 결정)**: *"일단 이렇게 가고 나중에 불편하거나 유저 요구가 생기면 그때 하자."*
> 착수 조건 = "윗면에서도 위로 끌면 쌓였으면 좋겠다"는 요구가 실제로 나올 때.
> 그때 방법은 있다 — 윗면도 카메라 기준으로 방향을 재서 **위로 끌면 기둥 / 옆으로 끌면 평면**으로 가른다.
> 대신 바닥 깔 때 손이 위로 치우치면 기둥이 서 버리는 위험을 감수해야 한다.

**확인 필요(브라우저)**
1. **옆면 클릭 + 오른쪽 드래그** → 오른쪽으로 줄이 늘어나는지
2. **옆면 클릭 + 위로 드래그** → 기둥이 서는지
3. **윗면 드래그** → 바닥이 평면으로 퍼지는지
4. **클릭 한 번**은 여전히 한 개
5. `Ctrl+Z` **한 번**에 그 획이 통째로 사라지는지 · `Ctrl+Y` 복구
6. 지우기 드래그는 그대로인지

### 2026-07-31 — ❌ ③ 영역 선택/복사 **철회** (만든 것을 되돌림)

> 사용자: *"select가 작업 범위 정할 때 없었던 것 같은데 언제 튀어나왔나. 사용성 이게 뭐지.
> 너무 복잡한 단계이고 이것 때문에 다른 기능과 진도에 걸림돌이나 리스크가 있다면 빼는 게 맞다."*

tsc·lint 클린 + 브릭 테스트 21/212/81/11/42(선택 관련 8개 제거) + `/test/brick` 200.

**출처는 나다.** 되돌리기를 끝낸 뒤 내가 "다음 할 일"에 후보로 적었고, 그게 사용자의 8개 목록으로 옮겨 갔다.
그 뒤 계획서에서 내가 **"브릭에서 선택이 무엇인가를 정하는 일"** 이라며 3순위로 올렸고,
심지어 **"놓기 연속 배치를 안 만드는 근거"** 로도 썼다. 사용자가 요청한 적은 없다.

**철회 사유**: ①개념 일곱 개(도구 전환·드래그·↑↓·Ctrl+C·Ctrl+V·클릭·Esc)를 배워야 바닥 한 장을 깐다
②이미 수식키 충돌로 버그가 났고, 앞으로 경사·걷기·협업이 전부 "선택 상태"를 신경 써야 한다
③핵심 루프(짓기·되돌리기·저장·게시)는 이것 없이 이미 돈다.

**삭제**: `lib/brick/selection.ts`(+테스트 14) · `SelectionBox.tsx` · 스토어 `selection`/`clipboard`/`copySelection`/
`deleteSelection`/`pasteAt` · `Tool`의 `'select'` · `BrickBuilder`의 영역 드래그·`RegionPlane`·붙여넣기 고스트·단축키 ·
도구 패널 버튼 · 스토어 테스트 8개.

**대체안 `§1-b 연속 놓기`를 계획서에 세웠다** — "넓게 깔기"라는 **진짜 필요**만 남기고,
지우기의 획 잠금(`eraseStroke.ts`)을 **그대로 재사용**한다. 새 개념이 하나도 안 늘어난다.

> **교훈**: *"이게 무엇인가를 정하는 일"* 같은 명분은 **강해 보이지만 검증되지 않은 이유**다.
> 그 말로 3순위를 정당화했고, 심지어 다른 항목(연속 놓기)을 미루는 근거로까지 썼다.
> 실제로 필요했던 건 **"넓게 깔기"** 한 줄이었고, 그건 훨씬 싸게 풀린다.

#### 🔴 이 과정에서 내가 저지른 사고 — `git checkout`으로 확인된 기능을 날렸다

선택 코드를 걷어내려고 스크립트를 돌리다 실패하자 **`git checkout src/components/brick/BrickBuilder.tsx`** 를 실행했다.
그 파일엔 **커밋되지 않은 확인 완료 기능**(연속 드래그 지우기 + 획 잠금 배선)이 들어 있었고 **그대로 사라졌다.**
`eraseStroke.ts`는 추적 전(untracked) 파일이라 살아남아, 배선만 손으로 복원했다.

> **규칙(G-5)**: **`git checkout <file>`은 되돌리기 도구가 아니다.** 커밋 안 된 작업을 조용히 지운다.
> 스크립트 편집이 꼬이면 **되돌릴 게 아니라 그 편집을 손으로 고친다.** 정말 되돌려야 하면 먼저 `git stash`.

### 2026-07-31 — ⬚ ③ 영역 선택 / 복사 (구현 로그 삭제됨)

> 이 기능은 바로 위 항목에서 **철회**되어 코드가 남아 있지 않다. 구현 상세 로그는 지웠다 —
> 철회 사유는 위 항목과 `BRICK_PLAN.md`에, 이때 배운 함정(G-5)은 `BRICK_PITFALLS.md`에 남아 있다.

### 2026-07-31 — 🎨 ② 자유 색 선택 — 브라우저 확인 대기

#### 🐛 하이드레이션 깨짐 (사용자 보고, 즉시 수정)

> `Hydration failed because the server rendered HTML didn't match the client.`

**원인**: `recentColors`를 **스토어 초기값에서 localStorage로 읽었다.**
서버엔 localStorage가 없어 `[]`, 클라이언트엔 저장된 색이 있으니 **서버 HTML(최근 줄 없음)과 클라이언트 렌더(최근 줄 있음)가 어긋난다.**

**수정**: 초기값은 **반드시 빈 배열**로 두고, `hydrateRecentColors()`를 `BrickToolPanel`의 `useEffect`에서 부른다.

> **규칙**: `localStorage` · `window` · `navigator` 같은 **브라우저에만 있는 값은 스토어 초기화에서 읽지 않는다.**
> 클라이언트 컴포넌트도 **서버에서 한 번 렌더된다** — 초기값이 다르면 그 자리에서 하이드레이션이 깨진다.
> 반드시 **렌더가 끝난 뒤(effect)** 채워 넣는다.

tsc·lint 클린 + 전 브릭 테스트 유지(21/212/81/11/42).

- **신규 `components/brick/BrickColorPicker.tsx`** — 채도·명도 사각형 + 색상 띠 + HEX 입력. **브릭 전용, 자체 완결.**
  > 구 `ColorPicker`(600줄+)는 되살리지 않았다 — 씬 `colorAssets`·그라데이션·스포이드·전역 싱글턴 팝업이 얽혀 있고,
  > 브릭에 필요한 건 **"색 하나 고르기"** 뿐이다. 색 변환은 살아남은 `lib/color.ts`를 그대로 쓴다.
  > **팝업(포털)이 아니라 패널 안에서 펼쳐진다** — 도구 패널이 이미 고정돼 있어 띄울 이유가 없다(바깥클릭·위치계산·z-index가 안 따라온다).
- **저장은 손댈 것이 없었다** — 청크 팔레트가 `[재질, r, g, b]`로 **임의 RGB를 이미 담고 있다.** 같은 색은 팔레트에 한 번만 들어가 용량도 안 는다.
- **최근 색 10개** — 팔레트 아래에 뜨고 **기기에 남는다**(씬을 옮겨도 유지).

#### 계획서와 어긋난 점 1 (문서를 먼저 고쳤다)
최근 색을 `BrickStorage.saveSettings`에 얹으려 했으나 **Supabase 어댑터의 `saveSettings`/`loadSettings`가 no-op**이다 —
**에디터에선 아무것도 저장되지 않는다**(`studStyle`도 사실 프로토타입에서만 유지되고 있었다).
게다가 최근 색은 씬 데이터가 아니라 **기기/사용자 취향**이라 씬마다 달라지는 것도 이상하다.
→ 신규 `lib/brick/colorPrefs.ts`가 **씬과 무관한 localStorage 키 하나**로 관리한다.

#### 만들며 걸러낸 것 셋
1. **`useCustomColor`라는 이름을 버렸다** — `use`로 시작하면 React가 훅으로 본다(스토어 액션인데 훅 규칙에 걸린다). → `rememberColor`.
2. **여는 순간 최근 색이 쌓이던 것** — 색 방출을 `useEffect([hex])`로 하니 고르개를 열자마자 현재 색이 다시 적용됐다.
   → **조작 이벤트에서 그 자리에 내보낸다**(`apply` 안에서). 렌더 결과를 보고 부모 상태를 바꾸는 모양도 함께 사라졌다.
3. **드래그 중 색이 전부 최근 목록에 쌓이던 것** → `onChange`(실시간)와 `onCommit`(손 뗐을 때 한 번)을 분리. 최근 색은 commit에서만.

**확인 필요(브라우저)**: 색 팔레트 끝 **무지개 `+` 버튼** → 고르개 펼침 · 사각형/띠 드래그로 색이 **실시간으로** 바뀌는지 ·
HEX 입력 · 손 떼면 **최근 줄에 쌓이는지**(중간 색은 안 쌓임) · **새로고침·씬 전환 후에도 최근 색 유지** · 임의 색으로 놓고 새로고침 시 색 유지.

### 2026-07-31 — 🛡 지우기 획 잠금 (`eraseStroke.ts`) — 브라우저 확인 대기

> 사용자 보고: *"바닥 위 브릭을 Ctrl+클릭해 아래로 드래그하면 그 브릭이 지워지고 **드래그하는 동안 그 자리의 바닥까지 사라진다.**
> 바닥뿐 아니라 포인터가 지나가는 자리가 **거리와 상관없이 무조건** 지워진다."* — 연속 지우기를 넣자마자 나온 치명적 결함.

tsc·lint 클린 + **신규 테스트 11/11** + 브릭 스토어 42/42 · 월드 81/81 유지.

**원인**: 지우기 판정이 **커서 밑에서 가장 가까운 것**을 집는다. 그런데 스윕은 *표면을 따라가는* 게 아니라
**화면을 가로지른다** — 커서가 브릭을 벗어나는 순간 그 뒤의 전혀 다른 것이 잡힌다. 거리 제한도 없었다.

> **이 조작 방식이 원래 안고 있는 위험이다.** 마인크래프트 크리에이티브도 같은 사고가 나는데,
> 거기선 ①크로스헤어 화면 중앙 고정 ②도달 거리 5블록 ③(서바이벌) 캐는 시간이 완충 역할을 한다.
> 우리는 **자유 커서**라 완충이 하나도 없었다.

**신규 `lib/brick/eraseStroke.ts` — 첫 클릭에서 획을 잠근다(A+B, 사용자 확정)**
- **(A) 같은 갈래 · 같은 층** — 브릭 획은 지형을 안 건드리고, 첫 면의 축 기준 **한 겹**만 훑는다
- **(B) 도달 거리 12m** — 화면 저 끝까지 훑었을 때의 사고를 막는 2차 그물
- 못 지울 것은 **빨간 강조도 안 한다** — 빨갛게 떠 있는데 안 지워지면 고장으로 보인다

**★ 층 판정은 앵커 좌표로 한다(히트 지점이 아니라).**
이웃을 지우고 나면 그 옆 브릭의 **노출된 옆면**이 잡히는데, 히트 지점으로 층을 재면 같은 층인데도 걸러져
**획이 중간에 멈춘다** — 그것도 카메라 각도에 따라 되다 안 되다 한다. 앵커로 재면 어느 면을 짚든 같은 값이다.

**감수하는 점**: 한 획으로 **여러 층은 못 지운다**(3층 벽을 앞에서 훑으면 앞면 한 겹). 예측 가능한 쪽이 낫다 —
의도치 않게 파괴되는 것보다 한 번 더 끄는 편이 싸다.

**테스트 11/11**(`npx tsx src/lib/brick/eraseStroke.test.ts`): **보고된 사고(브릭 획 → 지형)** · 반대(지형 획 → 브릭) ·
같은 층은 파츠/높이가 달라도 지움 · 뒤쪽 층·위층·아래층은 안 지움 · 도달 거리 · **옆면을 짚어도 이어짐**.
※ 사보타주(갈래 가드 제거)로 **정확히 그 항목이 깨지는 것** 확인(10/11).

**확인 필요(브라우저)**: ①**보고하신 그 동작** — 바닥 위 브릭을 지우며 아래로 끌어도 바닥이 안 사라지는지
②벽 앞면 훑기 · 바닥 한 층 걷어내기는 여전히 되는지 ③지형 드래그로 파기 ④`Ctrl+Z` 한 번에 전부 복구.

### 2026-07-31 — 📋 남은 작업 계획서(`BRICK_PLAN.md`) + ① 연속 드래그 지우기 — 브라우저 확인 대기

> 사용자: *"하는 도중에 수정이 너무 빈번히 일어나는 거 같다. 남은 작업별로 계획을 세우고 시작하자."*
> → 신규 **`doc/BRICK_PLAN.md`**(AGENTS.md에서 자동 로드). 항목마다 **기대 동작 · 안 하는 것 · 방식 · 닿는 파일 · 검증 · 위험**을 못 박았다.
> 규칙: **여기 안 적힌 건 안 만든다.** 어긋나는 상황이 나오면 코드가 아니라 **문서를 먼저 고치고 확인을 받는다.**

**계획 단계에서 바로잡은 것 넷** (만들기 전에 잡았다 = 이 문서의 값어치):
1. **`부드러운 조명`과 `정점 단위 밝기`는 같은 항목** — 둘 다 "면이 통짜라 각져 보이는 것"을 없애는 일이라 하나로 합쳤다.
2. **놓기 연속 배치는 안 만든다** — 지금 드래그가 **자리 미세조정**이라 정면 충돌한다. 넓게 까는 일은 **영역 복사**가 더 빠르고 정확하다.
3. **걷기에 물리 엔진을 다시 안 들인다** — 브릭은 정렬된 격자라 직접 푸는 편이 단순·결정적이고 **테스트가 된다**(순수 함수 `walk.ts`).
4. **협업에 CRDT를 다시 안 들인다** — 되돌리기가 이미 쓰는 **명령(place/remove)을 broadcast**하면 그대로 얹힌다. 충돌은 `place`가 `null`을 내는 것으로 이미 판정된다.

#### ★ 사용자 지적으로 폐기한 설계 — 영역 선택의 "2단계 드래그"

처음엔 바닥면 드래그 → 높이 드래그를 제안했다. 사용자: *"브릭을 움직여 원하는 위치에 놓는 것과 겹치지 않나?"*

도구가 다르므로 **직접 충돌하진 않는다**(좌버튼 = 지금 도구의 동작). 하지만 그 감이 가리킨 진짜 문제가 있었다 —
**두 번째 드래그가 "높이 조절"인지 "새 영역 시작"인지 화면에 안 보이는 상태로 갈린다.**
함정 E절의 **"버튼 하나에 두 뜻을 주지 말 것"** 을 그대로 어기는 구조다(우클릭이 회전이자 삭제였다가 터진 것과 같은 모양).

→ **드래그는 한 번(바닥면), 높이는 ↑/↓ 키.** 모드가 없어 "지금 무슨 상태지"가 안 생긴다.

#### ① 연속 드래그 지우기

tsc·lint 클린 + 브릭 스토어 **37 → 42/42**.

- `BrickBuilder`에 `eraseStrokeRef`(이번 획에서 지운 id `Set`) — 누름에 `beginBatch`, 뗌에 `endBatch`.
- 지우기 판정은 **이미 있는 `hoverBrick`을 그대로 쓴다** — 새 레이캐스트를 만들지 않았다.
- 같은 브릭이 한 획에서 두 번 처리되지 않는다(지운 직후 한 프레임 동안 같은 id가 다시 올라온다).
- **놓기 드래그는 무변경**(자리 미세조정).

> 지형을 드래그로 파도 `deepenAround`가 `filledTo`로 "채운 깊이"를 기억해 **아래로만 연장**하므로, 반복 호출이 낭비가 되지 않는다(별도 가드 불필요 — 확인함).

**테스트 +5**: 20개를 한 획으로 지우고 `undo()` **한 번**에 전부 복구 · 처음/끝 브릭이 제자리 · `redo()`로 다시 삭제.
※ 사보타주(배치를 op마다 한 단계로 쪼갬)로 **정확히 그 두 항목이 깨지는 것** 확인(40/42).
※ 단, 이 테스트는 **스토어 계약**을 잠근다 — `BrickBuilder`의 begin/end 호출 지점이 맞는지는 **브라우저에서만** 확인된다.

**확인 필요(브라우저)**: 지우기 도구로 **드래그해서 벽 허물기** · `Ctrl+Z` **한 번**에 전부 복구 · 놓기 드래그는 예전 그대로(자리 미세조정) · 지형도 드래그로 파지는지.

### 2026-07-31 — 🖱 마우스 조작 통일 (`BrickOrbit`) + 되돌리기 ✅

> 사용자: *"`/test/brick`과 통합된 에디터의 마우스 액션이 다르다. `/test/brick` 것을 적용해줘."* → **A안(프로토타입 그대로) + 타겟 y 클램프 해제** 확정.
> 되돌리기는 **✅ 확인 완료(2026-07-31, 사용자 "다 잘되")** — 놓기·드래그 자리 고치기·땅 파기·분기 폐기 전부.

tsc 클린 + lint 클린 + 브릭 스토어 37/37 + `/test/brick`·`/space` 200.

**★ 원인은 복붙이었다.** 같은 설정이 세 파일(프로토타입·에디터·뷰어)에 각자 적혀 있었고, **에디터만 오브젝트 시대 값**을 들고 있었다.

| | 프로토타입 | 에디터(전) |
|---|---|---|
| 우드래그 | **회전** | 패닝 (Ctrl+우라야 회전) |
| 가운데 드래그 | 패닝 | 줌 |
| 내려다보기 상한 | 89° | 85° |
| 위에서 보기 | 제한 없음 | 5.7°에서 막힘 |
| 휠 줌 속도 | 1 | 2 |
| 타겟 높이 | 자유 | **y ≥ 0 클램프** |

에디터 값은 전부 **이유가 이미 사라진 것들**이었다 — 좌클릭이 마퀴 선택이라 회전을 우버튼에서 밀어냈고, `target.y ≥ 0`은 바닥 **평면**이 있던 시절의 방어다. **지금은 지형을 파고 구덩이 안을 들여다봐야 해서 오히려 방해**가 된다.

**신규 `components/brick/BrickOrbit.tsx` — 세 화면이 같이 쓴다.** 설정을 바꿀 일이 생기면 이 파일만 고친다.
- 조작: 좌 = 브릭(orbit 아님) · **우드래그 = 회전** · 가운데 드래그 = 패닝 · 휠 = 줌
- 뺀 것: `target.y` 클램프 · `minPolarAngle` · `screenSpacePanning=false` · `zoomSpeed=2` · damping

**`mode` prop — 뷰어만 다르다(의도)**: `view`면 **좌드래그로 돈다.** 방문자는 짓지 않으므로 좌버튼을 비워 둘 이유가 없고, 3D 뷰어에서 좌드래그가 안 돌면 아무도 조작법을 못 찾는다. **이건 드리프트가 아니라 역할 차이**라 prop으로 명시했다.

> **교훈**: 같은 설정을 두 곳에 적으면 갈라진다 — 이번엔 **사용자가 먼저 발견**했다.
> 이미 `BrickScene`·`BrickToolPanel`·`BrickEnvironment`에서 같은 이유로 뽑아 놓고도 OrbitControls만 빠뜨렸다.

**✅ 확인 완료(2026-07-31, 사용자 "다 잘된다")** — 에디터 우드래그=회전 · 가운데=패닝 · 구덩이 안 시점 · 프로토타입과 동일한 감각 · 뷰어 좌드래그 회전.

### 2026-07-31 — ↩️ 되돌리기 / 다시 실행 (명령 스택) — ✅ 확인 완료

> 사용자 확정: **기능만, UI 버튼은 나중에.** 단축키 `Ctrl+Z` · `Ctrl+Y`(=`Ctrl+Shift+Z`).

tsc 클린 + lint 클린 + **브릭 스토어 테스트 21 → 37/37**(되돌리기 16개 추가) + 다른 브릭 테스트 21/212/81 유지.

**★ 스냅샷이 아니라 명령을 쌓는다.**
구 오브젝트 에디터는 편집마다 `objects` 배열 전체를 복사했다. 브릭 월드는 기본이 1만 개가 넘고 무한히 커지므로
같은 방식이면 **브릭 하나 놓을 때마다 월드를 통째로 복사**하게 된다. 브릭 편집은 실제로 일어나는 일이
**추가·삭제 둘뿐**이라 그 둘만 기록하면 정확히 되돌아간다.

- `UndoOp = { kind:'add'|'del', id, b:BrickSnap }` · 한 단계(`UndoEntry`) = op 배열 · 깊이 **100**
- `beginBatch`/`endBatch`로 **한 제스처 = 되돌리기 한 번**(`BrickBuilder`의 pointerdown/up)
- 단축키는 `BrickBuilder`에 붙였다 — **짓는 화면에만** 있는 컴포넌트라(뷰어는 `readOnly`) 범위가 정확히 맞는다

#### 🐛 테스트가 잡은 버그 2개 (구현 직후, 브라우저 가기 전에)

1. **지형을 파면 되돌리기가 반쯤만 됐다.** 땅을 파면 `deepenAround`가 아래를 채우며 브릭을 수십 개 만드는데,
   그것들을 `record()`로 하나씩 기록해 **op마다 한 단계**가 됐다. Ctrl+Z 한 번은 판 브릭만 되살리고
   채워진 72개는 남았다. → **`recordAll`**(여러 op을 한 단계로) 추가.
2. **드래그로 자리를 고치면 경로에 잔재가 남았다.** `moveBrick`은 "지우고 다시 놓기"라 `add,del,add,del,…`이 쌓이는데,
   되돌리며 되살린 브릭은 **새 id**를 받는다. 그런데 뒤이어 처리되는 add op은 **옛 id**를 가리켜 아무것도 못 지웠다.
   → **`compact`**(배치 안에서 생겼다 사라진 쌍을 상쇄 → 남는 건 마지막 자리 add 하나) + **`resolveId`**(id가 없으면 그 앵커 칸에 있는 것으로 찾기).

> **사보타주로 확인**: 둘 다 끄면 정확히 그 두 항목이 깨진다(35/37). 하나만 꺼도 통과하는 건 서로를 덮어 주기 때문 —
> **의도한 이중 방어**다(`compact`는 기록을 줄이고, `resolveId`는 스트리밍으로 청크가 내려간 뒤에도 버틴다).

#### ⚠️ 되돌릴 수 없는 것 (그럴 땐 스택을 비운다)

- **전체 지우기** — 저장소까지 비우므로 메모리에 없던 청크는 복원할 길이 없다
- **스트레스 채우기**(프로토타입 전용) — 12만 op을 쌓을 이유가 없다

> **원칙**: 못 되돌리면서 되돌릴 수 있는 척하지 않는다. `resetHistory()`로 명시적으로 비운다.

#### 알아 둘 것

- **스트리밍은 기록하지 않는다** — 청크 로드/언로드는 사용자 편집이 아니다(store 액션을 안 거치고 `placeFast`로 들어간다).
  대신 멀리 갔다 와서 되돌리면 그 브릭이 메모리에 없을 수 있는데, 그땐 **조용히 건너뛴다**(중간에 던지면 월드가 반쯤 바뀐 채 멈춘다).
- `undoDepth`/`redoDepth`를 스토어에 노출해 뒀다 — 나중에 버튼을 달 때 활성/비활성에 바로 쓴다.

**확인 필요(브라우저)**: ①브릭 놓고 Ctrl+Z → 사라짐 · Ctrl+Y → 되살아남 ②드래그로 자리 고친 뒤 Ctrl+Z **한 번**에 통째로 사라지고 경로에 잔재 없음 ③땅 파고 Ctrl+Z → 구덩이가 **한 번에** 메워짐 ④전체 지우기 뒤엔 Ctrl+Z가 아무 일도 안 함 ⑤되돌린 뒤 새로 놓으면 Ctrl+Y가 안 먹음(분기 폐기).

### 2026-07-31 — 🔻🔻🔻 2d 3단계: 스키마 · 스토어 · 협업 (구 시스템 삭제 완료)

> **✅ 브라우저 확인 완료(2026-07-31, 사용자 "다 잘된다")** — 짓기/지우기 · 씬 전환 · 저장 · 게시 뷰어.
> 파일 122개를 지우고도 **동작이 하나도 안 깨졌다.** 지운 것들이 실제로 아무 데도 안 쓰이고 있었다는 뜻이다.

tsc 클린 + lint 에러 0(브릭/에디터/뷰어) + 브릭 테스트 21/212/81/21 + analytics 20/20 + sceneNav 6/6 + 라우트 8종 200.
**이 세션 누적 삭제 파일 122개** — `src`에 남은 파일은 134개다.

#### 핵심 축소 (줄 수)

| 파일 | 전 | 후 |
|---|---|---|
| `store/sceneStore.ts` | 2,551 | **90** |
| `types/scene.ts` | 700 | **70** |
| `panels/inspector/EnvironmentPanel.tsx` | 1,461 | **88** |
| `canvas/EditorCanvas.tsx` | 1,277 | **176** |
| `space/ViewerClient.tsx` | 1,093 | **120** |
| `panels/InspectorPanel.tsx` | 395 | **38** |
| `space/ViewerCanvas.tsx` | 472 | **57** |
| `panels/inspector/ui.tsx` | 445 | **88** |

#### ★ `scene_data`의 뜻이 바뀌었다

예전엔 씬의 **모든 것**(오브젝트·에셋·재질·프리팹·이벤트·변수·HUD·애니)이 이 jsonb 한 행에 있었다.
**브릭은 여기 못 들어간다** — 씬 하나가 수십만 브릭이 되고, 그건 이미 `brick_chunks`에 청크 단위 바이너리로 산다.
→ `scene_data`는 이제 **"씬을 여는 방법"**(시작 뷰·노출)만 담는다. 그래서 `EnvSchema`가 필드 2개가 됐다.

> **하위호환 없음(방침대로)**: 옛 씬의 `objects`·`assets` 등은 **읽지 않고 버린다.** 다시 저장하면 그 필드도 사라진다.

#### `sceneStore` 2,551 → 90줄

지운 것: 오브젝트 CRUD·선택/다중선택·그룹/재부모화·프리팹·클로너·모터·애니 클립·게임 변수·HUD·씬 이벤트·에셋·재질/색 에셋·클립보드·정렬/배열·**Undo/Redo 히스토리**.
남은 것: 씬 메타 · 환경 · 저장 상태(`isModified`/`savedVersion`) · 카메라 요청 3종.

> ⚠️ **Undo/Redo가 지금 없다.** 예전 히스토리는 **오브젝트 배열 스냅샷**이라 브릭엔 한 줄도 못 쓴다.
> 브릭 Undo는 "놓기/지우기 명령"을 되돌리는 별도 설계다 — 다음 작업.
> `isModified`도 뜻이 좁아졌다: **환경 변경만** 가리킨다(브릭 저장은 `BrickScene`의 자동 저장이 따로 한다).

#### 협업(Yjs) 전체 삭제

`lib/collab/*`(문서·바인딩·프로바이더) · `store/collabStore` · `hooks/useCollab` · `CollabAvatars`.
**CRDT 문서가 `objects` Y.Map 위에 세워져 있었다** — 오브젝트가 없으면 동기화할 것이 없다.
브릭 협업은 **청크 단위 동기화**가 자연스럽다(저장 계층이 이미 청크라 broadcast 단위가 그대로 맞는다). 새로 설계한다.

#### 그 밖에 삭제

- **lib 24개**: actuator · animPivot · cloner · motion · objectBBox · objectPresets · pivotMath · playRouting(+test) · prefab · primitiveGeometry · sceneTemplates(+test·meta) · glbBboxCache · primBboxCache · cameraLimits(+test) · groundTextures · matcap · patternTextures · glbThumbnail · uploadAsset · liveTransformStore
- **UI**: ColorPicker · TexturePicker · colorPickerStore · RichContent
- **`inspector/ui.tsx`**: NumInput(드래그 스크럽·수식 입력)·LabeledNum·XYZRow·LiveTransformRows·Toggle·LinkToggle 삭제 → `SectionHeader`·`GroupBox`만.
- **대시보드 템플릿 5종 제거**: 빈 씬·쇼룸·갤러리·광장·카페 — 전부 **오브젝트로 지은 장면**이라 브릭 에디터로 열면 아무것도 안 보인다. `createProject`의 `template` 인자도 제거. ".glb 가져오기"도 뺐다(업로드 경로 자체가 없다).
- **패키지 5개 제거**: `@react-three/rapier`(물리) · `three-bvh-csg`(Boolean) · `three-subdivide` · `yjs` · `y-protocols`

#### 지금 없는 기능 (의도적, 다시 만들 것)

| 기능 | 왜 없어졌나 |
|---|---|
| **Undo / Redo** | 오브젝트 스냅샷 기반 — 브릭 명령 기반으로 새로 |
| **걷기(플레이 모드)** | 오브젝트 콜라이더 기반 — 청크 콜라이더가 필요 |
| **실시간 협업** | objects CRDT — 청크 동기화로 새로 |
| **임베드 이벤트 브릿지** | 보낼 이벤트가 없어짐 |
| **씬 템플릿** | 오브젝트 장면 — 브릭은 청크 복제 방식이라야 |
| **자유 색 선택** | ColorPicker가 씬 `colorAssets`에 묶여 있었다 — 브릭 팔레트로 새로 |

### 2026-07-31 — 🔻🔻 2d 구 오브젝트 시스템 삭제 (1·2단계: 에디터 · 뷰어/플레이)

> 사용자: *"2d 구 오브젝트 시스템 삭제부터 진행해"* — 앞서 합의한 순서(에디터 패널 → 뷰어/플레이 → 스토어·타입)대로.

tsc 클린 + 브릭 테스트 21/212/81/21 + 라우트 전부 200(`/test/brick`·`/space`·`/embed`·`/`·`/community`) + **에디터 lint 에러 0**. **파일 87개 삭제.**

#### 1단계 — 에디터

- **인스펙터 섹션 19개**: Transform·Material·Geometry·Physics·Motion·Actuator·MotorLink·Events·Light·Particle·Content·Prefab·Subdivision·Visibility·AnimationClip·MultiSelect·PivotPicker·MaterialPreview·GlbClipPicker.
  `InspectorPanel` 395줄 → **38줄**(브릭 도구 + Environment). **선택 분기가 아예 없다** — 브릭은 "선택해서 고치는" 모델이 아니라 파츠·색을 **먼저 고르고 놓는** 모델이라, 도구가 항상 같은 자리에 있다.
- **패널 8개**: LogicPopup · TimelinePanel · CommandPalette · BoundaryShapeModal · AssetPreviewPopup · GameVariablesSection · HudSection · SceneLogicSection.
- **★ `EditorCanvas` 전면 재작성 — 1,277줄 → 178줄.** 오브젝트 렌더·변환 기즈모·선택(클릭/마퀴/그룹 스코프)·배치 고스트·경계 벽·스폰 마커·태양 기즈모·애니 피벗·후처리·GLB 내보내기를 전부 들어냈다. 남은 책임은 **카메라 · 룩 · 브릭 레이어 · 시점 요청** 넷뿐.
  - 캔버스 컴포넌트 10개 동반 삭제: EditorObjectInstance · GizmoController · GlbObject · CharacterPreview · ActuatorGizmo · SelectionOutline · SelectionGroundGuide · ObjectRefsContext · boxSelectState · RemoteSelections.
  - **시점 프리셋·전체 맞춤의 기준이 바뀌었다** — 예전엔 "모든 오브젝트의 bbox"였는데 **브릭 월드는 무한**이라 성립하지 않는다. 지금 보고 있는 지점(orbit target) 둘레 16m를 잡는다.
- **★ 단축키 충돌을 실제로 고쳤다**: `BrickToolPanel`이 **1~9(파츠)·R(회전)** 을 쓰는데, `EditorClient`가 **Digit1~5=카메라 북마크 · KeyR=스케일 모드**를 잡고 있었다 — **지금까지 계속 충돌 중이었다.** 남긴 단축키는 Ctrl+S · Ctrl+Z/Y · Shift+F · Numpad 7/1/3뿐.
- **`EditorOnboarding` 삭제** — "상단 툴바 + 버튼으로 도형 추가", "좌측 GNB의 Assets" 등 **없어진 UI를 그대로 안내**하고 있었다.

#### 2단계 — 뷰어 / 플레이

- **★ `ViewerClient` 1,093줄 → 120줄 · `ViewerCanvas` 472줄 → 57줄.**
  이벤트 런타임(클릭·호버·근접·interact)·팝업·대화·게임 변수·HUD·승패·타이머·스폰·커스텀 스크립트·카메라 모드·모바일 컨트롤 전부 삭제. 남긴 것 = **캔버스 · 조회수 기록 · 소유자용 에디터 링크 · Park3D 배지**.
- **뷰어 파일 12개 삭제**: ViewerObject · PlayCanvas · PlayModeController · PhysicsObject · InstancedPrimitives · MobileControls · useObjectDialogue + 컨텍스트 5종.
- **⚠️ 걷기(플레이 모드)가 없어졌다.** 구 플레이 스택은 **오브젝트 콜라이더** 기반이라 브릭엔 한 줄도 쓸 수 없다. 브릭 위를 걸으려면 **청크 단위 콜라이더**가 필요하고, 그건 새로 만들 기능이다. (게다가 지금도 이미 무의미했다 — 브릭엔 콜라이더가 없어 캐릭터가 통과했다.)
- **임베드 브릿지 제거** — `show_popup`/`emit_event`를 부모로 쏘던 postMessage인데 **보낼 이벤트가 사라졌다.** prop만 남기면 거짓말이라 같이 지웠다. 호스트 페이지 연동은 브릭에서 "이벤트"가 무엇인지 정한 뒤 다시 설계한다.
- **레거시 테스트 페이지 10개 삭제**(area-enter·perf·contact·event-bridge·host-demo·move-object·rounded-box·shadow·snap·tonemap) — `/test/brick`만 남았다.

#### 고아 모듈 19개

three: BoundaryWalls · DefaultEnvironment · GradientSky · GroundPlane · OutlineEdges · ParticleEmitter · PostProcessingEffects · PrimitiveMaterial · useVoxelSkinMaterials
ui: DraggablePopup · InlineEditName · PopupFrame
lib: animSample · effectiveMaterial · exportGlb · glbEmbed · glbMaterials
store: editorPrefsStore · uiStore

> **찾는 법**: 파일 basename이 자기 파일 밖에서 한 번도 안 나오면 고아다. 단 **동적 import**(`dynamic(() => import('./X'))`)는 경로 문자열이라 단순 `from` 검색에 안 걸린다 — HeroScene·PlanGate·ContextMenu가 그 경우여서 하마터면 지울 뻔했다.

#### 남은 3단계 (다음)

- **`sceneStore`(2,551줄)** — 오브젝트 CRUD·선택·그룹·프리팹·클로너·모터·애니·변수·HUD 액션이 통째로 죽었다. 다만 `environment`(startView·노출)와 `sceneId`·저장/버전 경로는 살아 있어 **가려내며 지워야 한다.** ⚠️ 이 파일에서 정규식으로 블록을 자르다 265줄을 날린 적 있다(G-1) — **손으로 지운다.**
- **`types/scene.ts`(700줄)** · `lib/`의 오브젝트 계열(actuator · motion · playRouting · objectPresets · prefab · cloner · pivotMath · primitiveGeometry · voxelGeometry · voxelModel · mergeObjects · booleanObjects · sceneTemplates)
- `EnvironmentPanel`(1,461줄) — **대부분 죽은 컨트롤**이다(하늘·안개·조명·바닥은 렌더 경로가 이미 없다). 살릴 것은 startView·노출 정도.

### 2026-07-31 — 🔻 뷰포트 플로팅 툴바 삭제 (B안)

> 사용자: *"에디터 toolbar도 필요없지 않아?"* → 갈래 셋을 제시하고 **(B) 통째로 삭제** 확정.

tsc 클린 + `/test/brick` `/space` 200 + 브릭 스토어 테스트 21/21.

**살아 있던 버튼 6개를 따져본 결과** — 남길 이유가 있는 건 시점 프리셋 하나뿐이었고, 그마저 단축키가 있다.

| 버튼 | 판정 |
|---|---|
| 이동/회전/스케일 · 좌표계 | 오브젝트 **기즈모** 전환용. 브릭엔 기즈모가 없다 |
| 프리셋(바퀴·문·동전·로봇팔·모터·기어) | 전부 구 오브젝트 + 모터 — 삭제 확정 대상 |
| 와이어프레임 | `EditorObjectInstance`에만 걸려 있다. **브릭 인스턴싱엔 안 닿는 죽은 버튼** |
| 격자 평면 순환(XZ/XY/YZ) | 룩 정리에서 drei `Grid`를 뗀 순간 **죽은 버튼**이 됐다 |
| 시점 T/F/S/3D | 브릭에 쓸모 있지만 **단축키로 남는다**(Numpad 7/1/3 · Shift+F) |

**삭제**: `canvas/ViewportFloatingToolbar.tsx` + `EditorClient` 배선.
**연쇄 정리**: `gridPlane` 상태와 `cycleGridPlane` 액션(스토어) — 격자가 사라져 **아무도 안 보는 값**이 됐다. `EditorCanvas`의 죽은 구조분해도 함께.

**남긴 것**: 방위 기즈모(`ViewportOrientationGizmo`)는 그대로 — 지금 어느 쪽을 보고 있는지는 브릭에서도 필요하다.

**이어서 죽은 UI 2개 더 삭제 (같은 턴, 사용자 "지워")**
- **`panels/ViewportToolbar.tsx`(상단 바)** — `hidden` div 안에서 **여전히 마운트돼 30초 `setInterval`이 돌고 있었다**("마지막 저장" 문구 갱신). 화면엔 2026-07-22부터 안 보였는데 코드는 살아 있었다. 저장 버튼은 이미 `InspectorActionBar`에 있다.
- **`canvas/ViewportStatusBar.tsx`** — 선택 오브젝트의 좌표/회전을 띄우던 바. **JSX가 주석 처리된 채 import만 남아** lint 경고를 내고 있었다. 브릭엔 선택 오브젝트 자체가 없다.

→ **`EditorClient` lint 경고 0.** 틀린 주석("Objects/Assets 패널"·"GNB 레일")도 함께 고쳤다.

**교훈**: `hidden`으로 감춘 컴포넌트는 **삭제가 아니다.** 마운트되고 이펙트가 돌고 리렌더된다. "나중에 살릴 수 있게" 남긴 것이 반 년을 조용히 돌았다.

### 2026-07-31 — 🔁 씬 전환 되살림 (드롭다운)

> 사용자: *"씬 전환을 다시 추가"* — 바로 앞 삭제에서 함께 사라졌던 것.

tsc 클린 + lint 클린(신규 파일 경고 0). **✅ 확인 완료(2026-07-31, 사용자 "다 잘된다").**

**신규 `panels/SceneSwitcher.tsx` — 프로젝트명 아래 드롭다운 하나.**
예전 `ScenesSection`처럼 **인라인 목록으로 되돌리지 않았다** — 그러면 방금 헤더만 남긴 패널이 도로 길어진다. 전환은 자주 쓰므로 ☰ 메뉴에 묻지 않고 **한 번 클릭으로 닿는 자리**에 뒀다.

담은 것: **전환 · 새 씬 · 이름 변경 · 복사 · 삭제 · 메인 씬 지정**(행 hover 아이콘). 로직은 옛 `ScenesSection` 그대로 옮겼다.

**바꾼 것 둘**
1. **새 씬 = 빈 씬.** 템플릿 선택(쇼룸·갤러리·광장·카페)을 뺐다 — **구 오브젝트 시스템으로 지은 장면이라 브릭에선 의미가 없다.** 이름만 받는다.
2. **마지막 씬이면 삭제 버튼을 아예 숨긴다.** 예전엔 눌러야 "마지막 씬은 삭제할 수 없습니다" 경고가 떴다(죽은 항목을 만들지 않는다).

**⚠️ 알아둘 것 — 씬 복사는 브릭을 복사하지 않는다.** `scene_data`만 복제하는데 **브릭은 `brick_chunks`에 씬 id로 묶여** 따로 산다. 복사본은 빈 땅으로 열린다. 브릭까지 복사하려면 청크 행을 새 씬 id로 복제해야 한다(미구현).

**전환 시 저장소 재연결은 자동** — `BrickScene`이 `sceneId`를 보고 `attachBrickStorage`를 다시 부른다(같은 대상이면 월드를 안 건드리는 가드가 있다). 여기선 `loadScene`만 한다.

**확인 필요**: 씬 전환 시 **그 씬의 브릭으로 갈아끼워지는지**(빈 땅 → 지형 재생성) · 새 씬 · 이름 변경 · 메인 씬(집) 이동 · 저장 안 한 변경이 있을 때 경고.

### 2026-07-31 — 🔻 좌측 패널 = 브랜드·프로젝트명·☰ 만 남김

> 사용자: *"로고/햄버거메뉴/타이틀 빼고 삭제"*

tsc 클린 + 컴파일 클린 + `/test/brick` `/space` 200. lint 경고 1개(기존 `ViewportStatusBar`).

**삭제한 파일 5개** — 전부 좌패널에서만 쓰던 것이라 참조가 0이 됐다.
`HierarchyPanel` · `AssetBrowser` · `ScenesSection` · `EditorGnb`(GnbTab 타입만 남아 있던 껍데기) · `EditorEmptyState`

**★ `EditorEmptyState`는 요청 밖이지만 같이 걷어냈다 — 브릭 배치를 막고 있었다.**
`objects.length === 0`일 때 뜨는 카드인데 **브릭 씬은 오브젝트가 영원히 0**이라 항상 떴고, `pointer-events-auto`라 **화면 정중앙 클릭을 삼켰다.** 카드가 권하는 것도 상자/구체/문 같은 구 오브젝트였다.

**연쇄 정리**: `EditorClient`의 `gnbTab` 상태 · `GnbTab` import · `LeftPanel` 탭 프롭. 좌패널은 이제 **높이를 안 늘린다**(`bottom` 제거 + 루트 `h-full` 제거) — 헤더뿐인데 세로로 길면 빈 상자가 된다.

**⚠️ 함께 사라진 것 (의도, 결정 필요)**
- ~~**씬 전환·추가·삭제·메인 씬 지정**~~ → **바로 되살렸다**(아래 항목). 사용자가 다중 씬을 유지하기로
- **에셋 업로드(.glb·텍스처·오디오)** — 브릭엔 업로드가 없다
- **계층 트리** — 격자 위 브릭엔 부모/자식이 없다. 다만 **선택·이름·잠금 같은 개념을 브릭에서 어떻게 할지는 아직 답이 없다**(영역 선택이 그 자리를 대신할 후보)

**남은 진입점 확인**: ☰ 메뉴(대시보드·버전 기록·테마·공유/임베드·도메인·계정·로그아웃)는 그대로 살아 있다.

### 2026-07-30 — 🧩 에디터 통합 1단계: 브릭을 에디터 캔버스에 얹기 — ✅ 확인 완료

> 순서 결정(사용자와 합의): **통합 → Undo/Redo**. 근거는 "Undo UI를 두 번 만든다"가 아니다(그건 버튼 몇 개).
> **통합이 가장 위험한 단계이므로 먼저 해서 문제를 일찍 드러낸다.** 에디터엔 2,000줄 넘는 구 스토어가 얽혀 있다.
> 또 확정: **(A) 브릭만** — 기존 오브젝트와 공존하지 않는다. 공존은 선택·조작·저장이 두 체계로 갈려 결국 두 시스템을 영구히 떠안게 된다.

**1단계 = 순수 추가.** 신규 `canvas/BrickLayer.tsx` 하나 + `EditorCanvas`에 두 줄. 기존 상호작용·UI를 **하나도 안 건드렸고** `/test/brick`도 그대로다 → **되돌리기 쉽다.**

- 현재 씬의 `brick_chunks`에 연결(`sceneStore.sceneId` → `attachBrickStorage`), 씬 전환 시 갈아끼움
- 브릭 렌더 + **스트리밍**(`controls.target` 기준 — 카메라 위치 기준이면 둘러보기만 해도 발동해 버벅인다)
- 자동 저장(변경 멎고 600ms) + `pagehide` 대비
- **lint 에러 수가 변경 전후 동일**(6개, 전부 레거시 `EditorCanvas`의 React Compiler 규칙) — 내 변경이 하나도 안 늘렸다는 확인

**✅ 확인 완료**: `/test/brick?scene=<id>`로 만든 브릭이 **같은 씬의 에디터에서 보인다**(사용자). 데이터·렌더·저장 경로가 실제 씬에서 맞는다.

### 2026-07-30 — 🧩 2a: 상호작용을 **한 소스로** 추출 (`BrickBuilder`) — ✅ 확인 완료

> **이번 세션에 배치 규칙이 네 번 바뀌었다.** 프로토타입과 에디터가 각자 배선하면 **반드시 갈라지고**, 한쪽만 고쳐진다. 그래서 통합 전에 먼저 뽑았다.

- **신규 `components/brick/BrickBuilder.tsx`** — 렌더 + 놓기/지우기/드래그/마커를 한 덩어리로.
  - **포인터를 캔버스 DOM에 직접** 붙인다(`capture` 단계) → 얹는 쪽은 `<BrickBuilder/>` **한 줄**. 부모의 클릭 처리와 섞이지 않는다.
  - 커서는 `useEffectiveTool()` + `brickCursor()`로 **부모가 자기 DOM에** 적용 — 훅에서 온 값(`gl.domElement.style`)을 직접 바꾸면 React Compiler가 막는다.
- 프로토타입: 상호작용 코드 전부 제거 → 한 줄. **동작 무변경**
- 에디터 `BrickLayer`: `BrickInstances` → `BrickBuilder`. **에디터에서도 놓기/지우기가 된다**

**✅ 확인 완료(사용자)**: 프로토타입 동작 무변경 + **에디터에서 브릭 놓기 동작.** 에디터의 좌드래그 마퀴와도 충돌 없음(capture 단계가 먼저 받는다).

**남은 순서 (뷰어를 먼저 살려야 삭제가 안전하다)**
- **2b. 뷰어(`/space/[sceneId]`)에 브릭 렌더** — 순수 추가. **이게 없으면 게시된 씬이 빈 화면이 되므로 삭제보다 먼저.**
- **2c. 에디터 좌/우 패널을 브릭 도구로** 교체
- **2d. 구 오브젝트 시스템 삭제** — 에디터 패널 → 뷰어/플레이 → 스토어·타입 순. (모터·이벤트·변수·애니메이션도 **삭제 확정**, 필요해지면 새 시스템에 맞게 다시 만든다)

### 2026-07-30 — 🔴 스트리밍 언로드가 전혀 안 되고 있었다 (실측이 잡았다)

> 사용자 실측(멀리 걸은 뒤): `브릭 525,760 · 청크 8,215 / 2 · FPS 142 · 프레임 삼각형 104,448`
> **8,215 × 64 = 525,760** — 딱 맞는다. 즉 **걸어온 길의 지형이 하나도 안 내려갔다.** 정상은 약 169청크.

**원인**: 언로드 판정을 **저장소 목록(`chunkIndex`)** 으로 하고 있었다.

```ts
const c = chunkIndex.get(key);
if (!c) continue;   // ← 생성한 지형은 여기서 전부 빠져나간다
```

생성한 지형은 **저장하지 않는다**(`clearDirty` — 손 안 댄 바닥까지 저장하면 낭비). 그래서 저장소 목록에 없고, 언로드 대상 판정에서 **통째로 건너뛰어졌다.** `indexedChunks = 2`가 증거였다(저장소엔 사용자가 브릭 놓은 청크 2개뿐).

**왜 안 죽었나 / 왜 못 봤나**: 리전 컬링이 화면 밖을 98% 걷어내서 **FPS는 142로 멀쩡했다.** 눈에 보이는 증상이 없고 **메모리만 조용히 무한 증가**했다. 실측 숫자를 안 봤으면 계속 몰랐을 것이다.

**수정**
- **`world.loadedChunks()`(신규)** — 월드가 **실제로 들고 있는** 청크의 키+좌표. 언로드 판정은 반드시 이걸로.
- **내릴 때 지형 기록도 잊는다** — `terrainDone.delete` + **`forgetTerrainChunk`(신규)**. 안 그러면 ①`filledTo`가 기둥마다 남아 **걸은 만큼 무한히 커지고** ②되돌아왔을 때 "이미 깔았다"며 **땅을 다시 안 깐다.**
  - 판 구덩이는 저장소에 남아 있어, 되돌아오면 로드되며 `noteLoadedTerrain`이 학습한다 → **구덩이 유지.**

**테스트 +5 (21/21)** — 320m를 걸은 뒤: **로드 청크 상한 안**(≤400, 이론 상한 19×19=361) · **브릭도 무한히 안 쌓임** · **되돌아오면 지형이 다시 깔림** · **판 구덩이는 유지됨**.
※ 사보타주(저장소 목록으로 되돌림)로 **689청크 / 44,096브릭**으로 새는 것 확인.

**✅ 수정 후 실측 (사용자, 320m 이동)**

| | 전 | 후 |
|---|---|---|
| 브릭 | 525,760 | **15,616** (−97%) |
| 청크 로드 | 8,215 | **244** |
| 브릭 메시 | 638 | **20** |
| 지오메트리 총 삼각형 | 6,309,120 | **187,392** (−97%) |
| 빛 칸 · 계산 | 0 · 19ms | **0 · 12ms** |
| FPS | 142 | **140** |
| 프레임 삼각형 | 104,448 | **104,448** |

**★ 프레임 삼각형과 FPS가 그대로인 것이 이 버그의 성격이다** — 화면에 그리는 양은 처음부터 같았다(컬링이 걷어내고 있었으므로). **메모리만 97% 새고 있었다.** 눈에 보이는 증상이 0인 누수라 **통계 숫자로만** 잡혔다.

244청크는 정상 범위다 — 로드 반경 6(13×13=169)과 언로드 반경 9(19×19=361) 사이의 히스테리시스.

**✅ 조명도 확인 종료**: 야외에서 탐색 칸 **0**, 계산 **12ms**. 씨앗 탐색을 브릭 표면 → 기둥 단위로 바꾼 효과 확인(94ms → 12ms). 남은 12ms는 **로드된 기둥 수(244청크 × 256열 ≈ 62k)** 에 비례하는 순회 비용이고, 디바운스라 편집 감각에 영향 없다. 더 줄이려면 "낮은 이웃이 있는 기둥"만 색인해야 하는데(ledge index) 지금 값으로 충분해 **보류**.

### 2026-07-30 — ✅ 배치 해결 — 범인은 **드래그**였다 (호버는 처음부터 맞았다)

> **✅ 사용자 확인 완료** — *"지금은 잘 붙는데."* 이어서 4항목 전부 확인:
> **①옆면·천장 붙이기 ②끌어서 자리 고치기 ③플레이트로 얇게 겹치기 ④막힌 자리는 빨강 + 안 놓임.**
> → **면 기준 배치가 규칙 하나("겨눈 칸의 옆 칸")로 전 방향·전 크기에서 동작한다.** 배치 문제 종료.

**결정적 단서는 사용자 질문 하나였다**: *"숫자를 줄 때 마우스를 오버했을 때야 아니면 클릭했을 때야?"*
그리고 실측: **호버 `-6,-1,-1 / -6,0,-1`(정확) → 클릭하는 순간 `-5,0,0`.** 게다가 *"이 숫자는 포인터를 어떻게 움직이냐에 따라 달라진다."*

→ **호버 계산은 처음부터 맞았다.** 브릭을 옮긴 것은 **클릭 이후의 드래그**였다. 나는 다섯 번을 호버 계산(면 판정·스냅 공식·시차)만 들여다봤다.

**원인 두 개, 둘 다 드래그**

| | 문제 |
|---|---|
| **미끄러짐 평면 높이** | `planeAt`을 브릭 **중심**으로 잡았다. 표면이 아니라 **0.3m 위**라, 비스듬히 보면 투영 지점이 `0.3m × tan(기울기)`만큼 어긋난다 — 45°면 반올림해서 **한 칸**. 수직에서 0이라 "위에서는 맞다"까지 설명된다 |
| **클릭만 해도 발동** | `SlidePlane`의 `useFrame`이 마운트 첫 프레임에 돌아 **마우스를 안 움직여도** 브릭을 옮겼다 |

**수정**
- `planeAt` = **브릭이 표면에 닿는 면**(`dir>0`이면 앵커, 아니면 앵커+두께). 브릭 중심이 아니다.
- `dragRef.moved` — **실제로 포인터가 움직인 뒤에만** 미끄러진다. 그냥 클릭하면 놓인 자리 그대로.
- 클릭은 **강조된 칸 그대로**(`spot` 상태 = 화면에 보여준 값). ref로 "최신 값"을 읽던 것 삭제 — 그건 미리보기와 배치를 갈라놓는다.

**테스트 +2 (21/21)** — `planeAt`이 접촉 표면인지 전 파츠×회전×6면으로, 그리고 **같은 지점을 짚으면 자리가 안 변하는지**(클릭만 해도 옮겨지던 버그) 잠갔다.
※ 사보타주(평면을 브릭 중심으로 되돌림)로 **384/384 전부 실패** 확인.

> **교훈(함정 E-2b에 박음)**: 배치가 어긋나면 **호버 값과 클릭 후 값을 따로** 볼 것. 한쪽만 보면 영원히 못 찾는다.

### 2026-07-30 — 🧩 2b/2c: 뷰어 렌더 + 인스펙터 도구 + **룩 통일**

tsc 클린 + 브릭 테스트 21/212/81/21 + `/space` 200 · `/test/brick` 200 · 컴파일 클린.

**2b. 뷰어에 브릭 (`/space/[sceneId]`)** — 순서상 **삭제보다 먼저**여야 했다. 뷰어가 브릭을 못 그리면 게시된 씬이 빈 화면이 된다.

**★ 공용 레이어로 뽑았다 — `components/brick/BrickScene.tsx`**
에디터 `BrickLayer`와 뷰어가 각자 배선하면 저장소 연결·스트리밍·자동저장이 **세 곳(프로토타입·에디터·뷰어)** 으로 갈린다. 이번 세션에 배치 규칙이 네 번 바뀐 걸 생각하면 답이 정해져 있다.
- `readOnly`면 `BrickInstances`(보기만), 아니면 `BrickBuilder`(놓기/지우기)
- 자동저장은 `readOnly`에서 **아예 안 건다** — 방문자가 씬을 덮어쓰면 안 된다(스토리지 쓰기 자체를 안 만든다)
- 스트리밍은 양쪽 공통. 기준은 `controls.target`(카메라 위치로 하면 둘러보기만 해도 발동한다)

**2c. 인스펙터에 브릭 도구** — `BrickToolPanel`(2a에서 뽑아둔 공용 UI)을 **선택 없음** 브랜치 맨 위에 얹었다. 프로토타입과 **같은 컴포넌트**라 도구가 갈릴 일이 없다. 돌기 모양은 프로토타입에만 노출(`showStudStyle`).

**룩 통일 — 신규 `components/brick/BrickEnvironment.tsx` (에디터·뷰어 공용)**

기존 환경 렌더는 오브젝트 시스템을 위한 것이라 브릭과 **정면으로 충돌**했다:

| 걷어낸 것 | 왜 |
|---|---|
| **방향광 + 그림자 맵** | 구운 값 위에 **움직이는 음영**이 겹친다. 블록을 쌓으면 옆에 그림자가 지고 카메라를 돌릴 때마다 어떤 면이 번쩍인다. 마크엔 태양 방향이 없다. 실측에서 그림자 패스가 **프레임의 절반(10.3ms)** 이었다 |
| **`GroundPlane`(y=0 평면)** | 레이캐스트를 가로채 **판 구덩이 안에 브릭을 못 놓는다.** 게다가 지형 블록과 이중 바닥 |
| **`Grid`(회색 격자)** | 지형 블록 자체가 격자다. 블록 위에 겹쳐 지저분 |
| **HDR·Sky·GradientSky·ContactShadows** | 배경/반사 소스가 여럿이면 브릭 톤이 흔들린다. 배경은 단색 하나로 |

남는 건 **배경색 · 안개 · 환경광 하나**뿐이다. 입체감은 전부 `FACE_TONE` × 하늘빛 BFS가 만든다.
- 안개 끝(46m)은 **지형 반경(약 48m)보다 앞** — 안 그러면 스트리밍 경계가 그대로 드러난다
- `<Canvas shadows>`도 뗐다(그림자 캐스터가 없으니 패스만 낭비)

**정리한 죽은 코드**: `EditorCanvas`에서 `flatView`·`gridRot/gridPos`·`useHdr/isSkyMode/isGradient/isSolid/skyColor/skyHorizon` + 관련 import, `ViewerCanvas`에서 같은 지역변수 + `Grid/Sky/Environment/ContactShadows/GroundPlane/GradientSky/DefaultEnvironment/HdrPreset` import.

**⚠️ 도구 함정(또 걸렸다)**: `EditorCanvas.tsx`가 **CRLF**라 `\n` 붙인 문자열 치환이 **11개 전부 MISS**했다. 조용히 아무것도 안 바꾸고 성공한 척한다. → 줄 단위로 나눠 정규식 매칭하고 **원본 EOL로 되붙였다.** (G절에 추가)

**브라우저 확인 필요**
1. 에디터: 회색 격자·바닥 평면 없음 · 브릭에 그림자 안 짐 · **판 구덩이 안에 브릭이 놓임**
2. 인스펙터: 빈 곳 클릭 → 우측 맨 위 **브릭 도구**(도구/파츠/방향/재질/색), 1~9·R 단축키
3. 뷰어(`/space/<sceneId>`): 같은 룩으로 브릭이 보이고 **스트리밍**이 돌 것 (편집은 안 되는 게 정상)


### 2026-07-30 — ★★ 배치 확정: **겨눈 칸의 옆 칸. 자동 보정 없음.**

> 배치 어긋남으로 **한 시간 넘게, 네 번** 헛고쳤다. 사용자가 *"다른 대안을 찾아보라"* 고 **두 번** 말했는데도 나는 계속 보정 공식만 만졌다. 마지막에 사용자가 물었다: *"마인크래프트는 어떻게 하는 거야? 그 게임은 전/후/좌/우 전부 되는 것 같던데."*

**마인크래프트의 실제 규칙**: 조준점은 화면 중앙 고정 → 처음 맞은 블록과 그 면 → **그 면 쪽 옆 칸**에 넣음 → 검은 외곽선은 **겨누는 기존 블록**에 그림.
**핵심은 블록이 전부 1칸이라는 것.** "어느 면"이 정해지면 들어갈 칸이 **딱 하나** — 고를 여지가 없으니 계산도, 보정도 없다. 그리고 **막히면 그냥 안 놓인다.**

**진단**: 우리 브릭은 여러 칸이라 "그 면 위 어디에?"가 남는다. 나는 그 답을 **코드가 자동으로 채워 넣었다**:

| 넣었던 보정 | 하는 일 |
|---|---|
| `snapCentered` | 커서 좌표로 좌우 위치를 계산 |
| `seatAlong` | 걸리면 **알아서 한 칸 밀어냄** |
| 옆면 `b.y` 정렬 | 높이를 대상 브릭에 **자동 맞춤** |

하나하나는 "친절"인데 합쳐지면 **사용자가 겨눈 것을 내가 고쳐서 놓는 것**이다. 그래서 "한 칸씩 밀리거나 올라간다" — **내가 밀고 있었다.**

**확정한 규칙 (보정 전면 삭제)**
1. 표면의 **칸 하나**를 겨눈다 → 그 칸을 **강조 표시**(0.5m 정사각, 표면과 같은 평면이라 시차 0)
2. 새 브릭의 **첫 칸 = 그 칸의 옆 칸.** 끝.
3. **막히면 빨강으로 표시하고 안 놓는다.** 알아서 옮기지 않는다.
4. 큰 브릭이 뻗는 방향은 사용자가 **회전(R)** 으로 정한다.

- `snapCentered`·`seatAlong` **삭제**, 옆면 높이 자동 맞춤 **삭제** → `anchorFromFace`가 규칙 하나로 줄었다.
- `FacePlacement.cell`(겨눈 칸) 추가 → `PlacementMarker`가 **겨눈 칸(진하게) + 놓일 브릭 발자국(윤곽)** 두 개를 표면에 눕혀 그린다.
- 배치가 **항상 유효하지는 않다**(설계상). 유효성은 호출부가 `canPlace`로 판정해 색으로 알린다.

**테스트 전면 교체 (19/19)** — 잠그는 것이 바뀌었다:
- **① 겨눈 칸 = 표면의 그 칸** (커서가 있는 칸, 브릭 범위로 자름)
- **② 첫 칸 = 겨눈 칸의 옆 칸** — 나머지 두 축은 겨눈 칸 그대로 (보정이 끼어들 자리 없음)
- **★③ 파츠 크기·회전이 달라도 첫 칸이 같다** — 전 파츠×회전×6면×모든 표면 칸 **10,000+ 조합**. *"2×4가 됐다고 2×3이 된다는 보장이 있냐"* 에 대한 답.
- **④ 막히면 막혔다고 보고한다** (몰래 안 옮긴다)
- ⑤ 드래그는 고정 축을 안 건드린다 · 면 판정 6방향 · 겨눈 칸이 표면을 안 벗어난다

### 2026-07-30 — 🧱 브릭 종류 확장(11 + 플레이트 5) · 🐛 지형이 패이던 회귀 · ✋ 배치 방식 = 면 기준

#### 파츠 카탈로그 확장 (사용자 승인: A+B)

코드가 이미 `part.h`·`sx/sz`로 일반화돼 있어 **크기 추가는 거의 공짜**였다(지오메트리·배치·저장 전부 파생).

- **브릭 11종**(높이 3칸): 1×1, 1×2, **1×3, 1×4, 1×6, 1×8**, 2×2, **2×3**, 2×4, **2×6, 2×8**
- **플레이트 5종**(높이 **1칸** = 브릭의 ⅓): 1×1, 1×2, 2×2, 2×4, 2×6
  - 얇은 것을 만들 수 있느냐가 표현력을 가른다 — 책상 상판·바닥 마감·창틀·계단 디테일이 플레이트 없이는 전부 0.6m 덩어리가 된다. 3장 쌓으면 브릭 하나 높이(레고와 같은 규칙).
- **타일은 안 만들었다** — 기본 돌기 스타일이 `line`(돌기 없음)이라 **플레이트와 육안 구분이 안 된다.** 돌기를 켜는 옵션을 쓸 때만 의미가 생긴다.
- **저장 포맷 안전**: `PART_ORDER` **뒤에만** 추가 → 기존 인덱스 0~4 불변, 골든 픽스처 그대로 통과(212/212).
- **UI**: 16종이라 가로 나열이 안 맞아 **갈래 탭(브릭/플레이트) + 4열 격자**로. 숫자키 1~9는 **지금 보고 있는 갈래 안에서** 매핑.
- **테스트 +13 (81/81)**: 플레이트 3장 = 브릭 1개 높이 · 브릭↔플레이트 섞어 쌓기 · 같은 칸 중복 방지.
  - 🐛 **함께 발견**: 무작위 월드에 기대던 조건부 단언 하나가 파츠 분포가 바뀌자 **조용히 사라졌다**(69→68). 확정적 배치로 바꿨다 — 회귀를 숨기는 형태였다.

#### 🐛 브릭 하나 놓고 새로고침하면 주변 땅이 패이던 문제

> 사용자: *"아무것도 없는 공간에 브릭 하나 배치 → 새로고침 → 브릭 주변 땅이 패여 있다."*

**원인 — 청크는 세로로도 나뉜다**(`CHUNK_Y=32`).

| | y 범위 | 청크층 `cy` |
|---|---|---|
| 지형(흙) | −3 … −1 | **1** |
| 지면에 놓은 브릭 | 0 … 2 | **2** |

브릭 하나를 놓으면 **`cy=2` 청크만** 저장된다(지형은 손대지 않아 저장 안 함). 그런데 로드 시 `markTerrainGenerated(c)`가 **`cy`를 안 보고** `(cx,cz)` 기둥을 "지형 이미 깔림"으로 표시해, `generateSurface`가 통째로 건너뛴다 → **8m×8m 흙이 안 생긴다.**

**브릭 종류 추가와는 무관**했다. 이 로직은 파츠 카탈로그와 상관이 없고, 원인은 앞서 넣은 `clearDirty()`(손 안 댄 지형은 저장하지 않음)로 **지면 청크가 더는 저장되지 않게 된 것**이다.

- **① 지면층만 증명한다** — 신규 `provesTerrainGenerated(c)` = `c.cy === GROUND_CY`. 브릭만 든 청크는 지형에 대해 아무것도 증명하지 않는다. **이미 망가진 월드도 저절로 복구**된다(브릭 y≥0, 흙 y<0이라 겹치지 않아 다시 깔린다).
- **② 같은 계열 잠복 버그** — 빈 청크는 저장소에서 **삭제**되므로, 지형을 한 청크 전부 파내면 다음 로드에서 읽을 게 없어 **판 구덩이가 도로 메워진다.** → **지면층은 비어도 남긴다.**
- **③ 회귀 테스트 신규 `store/brickStore.test.ts` (11/11)** — 이 경로는 순수 함수가 아니라 스토어+저장소라 테스트가 없었다. 메모리 저장소를 물려(`setBrickStorage`) "놓기 → 저장 → 새로고침" 을 재현한다.
  - ※ 사보타주로 확인: ①을 되돌리면 2건, ②를 되돌리면 2건 실패.
  - 🐛 **처음엔 ②의 테스트가 사보타주를 통과해 버렸다** — 표면만 파면 `deepenAround`가 아래를 채워 청크가 안 비기 때문. **지면층 y 범위를 전부 훑어** 진짜로 비우도록 고쳤다. *테스트가 실패하는 걸 본 적이 없으면 그 테스트는 아직 아무것도 지키지 않는다.*

#### ✋ 배치 방식 전환 — 드롭 → **면 기준 + 끌어서 자리 고치기**

> 사용자 요청: 면을 기준으로 추가 · 호버 시 **검은 아웃라인** 고스트 · 누르면 추가되고 끌면 이동 · **옆면은 좌우만, 윗면은 상하좌우** · 떼면 확정.

- **`placement.ts` 재작성** — `anchorFromPointer`(드롭) 폐기, 신규 `faceOf` / `anchorFromFace` / `slideAnchor`.
  - **면 판정은 지오메트리 법선이 아니라 월드 AABB 거리**로 한다. 모따기·돌기가 있으면 법선이 모서리에서 휘어 엉뚱한 면이 나온다.
  - **가리킨 칸을 덮도록** 놓되, **옆면에서는 클릭한 브릭과 밑면을 맞춘다.** 세로로 가운데 정렬을 하면 같은 면이라도 위/아래 어디를 짚었느냐로 한 칸씩 어긋나 땅에 파묻히거나 뜬다.
  - **미끄러지는 축을 면이 정한다** — 수평면 2축, 옆면은 **수평 1축**(높이 고정). 세로로도 움직이면 받쳐줄 게 없는 자리에 뜬다.
- **액션이 뗄 때가 아니라 누를 때** 일어난다 → 카메라를 돌리고 손을 떼며 엉뚱한 곳에 놓이던 문제가 **구조적으로** 사라져 드래그 임계값(4px)이 필요 없어졌다.
- **신규 `SlidePlane`** — 처음 붙은 면의 **수학 평면**에 포인터를 투영(`useFrame` 직접 레이캐스트). R3F 이벤트를 안 쓴 이유: ①방금 놓은 브릭이 뒤의 평면을 가려 이벤트가 끊긴다 ②브릭 표면에서 좌표를 받으면 높이가 달라 커서와 어긋난다. 안전장치 = 시선이 평면과 거의 나란하면 무시 + 시작점에서 12m 밖은 무시.
- **`moveBrick(id, x,y,z)`**(스토어) — 막힌 자리로는 안 옮기고 **원래 자리에 되돌린다**(브릭이 사라지지 않게). `place`는 이제 새 브릭 id를 반환한다.
- **고스트 = 검은 아웃라인**(`EdgesGeometry` + `depthTest:false`). 면에 붙이는 방식에선 고스트가 늘 다른 브릭에 맞닿아 있어 반투명 덩어리가 아래 색을 가리고 경계를 흐린다. 못 놓는 자리는 빨강.
- **높이 고정(L) 모드 삭제** — 면에 붙이면 천장·2층 바닥이 그대로 해결된다. 작업 평면 격자·`toggleLock`/`nudgeLock`·L·PageUp/Down 전부 제거. `PlacementGrid`는 **수평면일 때만** 표시.
- **테스트 전면 교체 (20/20)** — 드롭 시절 불변식(restY 16,214건)은 배치 경로가 아니게 되어 삭제하고, 면 기준 불변식으로 다시 짰다: **①가리킨 브릭을 파고들지 않는다 ②면에 딱 맞닿는다**(파츠×회전×6면×짚은 지점 전수 10,000+ 조합) **③고정 축은 드래그해도 안 변한다**(옆면은 높이가 고정) + 면 판정 6방향.

#### 🐛 "전체 지우기 → 새로고침 → 바닥도 아무것도 안 나온다" — **바로 위 ② 수정의 부작용**

> 규칙 하나를 고치면 **반대쪽도 확인해야 한다**는 것을 그 자리에서 다시 배웠다.

`clear()`는 월드만 비우고 "빈 청크는 저장 시 삭제된다"에 기대고 있었다. 그런데 ②로 **지면층은 비어도 남기게** 되면서, 그 빈 청크가 "지형을 다 파냈다"로 읽혀 **새로고침 후 바닥이 영영 안 깔렸다.**

**지우기는 "다 팠다"가 아니라 "없던 일로 한다"** 이므로 저장소를 통째로 비워야 맞다.
- `clear()`가 `storage.clear()` + `chunkIndex.clear()`까지 하고 **비동기**가 됐다.
- 지운 직후 **그 자리에 바닥을 다시 깔아 준다**(`lastCenter`) — 안 그러면 카메라를 움직일 때까지 빈 화면이다.
- **테스트 +5 (16/16)**, 사보타주로 2건 실패 확인.

#### 🐛 "격자에 맞춰 클릭해도 1칸씩 어긋난다" — 원인 두 가지

**① 클릭이 한 프레임 낡은 값을 썼다.** 호버가 `setSpot`으로 **React 상태**를 갱신하는데 `pointerdown`은 **직전 렌더에 잡힌 값**을 읽는다. 움직이면서 바로 누르면 그 사이에 끼어들어 **한 칸 전 자리**에 놓인다.
→ 호버 때 **ref에도 같이 써 두고 클릭은 ref를 읽는다**(ref는 즉시 반영). 유효성(`canPlace`)도 클릭 시점에 새로 판정 — 상태로 들고 있으면 같이 낡는다. 상태는 **고스트를 그리기 위해서만** 둔다(한 프레임 늦어도 눈에 안 띈다).

**② 짚은 칸의 뜻이 크기마다 달랐다.** `centerOffset = floor((칸수-1)/2)`로 커서를 브릭 **중앙**에 두려 했는데, 짝수 크기에선 중앙이 칸 경계에 걸려 **1×2는 맨 앞 칸, 2×4는 앞에서 두 번째 칸**이 기준이 됐다.
→ **짚은 칸 = 브릭의 시작 칸**(회전 후 AABB 최소 모서리)으로 통일. "여기서부터 깐다"라 **크기·회전과 무관하게 규칙이 하나**다. `centerOffset` 삭제, `slideAnchor`도 파츠 인자가 필요 없어졌다.
- 테스트도 **"덮는다" → "정확히 시작 칸이다"** 로 강화(16/16).

**배치 격자(`PlacementGrid`) 삭제** — 고스트 아웃라인이 같은 일을 하는데 선이 두 벌이라 지저분했다(사용자). 스냅 기능은 `anchorFromFace`가 하므로 그대로.

#### 🎯 진짜 원인은 **시차** 였다 — 떠 있는 고스트를 없애고 접촉면만 그린다

> 위 ①②를 고쳐도 사용자는 *"변화 없다, 똑같이 어긋난다. 느낌상 앞으로 한 칸. 카메라 때문에 포인터 위치를 잘 인식 못 하는 것 같다"* 고 했다.
> **판별 질문 하나로 끝났다** — *"수직으로 내려다보면?"* → **"위에서 볼 때는 잘된다."**

**배치는 처음부터 맞았다. 보이는 것이 틀렸다.**
브릭 높이 **0.6m**, 셀 **0.5m**. 카메라가 45°면 0.6m 떠 있는 상자의 윗면은 화면에서 **0.6m 밀려 보인다 — 한 칸보다 크다.** 수직에서 정확했던 이유도 같다(시차 = 높이 × tan(기울기), 90°에서 0).

> **내 실책**: 계산에 +1이 나올 자리가 없다는 걸 코드로 확인하고도 **계산을 두 번 더 고쳤다.** 함정 목록 F-1("숫자가 상식과 어긋나면 계기판을 먼저 의심")을 써 놓고 정작 안 했다. 판별 질문 하나가 세 번의 헛수정보다 빨랐다.

**수정 — 마인크래프트와 같은 모델**(신규 `PlacementMarker`, `BrickGhost` 삭제):
- **떠 있는 상자를 아예 안 그린다.** 대신 브릭이 **상대 표면에 닿는 접촉면**만 그린다(반투명 채움 + 검은 외곽선). 표면과 **같은 평면**이라 시차가 원리적으로 0이고, 커서가 그 위에 정확히 얹힌다.
- 마인크래프트의 검은 외곽선도 "놓일 자리"가 아니라 **지금 겨누는 블록 표면**에 그려진다. 그래서 한 번도 어긋나 보이지 않는다.
- 윗면을 겨누면 바닥 발자국, 옆면을 겨누면 벽의 접촉 사각형. **크기·방향은 접촉면의 가로세로가 그대로 알려준다.**
- 표면에서 6mm 띄워 z-파이팅을 피한다. 갱신은 **커서가 칸을 옮길 때만**(useMemo) — 추가 비용은 삼각형 2개 + 선분 4개로, 실측 157,128 삼각형·7 드로우콜 옆에서 잴 수 없는 양이다.

#### 🎯 그리고 진짜 원인 — **격자 스냅 방식** (세 번째 만에)

> 접촉면으로 바꾼 뒤에도 어긋났다. 그런데 사용자가 결정적인 관찰을 줬다: *"바닥에 사각형이 보인다. **커서가 사각형의 우/상 정도에 있다.**"*

기본 파츠가 **2×4**라 사각형이 1m×2m인데, 커서가 그 **모서리**에 붙어 있었다. 즉 브릭이 커서에서 한쪽으로 뻗어나가고 있었다 — 앞서 내가 추천한 **B(짚은 칸 = 시작 칸)가 틀렸다.**

**근본 원인은 스냅 공식이다.** `floor(좌표/셀)`로 칸번호를 잡고 보정하는 방식은 **짝수 칸에서 반드시 어긋난다** — 2칸·4칸짜리의 중앙은 칸 한가운데가 아니라 **칸 경계**에 있기 때문이다. A(중앙 정렬)든 B(시작 칸)든 이 틀 안에선 못 고친다.

→ **`snapCentered(좌표, 축, 칸수) = round(좌표/셀 − 칸수/2)`**. 칸번호를 거치지 않고 **브릭 중심이 커서에 가장 가까운 자리**를 바로 고른다. 홀수·짝수 구분이 사라지고, 자리는 커서가 **중간 지점을 넘을 때** 딱 한 칸씩 바뀐다.

- **실측(사보타주)**: 예전 방식은 16,384건 중 **8,448건**이 어긋났고 **최대 어긋남이 정확히 한 칸(0.500m)** — 사용자가 본 그대로다.
- 테스트를 **"커서가 브릭 한가운데(반 칸 이내)"** 로 바꿨다. 칸 한가운데뿐 아니라 **경계 근처까지 0.25칸 간격으로 훑는다**(17/17).
- `FacePlacement.ext`를 들고 다녀 드래그 중에도 같은 규칙을 쓴다.

> **세 번 헛짚었다** — ①낡은 상태 ②시작 칸 정렬 ③시차. 매번 "느낌"만 듣고 고쳤기 때문이다. **"커서가 도형의 어디에 있냐"** 를 물었어야 했고, 그 한마디에 바로 끝났다.

**확인 필요(브라우저)**: ①면에 호버 → 검은 아웃라인 ②누르면 그 자리에 놓임 — **격자에 맞춰 클릭했을 때 안 어긋나는지** ③누른 채 끌면 **윗면=상하좌우 / 옆면=좌우만**(높이 안 변함) ④떼면 확정 ⑤못 놓는 자리는 빨간 아웃라인 ⑥플레이트로 얇게 겹쳐 쌓기 ⑦**브릭 놓고 새로고침 → 땅 안 패임** ⑧땅 파고 새로고침 → 구덩이 유지 ⑨**전체 지우기 → 바닥 즉시 복구 → 새로고침해도 유지**.

### 2026-07-29 — ⛏️ 바닥 파기 (지형 블록)

> **사용자 지적**: *"바닥도 마인크래프트처럼 파져야 한다는 건 계획할 때 이미 언급됐는데 왜 구현이 안 됐나?"*
> **맞다. 내 순서 판단 착오다.** 설계서 §3.4에 "가상 바닥"을 써뒀고 로드맵 P2에도 있었는데, 폴리곤 실측 후 "그림자가 프레임 절반을 먹으니 P3를 앞당기자"로 순서를 바꾼 뒤 계속 밀렸다.
> **측정에서 나온 최적화 이슈가 사용자가 요구한 기능을 계속 뒤로 밀어냈다** — 성능 수치는 눈앞에 있고 요구사항은 문서 안에 있으니 눈앞의 것만 따라간 것.

**설계 (사용자 확정: 2×2 격자 · 지형 전용 블록 · 지하까지)**

지형을 **특별 취급하지 않는 것**이 핵심이다. 한 번 생성되면 그냥 브릭이라 **삭제·저장·조명·컬링·스트리밍이 이미 있는 코드로 전부 처리된다.**

- **`parts.ts`** — `terrain` 파츠(2×2 스터드 = 1.0 × 0.6 × 1.0m). 1×1로 깔면 블록 수가 4배라 2×2. `PART_LIST`(사용자 선택 목록)에선 제외.
- **`serialize.ts`** — `PART_ORDER`에 **뒤에만 추가** → 기존 인덱스 불변, **골든 픽스처 그대로 통과**.
- **`brickGeometry.ts`** — 지형은 돌기 스타일과 무관하게 **항상 민짜 상자**(브릭이 아니라 땅이므로).
- **`terrain.ts`(신규)** — `generateSurface`(청크 표면 한 겹, y=−3..−1 → 윗면이 y=0) · `deepenAround`(팔 때 **먼저** 아래·옆 기둥을 8층 채움 → 구멍에 벽과 바닥이 생긴다). 층별 색(흙→굳은흙→돌).
- **스토어** — 스트리밍으로 청크가 올라올 때 지형 생성(이미 저장된 청크는 생성됨으로 표시 → 다 파낸 청크가 되살아나지 않는다). 파기는 **기존 삭제 경로 그대로**.
- **프로토타입** — 평면 바닥 제거. 이제 **지형 블록이 바닥**이다.

**★ 핵심 수정: 기둥 높이가 음수를 다뤄야 한다**
> 예전 코드는 y=0을 바닥으로 가정해 `?? 0` / `top > 0` / `let top = 0`으로 잘랐다. 그러면 **판 구멍 안에 브릭이 못 얹히고 지면 높이에 뜬다.**
> → `raiseColumns`·`recomputeColumn`·`restY` 세 곳을 음수 지원으로 고쳤다.

**되살아남 방지**: 한 칸씩 게으르게 채우면 "방금 판 자리가 되살아나는" 문제가 생긴다. → **파기 전에 그 기둥을 아래로 통째 채운다**(빈 칸을 다시 채울 일이 없어짐).

**테스트 +9 (46/46)** — 지형 윗면=지면 · 파면 기둥 높이가 **음수** · **구멍 안에 브릭이 얹힌다** · 안 판 옆자리는 지면 유지 · 계속 파 내려감.
※ `restY`의 음수 지원을 되돌려 보니 **"구멍 안에 얹힌다 — got 0, want -3"** 이 정확히 실패하는 것을 확인했다.

#### 🐛 파기 버그 3종 (사용자 보고 직후 수정)

| 증상 | 원인 |
|---|---|
| **빈 월드에서 바닥이 안 생김**(브릭을 하나 놓고 새로고침해야 생김) | `streamAround`에 **조기 반환**이 있었다. 저장된 청크가 없으면 `toLoad`가 비어서 **지형 생성 전에 return**. 브릭을 놓으면 청크가 생겨 그때부터 돌았던 것 |
| **한 칸 팠는데 옆 땅·바위가 우르르 보임** | `deepenAround`가 **이웃 기둥까지** 8층 채웠다 → **판 기둥만** 채우게 수정. 구멍 옆면은 원래 있던 이웃 지형의 측면이 드러나는 것으로 충분 |
| **구멍 안에 브릭을 못 놓음**(항상 지면 높이) | y=0에 깔아 둔 **투명 판이 레이를 먼저 가로챘다.** 비스듬한 시점에서 구멍 바닥이 아니라 지면 높이 지점이 잡혀 XZ가 어긋남 → **판 제거**(지형 블록이 바닥이므로 불필요) |

> **교훈**: 지형을 넣으면서 "평면 바닥은 이제 backdrop 용도"라며 투명 판을 남겼는데, **그 판이 정확히 새 기능(파기)을 막고 있었다.** 대체한 것은 남기지 말 것.

#### ⚡ 회전 시 버벅임 — 스트리밍 기준을 카메라 위치 → **보는 지점**으로

> 사용자: *"마우스로 카메라 막 돌리니 버벅인다. 자연광 때문인가?"* — **자연광 아님**(그림자는 이미 꺼져 있다).

**원인**: `Streamer`가 **카메라 위치** 변화로 스트리밍을 발동했다. OrbitControls는 회전할 때 **눈 위치가 궤도를 따라 크게 움직이므로**, 가만히 서서 둘러보기만 해도 임계값을 계속 넘겨 스트리밍이 반복 발동 → 그때마다 지형 생성 + **전체 가시성·빛 재계산**.

**수정 2가지**
1. **기준을 `controls.target`(보는 지점)으로** — 회전해도 안 움직이므로 **실제로 이동할 때만** 스트리밍이 돈다. 임계값도 4m → 6m.
2. **스트리밍 중 빛 재계산을 디바운스로** — 동기로 돌리면 이동 내내 끊긴다(편집 경로는 이미 디바운스였는데 스트리밍만 동기였다).

**바닥 회색 격자 제거** — 지형 블록 자체가 격자라 중복이고 블록 위에 겹쳐 지저분했다. 놓을 자리 안내는 `PlacementGrid`(커서 추종)가 담당. (drei `Grid`는 **높이 고정 모드의 작업 평면**에만 남는다)

**주의(사용자용)**: 파낸 구멍은 **2×2**다. 기본 파츠가 **2×4**라 그대로는 안 들어간다 — 구멍 안에 놓으려면 **1×1 / 2×2**를 고를 것.

#### 🐛 45° 대각선으로 땅속이 비침 — **내가 직전 수정으로 만든 버그**

> 사용자: *"땅을 파다 보면 대각선 45도 방향으로 바닥이 뚫린 듯 땅속이 보인다."*

**원인**: 바로 앞에서 "옆 땅이 우르르 보인다"를 고치며 `deepenAround`가 **판 기둥만** 채우게 했다. 그러자 이웃 기둥은 **표면 한 겹뿐**이라 구덩이의 옆·대각선이 빈 공간이 됐다. 특히 상하좌우만 채우면 **정확히 45° 시선으로 뚫린다** — 대각선 기둥이 비어서.

**수정**: `deepenAround`를 **"채운 깊이를 기억하고 그 아래로만 연장"** 하는 방식으로 재작성.
- `filledTo`(기둥 → 최하단 y) 기록 — 이게 없으면 **판 자리를 다시 채우는** 문제가 생긴다(빈 칸을 보고 채우면 구멍이 되살아남). 채운 끝에서 **아래로만** 연장하므로 안전.
- **대각선 포함 3×3** 기둥을 연장 (상하좌우만 하면 45°로 뚫림)
- `noteLoadedTerrain` — 불러온 지형에서 기둥 최하단을 학습(이어서 팔 때 되살아나지 않게)

#### ⚡ 빛 계산 485ms / 43만 칸 — BFS가 **빈 지하로 흘러넘침**

사용자 실측: `빛 칸 438,302 · 485ms`. 비정상.

**원인**: `isOpenSky`가 `topOfColumn`(기본값 **0**)을 썼다. 지형이 없는 기둥은 `y<0`이 전부 "그늘진 공기"로 잡혀, **지형 가장자리에서 시작된 빛이 무한한 빈 지하로 30칸씩 퍼졌다.**

**수정**: `isOpenSky`만 **기본값을 `Y_MIN`으로** — 아무것도 없는 기둥은 통째로 "열린 하늘"로 본다(땅이 없는 곳은 지하가 아니라 허공이므로 탐색 대상이 아님).
> ⚠️ 배치용 `topOfColumn`은 **기본값 0을 유지**해야 한다(빈 곳에 놓으면 지면 높이에 놓여야 하므로). **같은 값에 용도별로 다른 기본값**이 필요한 드문 경우.

#### ⚡ (2차) 여전히 35만 칸 / 510ms — **땅 밑은 공기가 아니라 "아직 안 판 땅"이다**

재측정: `빛 칸 357,488 · 510ms`. 거의 안 줄었다 — 위 수정은 **지형이 아예 없는 기둥**만 막았고, 진짜 원인은 따로 있었다.

**원인**: 지형은 **한 겹**(y=−3..−1)뿐이고 그 아래는 데이터상 **빈 칸**이다. `colTop`은 지형 윗면(0)을 가리키므로 `y<0`은 전부 "그늘진 공기"로 잡힌다. → 지형 가장자리에서 들어온 빛이 **지형 들판 전체의 밑을 훑는다.** 실제로는 파본 적 없는 흙 속인데.

**수정**: 월드에 "이 기둥은 y 아래로 아직 안 판 땅"을 기록한다.
- `world.markSolidBelow(x,z,y)` + `solidBelow: Map<colKey, y>` → `isUnminedGround()`가 `lightAt`·BFS `seed` 양쪽에서 **브릭과 똑같이 빛을 막는다.**
- `terrain.ts`가 지형을 깔 때마다(`generateSurface` / `extendColumn` / `noteLoadedTerrain`) 표시. 지형은 2×2라 **네 개 열 전부** 표시해야 한다(하나라도 빠지면 그 틈으로 샌다).
- `LIGHT_MAX` 30 → **20**. 전파 반경이 곧 탐색 부피(≈r³)라 상수 하나로 비용이 크게 준다. 실내 밝기 차이는 미미.

> **핵심**: 파낸 구멍은 `solidBelow`를 **안 건드린다**(위에서부터 파 내려가면 `filledTo`가 갱신되며 경계도 함께 내려간다). 즉 **판 곳에만 빛이 들어오고, 안 판 흙 속은 통째로 불투명** — 마인크래프트와 같은 모델이다.

**결과(사용자 실측)**: `빛 칸 0 · 94ms` — 야외에서 **탐색 칸이 0**이 됐다. BFS는 사라졌고 남은 건 씨앗을 찾는 비용뿐.

#### ⚡ (3차) 남은 94ms = **씨앗 찾기** — 하늘빛은 항상 **옆에서만** 들어온다

빛 칸이 0인데 94ms가 걸린다는 건, **"씨앗이 없다"를 증명하는 데만** 그만큼 썼다는 뜻이다. 예전 방식은 브릭 표면 칸을 전부 훑었다(브릭당 32칸 × 1만 브릭).

**깨달은 불변식**: 그늘 칸의 정의가 `y < colTop`이다. 그러면 바로 위 칸이 열린 하늘이려면(`y+1 >= colTop`) 그 칸은 기둥 꼭대기 = **브릭**이다. 아래도 마찬가지.
> **즉 하늘빛이 그늘로 들어오는 자리는 항상 옆(4방향)이다.** 위·아래로는 구조적으로 불가능하다.

→ 씨앗 탐색을 **기둥 단위**로 바꿨다: 기둥마다 "이웃 기둥이 더 낮아서 옆이 트인 높이 구간"만 본다. 평지는 이웃 높이가 같아 **구간이 비므로 비용 0**. 발광 브릭만 예전처럼 면을 훑는다(수가 적다).

**+ 루프 안의 낭비 제거** — 기둥이 수만 개라 미세 비용이 그대로 드러났다(43k 기둥에서 36ms).
`[[1,0],[-1,0],…]` 리터럴이 매 기둥 할당되고 있었고, 이웃 키를 `colKey(x±1,z)`로 다시 계산했다.
→ 이웃 키는 **더하기 한 번**(`COL_STEP_X`/`COL_STEP_Z`), 좌표 복원은 **실제로 씨앗이 있을 때만**.

**실측(지형 1만 블록)**: `94ms → 27ms`. 밀폐된 방을 지어도 그대로, 문을 뚫으면 2,076칸 · 33ms.

#### ⚡ (4차) 진짜 끊김의 원인은 빛이 아니라 **전량 재계산**이었다

빛은 이미 디바운스라 이동 중엔 안 돈다. 계기판을 더 보니 **스트리밍 한 걸음마다 `recomputeAll()`이 동기로** 돌고 있었다 — 실측 **24~33ms**. 게다가 이 함수는 **모든 리전을 갱신 대상으로 표시**해 렌더러가 인스턴스 버퍼를 통째로 다시 만든다(GPU 쪽 비용은 별도).
**땅을 팔 때도 매번 돌고 있었다**(`deepenAround`가 `placeFast`라서).

- **`world.refreshPending()`(신규)** — `placeFast`가 넣은 브릭을 모아 뒀다가 **그 브릭과 맞닿은 이웃만** 다시 굽는다. 새 브릭이 바꿀 수 있는 건 그것뿐이다.
  - 단 **들어온 양이 월드의 1/3을 넘으면 전량으로 넘긴다** — 주변 훑기는 브릭당 면 칸 32개를 보므로 첫 로드처럼 대량일 땐 오히려 비싸다(100ms vs 28ms).
- **`unloadChunk`도 이웃을 되살린다** — 가리던 브릭이 내려가면 이웃 면이 열린다. 예전엔 뒤따르는 `recomputeAll`이 덮어 주고 있었다.
- **순수 지형 스트리밍은 빛을 아예 안 돌린다** — 새로 깐 지형은 윗면이 열린 하늘(O(1))·옆은 이웃 지형·아래는 안 판 땅이라 빛 지도에 아무것도 더하지 않는다. **저장된 청크를 실제로 읽어왔을 때만** 다시 굽는다.

| | 전 | 후 |
|---|---|---|
| 스트리밍 한 걸음(13청크) | 지형 10ms + **가시성 28ms** + 빛 | 지형 10ms + **가시성 7ms** (빛 없음) |
| 땅 파기 1회 | **34ms** | **3ms** |
| 편집 후 빛(디바운스) | 94ms | 27ms |

**테스트 +14 (60/60)** — 이 경로는 틀려도 에러가 안 난다(브릭이 조용히 안 그려지거나 없어진 벽이 계속 보인다). 그래서 **`refreshPending` 결과 == `recomputeAll` 결과**를 가시성·스터드컬링·리전 멤버십 세 지문으로 잠갔다. 청크 경계를 사이에 둔 언로드 케이스도 포함.
※ 이웃 갱신을 각각 빼 보니 **정확히 2건씩 실패**하는 것을 확인했다.

#### 🔆 조명 모델 정정 — **마인크래프트엔 태양 방향이 없다**

> 사용자: *"카메라를 돌리다 보면 어느 부분은 빛이 없고 어느 부분은 직사광선 쬐듯 반짝인다."*
> 이어서: *"마인크래프트는 블록을 쌓아도 블록에 그림자가 지거나 그러지 않는 것 같은데."* — **맞다.**

**내 잘못**: 이 문서 아래쪽(P0 실측 ④)에 내가 직접 *"마인크래프트는 실시간 그림자가 없다 — 빛 레벨 전파 + 정점 AO로 입체감을 낸다. 우리 P3가 정확히 그 계획"* 이라고 써 놓고, **실시간 그림자만 끄고 방향광(intensity 1.1)은 그대로 뒀다.** 그러면 구운 값 위에 **움직이는 음영**이 겹친다 — 블록이 옆 블록에 그림자를 드리우고, 카메라를 돌릴 때마다 반사광이 옮겨 다닌다. 계획을 절반만 실행하고 남은 절반을 말하지 않았다.

**정정한 모델** (마인크래프트와 동일):

| | 마인크래프트 | 우리(전) | 우리(후) |
|---|---|---|---|
| 태양 방향 | **없음** | directionalLight 1.1 | **없음**(그림자 측정 토글일 때만) |
| 실시간 그림자 | 없음 | 꺼짐 | 꺼짐 |
| 면 밝기 | 빛 레벨 × **면 방향 고정 배율** | 빛 레벨만 | 빛 레벨 × **고정 배율** |
| 조명 | — | ambient + directional + hemisphere | **ambient 하나** |

- **`world.FACE_TONE = [0.6, 0.6, 1.0, 0.5, 0.8, 0.8]`**(월드 [+x,−x,+y,−y,+z,−z]) — 윗면 1.0 · 남북 0.8 · 동서 0.6 · 밑면 0.5. `faceLight`가 하늘빛에 곱한다. **카메라와 무관하게 고정**이라 돌려도 밝기가 안 변한다.
- **프로토타입 조명 = `ambientLight` 하나**(1.6). hemisphere는 위아래 그라데이션을 만들어 `FACE_TONE`과 이중 계산이라 뺐다.
- **거칠기** — 반사광이 view-dependent라 번쩍임의 직접 원인이었다. 브릭 0.45 → **0.72**(플라스틱다운 옅은 광택), **지형은 0.98 무광**. 그룹 키가 이미 `part`별이라 지형 전용 재질을 써도 **드로우콜이 안 늘어난다**.
- `LIGHT_MAX`를 20으로 줄였던 것도 **30으로 되돌렸다**("그림자가 심하다" 피드백). 비용은 씨앗 탐색이 지배하므로 이 값과 거의 무관하다.

**테스트 +5 (65/65)** — 사방이 트인 브릭의 면 밝기가 **정확히 고정 배율**이어야 한다(윗면 1.0 · 남북 0.8 · 동서 0.6 · 밑면 0.5, 좌우 대칭). 방향광을 되살리면 이 고정성이 깨진다.

**🐛 함께 수정**: `Mover`의 우측 벡터가 `(fwd.z, 0, −fwd.x)`로 **부호가 뒤집혀 A/D가 반대**로 갔다. 오른쪽 = `fwd × up` = `(−fwd.z, 0, fwd.x)`.

#### 🐛 "브릭을 하나 놓으면 양옆에 그림자가 진다" — 그림자가 아니라 **평균의 함정**

> 사용자: *"분명 그림자 없앤다고 하지 않았니."* — 없앴다. 이건 다른 문제였다.

**원인**: 지형 타일은 **2×2 스터드**인데 브릭은 아무 칸에나 놓인다. 그래서 브릭이 **타일 두 장에 걸치는** 일이 흔하다. 그런데 `faceLight`는 면 전체를 **평균 하나**로 굽는다 → 타일의 절반만 덮였는데도 **타일 전체가 균일하게 어두워진다**(실측 1.00 → 0.58). 그게 양옆으로 번진 그림자처럼 보였다.

**수정**: 평균에서 **막힌 칸을 뺀다.** 그 부분의 면은 브릭에 **가려져 안 보이므로**, 보이는 부분의 밝기를 끌어내릴 이유가 없다. 전부 막혔으면 어차피 안 그려지는 면이다.

**테스트 +4 (69/69)** — 타일 경계에 걸쳐 놓았을 때 **양쪽 타일 모두 밝기가 안 변해야** 한다. ※`continue` 한 줄을 빼 보니 정확히 `1.00 → 0.58`로 2건 실패하는 것을 확인했다(사용자가 본 그대로).

**남은 한계(후속)**: 면 하나에 밝기 하나라는 구조 자체는 그대로다. 마인크래프트는 **정점 단위 밝기(smooth lighting + AO)** 라 모서리가 자연스럽게 어두워진다. 지금은 그 표현이 없어 구석이 밋밋하다 — 인스턴스 속성을 6개(면) → 24개(정점)로 늘려야 해서 별도 작업.

#### 🖱️ 입력 재설계 — **액션은 왼쪽 클릭 하나**, 도구 팔레트로 뜻을 정한다

> 사용자: *"오른쪽 마우스 드래그 후 떼면 브릭이 삭제되어 버린다. 추가/삭제 버튼을 만들고 커서 모양을 바꾸고 액션은 왼쪽 마우스로만 하면 어떠니?"* — **동의. 다만 그것만으론 부족하다.**

**버그 원인**: `mouseButtons`가 `RIGHT: ROTATE`인데 `onContextMenu`도 삭제를 물고 있었다. **버튼 하나에 두 뜻** — 화면을 돌리고 떼면 그 자리가 지워진다.

**바꾼 것**

- **도구 팔레트(`tool: 'place' | 'erase'`)** — 왼쪽 클릭이 무슨 뜻인지 도구가 정한다. 우클릭 액션은 **완전히 폐기**.
  - 근거 ①**모바일·트랙패드엔 우클릭이 없다** — 웹 서비스라 이게 결정적 ②카메라와 충돌 원천 제거 ③지금 무슨 모드인지 화면에 보인다.
- **★ 드래그 임계값 4px** — **입력 방식과 무관하게 필요하다.** R3F `onClick`도 `contextmenu`도 **얼마나 끌었든 뗄 때 발동**한다. 지금은 왼쪽이 회전을 안 물어 안 드러나지만, **드래그로 여러 개 칠하기**나 영역 선택을 넣는 순간 같은 버그가 재발한다. 도구 버튼만으로는 이게 안 고쳐진다.
- **Alt = 도구 일시 반전** — 벽 쌓다 하나 잘못 놓았을 때 팔레트 왕복을 없앤다(Figma의 임시 도구 전환).
- **커서**(SVG data-URI, 외부 파일 없음) — 놓기 `＋` · 지우기 빨간 `－`.
- **지우기 미리보기** — 지우기 도구에선 배치 고스트·격자를 숨기고, **지워질 브릭을 빨갛게** 감싼다(`EraseHighlight`). 놓을 자리가 없는 도구에 놓을 자리 안내를 띄우지 않는다.
- 버튼 정리: **왼쪽 = 도구 · 오른쪽 드래그 = 회전 · 가운데 드래그 = 이동 · 휠 = 줌.**

#### 🧹 예전 조명 방식 제거

조명 모델이 확정됐으므로 **되돌아갈 길을 남기지 않는다**([[feedback_treat_as_rewrite_not_renovation]]).

- **`skyFactor`(P3-a 깊이 감쇠) 삭제** — 호출부 0개. BFS가 완전히 대체했고 "폴백"이라는 주석만 남아 있었다. `SKY_FALLOFF`도 함께.
- **`shadowsOn` 토글·`shadows="percentage"`·방향광 삭제** — 그림자 비용은 이미 측정을 끝냈고(10.3ms) 이 문서에 남아 있다. 더 중요한 건 **이게 오늘 두 번 보고된 버그(블록에 그림자)를 되살릴 수 있는 유일한 통로**였다는 점. 다시 필요하면 10줄이다.
- **`recomputeAll`은 남긴다** — 처음엔 지우자고 했으나 **틀렸다.** `refreshPending`이 임계값을 넘으면 직접 호출하는 **첫 로드 경로**이고, 증분 갱신 테스트의 **정답 역할**을 한다. 지우면 검증 기준이 사라진다.

#### 🔦 "빛이 한 곳에 집중된 것 같다" — 안개·배경색 문제였다

> 사용자: *"자연광 같지 않고 한 곳에 집중되는 것 같다."*

**원인**: 배경이 거의 검정(`#1a1a20`)인데 안개가 30~70m라, 지형(반경 48m) 대부분이 **검게 사그라들어** 카메라 주변만 밝은 스포트라이트처럼 보였다.
→ **실시간 그림자를 끈 뒤로는 배경·안개 색이 룩을 좌우한다.** 어두운 프로토타입 기본값이 그대로 남아 있던 것.

**수정**: 배경·안개를 **낮 하늘 톤(`#a6bccf`)** 으로. 안개 범위도 `18~46m` — **지형 반경(48m)보다 먼저 완전히 잠기도록**(far가 지형 끝보다 멀면 땅 가장자리가 그대로 드러난다).

#### 🚶 이동 수단이 없었다 (WASD 추가)

> 사용자: *"멀리 이동은 어떻게 테스트하나? 이동이 가능하니?"* — **불가능했다.**

OrbitControls에 패닝을 안 넣어 **제자리 회전·줌만** 됐다. 게다가 직전에 스트리밍 기준을 "보는 지점"으로 바꿨는데 그 지점을 옮길 방법이 없어 **스트리밍이 영영 안 도는 상태**였다.

- **`Mover`(신규)** — `WASD` 이동 · `Q/E` 위아래 · `Shift` 3배속. 카메라와 **보는 지점**을 함께 옮긴다. 수평 기준 전방/우측이라 위아래를 봐도 평면을 걷듯 움직인다. 속도는 줌 거리 비례. 창 이탈 시 키 상태 초기화(눌린 채 남지 않게).
- 마우스 **가운데 드래그 = 패닝**(기존 DOLLY에서 변경. 줌은 휠로 충분).

**남은 것**: 지형 종류 확장(모래·풀) · 파낸 지형의 저장량(방문 청크마다 64블록) 관찰 · 플레이 모드 충돌(P5).

### 2026-07-29 — 🔻 기존 시스템 은퇴 1차 (짓기 도구)

> **사용자 판단**: *"방식을 바꾸는 이유가 기존 오브젝트 방식이 비효율적이어서인데, 둘 다 쓰자는 건 언밸런스하다."* — 맞는 지적. 앞서 제안한 "모드 토글"은 설계가 아니라 결정을 미룬 것이었다.

**은퇴 원칙**: 오브젝트 시스템의 복잡함 **대부분은 "짓기"를 지원하려고** 존재했다. 격자 조립이 짓기를 가져가면 그 복잡함은 갈 곳이 없다. 특히 **앵커·핸들·정렬은 "박스를 서로 맞추려고"** 만든 것들이라, 격자가 그 문제 자체를 없앴다.

**⚠️ 삭제 범위를 좁힌 두 가지 발견 (감사에서)**
1. **`pivotMath`는 못 지운다** — 앵커 수학이 **액추에이터(모터·경첩)** 를 돌리고 있다(`lib/actuator.ts`·`PlayCanvas`·`ViewerObject`·`ActuatorGizmo`). UI만 걷어내고 수학은 남긴다.
2. **펜툴·복셀의 *렌더 경로*도 남긴다** — 지우면 기존 씬의 돌출/회전체/복셀 오브젝트가 **안 열린다**(`primitiveGeometry.ts`). **만드는 도구만 없애고 그리는 코드는 둔다.**

**이번에 삭제한 것**
- `inspector/ArraySection.tsx` · `inspector/ClonerSection.tsx` · `inspector/GridPreview.tsx`
- `lib/mergeObjects.ts` · `lib/booleanObjects.ts`
- `MultiSelectPanel`의 **Merge · Boolean · Align · Arrange(Tidy/Distribute)** 섹션과 핸들러 → 일괄편집·거리 표시만 남음
- `InspectorPanel`의 **'모델 에셋으로 저장'**(Merge 파이프라인에 얹혀 있었다) + Array/Cloner 섹션 렌더

**🐛 중간에 저지른 실수(기록)**: `sceneStore.ts`(2,500줄)에서 액션 블록을 스크립트로 잘라내려다 **`alignSelected`에서 265줄을 통째로 날렸다**(블록 끝 판정이 틀림). 즉시 `git checkout`으로 복구.
→ **교훈: 큰 파일에서 정규식으로 코드 블록을 자르지 말 것.** 스토어 액션 정리는 다음 단계에서 파일을 직접 읽고 손으로 지운다. 지금은 `lib/cloner.ts`를 되살려 스토어를 **원형 그대로** 두었다(죽은 액션이 남아 있으나 동작엔 무해).

### 2026-07-29 — 🔻 은퇴 2차 (조작 도구 · 툴바 · 모달)

tsc 클린 + 브릭 테스트 16,214/68/37 + 라우트 200. **브라우저 확인 대기.**

**삭제**
- `canvas/ManipulationHandles.tsx` (코너·면 스케일 핸들) + `EditorCanvas` 배선
- `panels/PenToolModal.tsx` · `panels/VoxelToolModal.tsx` + `EditorClient` 배선
- 툴바(`ViewportFloatingToolbar`): **스냅 드롭다운 · 도형 추가 · 펜툴 · 복셀 · 정렬 드롭다운** + 죽은 import·구조분해
- `TransformSection`의 **앵커(기준점) 피커 UI**

**남긴 것과 이유**
| 대상 | 왜 |
|---|---|
| **`lib/pivotMath.ts`** | 앵커 수학이 **액추에이터(모터·경첩)** 를 돌린다 |
| **`inspector/PivotPicker.tsx`** | **애니메이션 클립**의 피벗 선택에 쓴다(은퇴 목록 밖) |
| 툴바 **프리셋** 메뉴 | 모터·기어·문 등 — "움직이는 브릭"이 미해결이라 유일한 진입점 |
| 펜툴·복셀 **렌더 경로** | 지우면 기존 씬이 안 열린다 |
| 스토어 죽은 액션 | 호출부가 없어 **동작 무해**. 큰 파일이라 다음에 손으로 정리 |

### 2026-07-29 — 🔻 은퇴 3차 (지오메트리 경로) + **하위호환 방침 폐기**

> **사용자 지적**: *"기존 씬을 왜 신경쓰나? 서비스가 안 열렸고 만들어 둔 파일도 별로 없다."* — **맞다. 내가 '하위호환을 지킨다'를 상황 확인 없이 습관으로 적용했다.** 지킨 대상이 사실상 없었다.
> 확인해보니 위험도 작았다 — `createPrimitiveGeometry`의 `default`가 `BoxGeometry`라 렌더 경로를 지워도 **크래시가 아니라 박스로 보일 뿐**이다.
>
> **새 방침(사용자)**: *"기존 기능을 신경쓰면 오히려 에러 날 확률이 는다. 지울 건 지우고, 필요하면 나중에 새 시스템에 맞게 다시 만든다."*

**삭제**
- `primitiveGeometry.ts` — `extrude`·`lathe`·`loft`·`voxel` 케이스 + `makeExtrude`/`makeLathe`/`makeLoft`/`normalizeUnit`/`profileSig` + 캐시 키의 죽은 파라미터
- `lib/voxelGeometry.ts` · `lib/voxelModel.ts`
- 세 렌더 경로(`EditorObjectInstance`·`ViewerObject`·`InstancedPrimitives`)의 관련 import·의존성 배열 정리

**⚠️ 이번에도 sed가 의존성 배열을 망가뜨렸다**(`profileSig(...)` → `object.geom?.profileClosed(...)`). tsc가 잡아서 손으로 고쳤다. **큰 파일 편집은 sed 말고 Edit 도구로.**

**의도적으로 남긴 것 — 순서 때문**
> **기즈모 · Transform 섹션 · 계층 트리 · 다중선택**은 브릭이 **에디터에 들어간 뒤에** 지운다. 지금 지우면 에디터가 아무것도 못 하는 상태가 된다. 남긴 이유는 "가치가 있어서"가 아니라 **대체재를 아직 안 만들어서**다.

**남은 것**
- 복셀 스킨 잔재: `useVoxelSkinMaterials` + `addVoxelObject`/`updateVoxelObject`(스토어)
- 스토어 죽은 액션(손으로): `alignSelected`·`tidyUpSelected`·`distributeSelected`·`arraySelected`·`makeCloner`·`updateCloner`·`mergeIntoAsset`·`objectSnap`·`setPenToolOpen`·`setVoxelToolOpen`
- **조명·유리·물·장식물 → 브릭** — **결정 대기**: 발광 브릭이 GLB 소품을 비추지 못한다. (A) 발광 브릭이 실제 PointLight도 생성 (B) 소품은 환경광만
- **브릭을 에디터에 통합** → 그 뒤 기즈모·Transform·계층 삭제

### 2026-07-29 — 기본값 라인 확정 · 설정 저장 · **P3-a 구운 하늘빛**

**① 기본 돌기 = `line`** (사용자 확정). 5종은 전부 남긴다 — 사용자가 고르게 할 수도 있으므로.
> **선택지로 있는 것과 기본값인 것은 다르다.** 제품의 얼굴(스크린샷·템플릿·대부분의 사용자 콘텐츠)은 기본값이 정한다. 원형을 선택지로 두는 건 어떤 브릭 렌더러에나 있는 형태라 무방하지만, **기본값이면 제품 전체가 레고처럼 보인다.** UI 라벨도 "원형"이지 "레고형"이 아니다.

**② 설정 저장** — `BrickStorage`에 `loadSettings`/`saveSettings` 추가. 청크가 아니라 **월드 단위 설정**이라 별도 키(`brickcfg:*`). 씬 통합 시 `scene_data`로 간다. 브릭을 전부 지워도 설정은 남긴다(사용자가 고른 룩이므로).

**③ P3-a — 면 단위 구운 하늘빛**

> ⚠️ **착수 전 확인한 구조적 제약(사용자 승인)**: 마인크래프트는 청크를 메시 하나로 구워 **정점마다** 빛을 박는다. 우리는 인스턴싱이라 **지오메트리를 공유**하므로 정점별 값을 줄 수 없다.
> → **인스턴스마다 6면의 밝기**를 넘기고 정점 셰이더가 법선으로 고르는 방식. 결과는 마크의 **'Fast 그래픽' 룩**(면마다 밝기가 딱 갈림). **면 내부 그라데이션과 모서리 정점 AO는 불가** — 되돌리려면 인스턴싱을 포기해야 하는데 그건 파츠별 색·클릭 피킹·비큐브 파츠가 전부 무너진다.

- **`world.ts`** — `skyFactor(x,y,z)`(기둥 꼭대기 대비 파묻힌 깊이 → 0..1, 감쇠 15브릭, 최소 0.16) + `faceLight(brick, out)`(면 바로 바깥 칸들의 평균, 월드 순서 `[+x,-x,+y,-y,+z,-z]`). **기존 `colTop` 캐시를 그대로 써서 O(1)** — 빛 격자도 BFS도 없다.
- **`BrickInstances.tsx`** — `aLightA`/`aLightB` 인스턴스 속성 2개(정점 속성은 최대 4성분이라 6개를 vec3 둘로 나눔) + `injectFaceLight` 셰이더. `FACE_MAP`으로 회전(0/90/180/270)에 맞춰 월드 면 → 로컬 슬롯 매핑. **발광 재질엔 안 곱한다**(스스로 빛나므로).
- **★ 지오메트리 공유 함정**: 캐시된 지오메트리는 그룹끼리 공유되므로 거기에 인스턴스 속성을 붙이면 서로 덮어쓴다. → **정점 버퍼는 공유하고 껍데기만 새로** 만드는 얇은 사본(메모리 낭비 없음).
- **실시간 그림자 기본 끔**(P3-c 선반영) + 조명 밸런스 조정(ambient 0.5→0.9, directional 2.2→1.1). 구운 값이 실시간 조명에 뭉개지지 않게.

**P3-a의 한계(의도)**: 깊이 기반이라 **문·창으로 빛이 옆에서 새어 들어오지 않는다.** 창문 있는 방도 균일하게 어둡다. → **P3-b에서 해소.**

### 2026-07-29 — 씬 통합 1단계: DB 저장 (`brick_chunks`)

localStorage → **Supabase**. tsc·lint 클린 + 라우트 200(로컬·씬 양쪽). **✅ 마이그레이션 적용 완료(2026-07-30, 사용자).**

- **★ `supabase/migrations/0009_bricks.sql`(신규) — ✅ 적용 완료.**
  - 🐛 **1차 실행 실패**: 공개 읽기 정책에 `scenes.is_published`를 썼는데 **그 컬럼은 `projects`에 있다**(`ERROR: 42703`). `scenes`의 공개 읽기 정책도 프로젝트를 조인해 판정하는데 그걸 안 읽고 짐작했다(함정 B-0). → 프로젝트 조인으로 수정 + **`drop policy if exists`** 를 앞에 붙여 재실행 안전하게.
  - **순수 추가**: 새 테이블 하나뿐, 기존 테이블 무수정 → **`main` 브랜치 무영향**(§7 규칙 준수)
  - **희소 저장**: 비어 있지 않은 청크만 행으로 → 무한 맵의 빈 공간 비용 0
  - RLS 2개: 소유자 전체 권한(`scene_versions` 패턴) + **게시된 씬은 공개 읽기**(뷰어가 브릭을 그려야 하므로)
  - **`data`가 `bytea`가 아니라 `text`(base64)인 이유**: PostgREST/supabase-js에서 bytea는 `\x…` 16진 문자열로 오가 인코딩 실수가 나기 쉽다. base64는 **이미 있는 경로(`serialize.ts`)를 그대로** 쓴다. 용량 +33%지만 청크가 수 KB라 무해. 나중에 bytea로 옮기려면 포맷 버전을 올린다.
- **`supabaseBrickStorage(sceneId)`** — localStorage판과 **인터페이스가 같아 갈아끼우기만** 하면 된다(P1에서 어댑터로 둔 이유가 이것). `listChunks`는 좌표만 select(데이터 제외), `save`는 upsert + 빈 청크 delete.
- **`attachBrickStorage(sceneId|null)`** — 저장소 교체 + 인덱스 초기화. 훅이 아니라 부수효과 함수라 이름을 바로잡았다(`use…`였음).
- **검증 경로**: `/test/brick?scene=<sceneId>` → DB · 파라미터 없으면 localStorage. 우측 패널에 `DB ·` 표시.
- **✅ DB 저장 확인 완료(2026-07-30, 사용자)** — 마이그레이션 적용 후 실제로 `brick_chunks`에 저장됨.
  - 🐛 도중에 `new row violates row-level security policy` — 원인은 **sceneId 자리에 projectId를 붙인 것**(에디터 주소는 `/editor/<projectId>`, 씬 id는 **미리보기**가 여는 `/space/<sceneId>`에 있다). 코드 문제가 아니었다.
  - 🐛 `Jest worker encountered … child process exceptions` — `.next` 캐시 손상. 포트 3000 종료 → `.next` 삭제 → `npm run dev`.

**다음(미착수)**: 에디터 통합(브릭 모드) · 뷰어 렌더 · 씬 데이터에 브릭 설정(돌기 모양) 저장.

### 2026-07-29 — P2-b 청크 스트리밍

12만 브릭 **3초 로딩**을 없앤다. tsc·lint 클린 + 16,214 / 68 / **37** + 라우트 200. **브라우저 확인 대기.**

- **`storage.ts`** — `listChunks()`(**데이터는 안 읽고 목록만**) + `loadChunk(coord)`. 스트리밍의 출발점.
- **`world.ts` `unloadChunk(key)`** — 메모리에서만 내린다. 점유·기둥높이·리전 멤버십까지 함께 정리.
- **`brickStore.streamAround(camX, camZ)`** — 로드 반경 6청크(≈48m) / 언로드 9청크(**히스테리시스** — 경계에서 껌뻑이지 않게). 가까운 청크부터, **24개씩 나눠 읽고 사이사이 양보**해 화면이 안 멈춘다. 배치마다 version을 올려 들어온 만큼 바로 보인다.
- **`loadWorld`가 더 이상 전부 읽지 않는다** — 목록만 만들고 원점 주변부터 스트리밍.
- **`Streamer`**(프로토타입) — 카메라가 4m 이상 움직였을 때만 요청(매 프레임 호출 방지).
- **안개(30~70m, 배경색과 동일)** — 청크가 나타나고 사라지는 경계를 가린다. LOD 대신 쓰는 방식(설계서 §2).

**★ 두 가지 함정과 대응**

1. **언로드는 편집이 아니다.** dirty로 표시하면 **"빈 청크"로 저장돼 실제 데이터가 지워진다.** `unloadChunk`는 dirty를 안 건드리고, 스토어는 **내리기 전에 반드시 `flush()`** 한다.
2. **세로는 전부 올린다.** 아래 청크가 없으면 기둥 높이(`colTop`)가 틀려 **브릭이 허공에 얹힌다.** 스트리밍은 **가로만** 한다(청크가 희소해 세로 전체를 올려도 싸다).

**테스트 +11 (37/37)** — 언로드는 실수하면 조용히 데이터가 사라지는 경로라 집중적으로 잠갔다:
언로드 후 개수·`hasChunk`·**dirty 안 생김**·리전에 유령 안 남음·재로드 복원 · **전부 내리면 기둥 높이도 0**(허공 배치 방지).
※ `unloadChunk`가 dirty를 남기도록 고쳐 보니 **그 항목이 정확히 실패**하는 것을 확인했다.

### 2026-07-29 — P3-b 빛 전파 (BFS)

문·창으로 빛이 옆에서 들어오고, 발광 브릭이 주변을 밝힌다. tsc·lint 클린 + 16,214 / 68 / **26** + 라우트 200. **브라우저 확인 대기.**

**★ 무한 맵인데 BFS가 폭발하지 않는 이유** — 이 설계의 핵심:
> **열린 하늘 칸은 아예 다루지 않는다.** `y >= colTop`이면 항상 최대 밝기라 **O(1)로 답이 나오고**, 저장할 필요도 전파할 필요도 없다.
> BFS는 **그늘진 공기**(구조물 안쪽)만 훑으므로 탐색 범위가 **사용자가 지은 만큼**으로 자연히 한정된다. 야외 씬은 사실상 0, 밀폐 공간만 비용이 든다.

- **`world.ts`** — `light: Map<cellKey, 0..30>`(그늘진 공기만) + `lightAt()`(열린하늘 1 / 막힘·미도달 최소값) + `recomputeLight()`.
  - **레벨별 버킷 큐** — 밝은 버킷부터 처리해 각 칸을 사실상 한 번만 확정. `seed`는 항상 더 낮은 버킷에 넣으므로 순회 중 배열이 커지지 않는다.
  - 씨앗 = 브릭에 맞닿은 그늘 칸 중 ①열린 하늘과 닿은 것(하늘빛 유입) ②발광 브릭 옆(광원).
  - `LIGHT_BUDGET = 400,000`으로 병적인 밀폐 구조에서 멈춘다.
- **`faceLight`가 `skyFactor` → `lightAt`으로 전환** — P3-a의 깊이 감쇠는 폴백으로 남는다.
- **스토어 디바운스(140ms)** — 편집마다 BFS를 돌리면 배치가 끊긴다. 편집이 멎은 뒤 한 번 돌리고 version을 올려 면 밝기를 다시 굽는다. 불러오기·스트레스 직후엔 즉시 계산.
- **통계 패널에 `빛 칸 · 계산(ms)` 노출** — 밀폐 구조를 지었을 때 비용을 눈으로 확인할 수 있게.

**테스트 +8 (26/26)** — 빛은 틀려도 에러가 안 나고 어둡게만 보이므로 "반드시 이래야 하는 것"만 잠갔다:
열린 하늘=1 · 밀폐된 방은 어둡다 · **문을 뚫으면 방이 밝아진다**(P3-a가 못 하던 것) · 문에서 멀수록 어둡다 · 발광 브릭이 밀폐 공간을 밝힌다 · 윗면 > 밑면.
※ BFS 전파를 빼 보니 **3건이 실패**하는 것을 확인했다.

### 2026-07-29 — 돌기 모양 5종 + 놓을 면 격자 가이드 (IP·룩 검토용)

**배경**: "누가 봐도 레고인데 괜찮을까"(사용자). 시각적 차별화 안을 **눈으로 보고 고르도록** 전환 기능으로 구현. 상세 IP 판단은 `BRICK_SYSTEM.md §8.5`.

**돌기 모양 5종** (`brickGeometry.ts` `StudStyle`, 실측 삼각형 — 2×4 기준)

| | 보일 때 | 가려질 때 | 비고 |
|---|---|---|---|
| 원형 | 268 | 12 | 레고와 동일 |
| 링 | 396 | 12 | 가운데 뚫림 — 위에서 가장 구별됨 |
| 육각 | 204 | 12 | 가까이서만 구별 |
| 모따기 | 108 | **108** | 돌기 제거 + `RoundedBoxGeometry` |
| **라인** | **12** | **12** | 돌기 제거 + **셰이더 외곽선** |

> 🐛 **정정**: "돌기를 없애면 삼각형이 크게 준다"고 했는데 **모따기에선 틀렸다.** 돌기가 없으면 **스터드 컬링이 무의미해져**(가려져도 108 그대로) 밀집 구조에선 원형(268/12 혼합)보다 **총량이 22% 늘어난다.** 삼각형이 실제로 주는 건 **라인 방식(12)** 이다.

**라인 방식 — 확대/축소에 안 깨지게** (사용자 질문): 선 두께를 UV가 아니라 **화면 미분(`fwidth`)** 으로 잡는다.
- 확대해도 굵어지지 않고 축소해도 사라지지 않음(항상 ~1.2px) · `smoothstep`으로 안티에일리어싱
- **멀어지면 선을 지운다** — 안 그러면 선이 뭉쳐 벽 전체가 거뭇해진다(모아레)
- `vUv`는 텍스처가 없으면 three가 안 만들어 준다 → `#define USE_UV` 주입(three 0.185 지원 확인)
- 지오메트리는 민짜 상자 12삼각형 그대로 — 선은 **픽셀 단계에서만** 그린다

**신규 `PlacementGrid.tsx` — 놓을 면 격자 가이드**
> 사용자 피드백: *"돌기가 없으니 어디에 놓아야 할지 애매하다."*
> **돌기는 장식이 아니라 격자를 눈에 보이게 하는 장치였다.** 없애면 반드시 대체가 필요하다.

브릭이 앉을 **밑면 높이**(anchor.y)에 격자 판(삼각형 2개)을 커서 따라 띄운다. 월드 좌표 기준이라 실제 셀에 정확히 맞고, 라인과 같은 `fwidth` 기법이라 확대/축소에 일정하다. 가장자리 페이드로 하드컷 방지, 멀면 사라짐. `useFrame`으로 위치만 갱신(리렌더 없음), `raycast=null`.
**고정 모드에선 미표시** — 작업 평면 격자가 이미 같은 일을 한다(중복 방지).

### 2026-07-29 — P2-a 리전 배칭 (프러스텀 컬링 살리기)

12만 브릭 실측이 가리킨 병목을 고쳤다. tsc·lint 클린 + 테스트 16,214 + 68 + 18 + 라우트 200. **브라우저 재측정 대기.**

**문제**: 12만 브릭이 `InstancedMesh` **2개**에 들어 있었다. three는 **오브젝트 단위**로 프러스텀 컬링하므로, 오브젝트가 2개면 화면 밖 지오메트리도 매 프레임 전부 처리된다 — **컬링이 사실상 0**.

**수정**: **리전**(렌더 배칭 단위)을 도입. 청크(저장 단위)와 **일부러 다르다**.
- 청크마다 만들면 draw call 폭발(507청크), 전체를 하나로 묶으면 컬링 0 → 그 사이 크기로 자른다
- `REGION = 4×4×4 청크 = 64×128×64 셀 = 32 × 25.6 × 32 m`
- `InstancedMesh = (리전 × 파츠 × 스터드변형 × 재질군)`

**덤으로 얻은 것 — 부분 갱신**: 예전엔 브릭 하나 놓을 때마다 **보이는 인스턴스 전부**의 행렬을 다시 썼다(12만에서 16,808개). 이제 `regionRev`(리전별 리비전)로 **바뀐 리전만** 다시 쓴다. 리비전은 **렌더 결과가 실제로 바뀔 때만** 올린다(가시성 전환 / 스터드 有↔無).

**구현**
- `grid.ts` — 리전 상수 + `regionCoordOf`·`regionKeyOf`
- `world.ts` — `regionBricks`(리전 → **보이는** brickId) + `regionRev` + `regionSnapshot()`/`regionBrickIds()`. `refresh()`가 상태 변화를 감지해 멤버십·리비전 갱신
- `BrickInstances.tsx` — 리전별 `<Region>`이 `rev`로 memo. 재질은 리전마다 만들지 않고 캐시 공유. `computeBoundingSphere()` 필수(컬링 판정에 쓰임)

**실측 (사용자, 12만 브릭 · 배율 1 · 1920×911)**

| | 리전 이전 | 리전 이후 |
|---|---|---|
| FPS | 32 | **52** (+62%) |
| 삼각형(총) | 1,481,696 | 1,481,940 (그대로) |
| 보이는 브릭 | 16,808 (13%) | 16,807 (13%) |
| InstancedMesh | 2 | 45 |

**삼각형이 그대로인데 프레임만 올랐다 = 순수하게 컬링이 살아난 효과.**

> 🐛 **계기판을 두 번 틀렸다 (기록해 둠)**
> 1. `draw call`로 표시하던 값이 실제 draw call이 아니라 **씬의 InstancedMesh 총 개수**였다 → `gl.info.render`로 교체.
> 2. 그런데 `gl.info.render`는 **한 프레임의 모든 패스 합계**다. directional light가 `castShadow`라 **그림자 맵 패스**에서 씬을 한 번 더 그리고, 그게 함께 잡힌다.
>    → 실측에서 `실제 삼각형 1,883,276 > 브릭 총량 1,482,756`이라는 **말이 안 되는 수치**로 드러났다(컬링 효과가 그림자 패스에 묻힘).
>    → 라벨을 **"프레임 전체(그림자 패스 포함)"** 로 바로잡고, **그림자 on/off 토글**을 넣어 비용을 분리 측정하게 했다.
>
> **교훈**: 계기판이 틀리면 최적화 판단 전체가 틀어진다. 숫자가 상식과 어긋나면(실제 > 전체) 코드보다 계기판을 먼저 의심할 것.

> 💡 **그림자에 대한 설계 메모**: 지금의 실시간 directional 그림자는 프로토타입 뼈대에서 온 **기본값이지 설계 결정이 아니다.**
> 마인크래프트는 실시간 그림자가 없다 — **빛 레벨 전파 + 정점 AO**(전부 구워진 값)로 입체감을 낸다. 우리 P3가 정확히 그 계획이다.
> → **P3가 들어오면 실시간 그림자 패스를 아예 뺄 수 있다.** 더 마크다운 룩이면서 프레임도 벌린다. P3 착수 시 함께 판단할 것.

**그림자 on/off 분리 측정 (12만 브릭, 구조물 근접)**

| | 그림자 켬 | 그림자 끔 |
|---|---|---|
| FPS | 53 | **116** |
| 프레임 삼각형 | 623,536 | **157,128** |
| 프레임 draw call | 18 | **7** |
| 브릭 삼각형 총량 | 1,482,756 | 1,482,756 |

**① 컬링 효과 확인** — 총량 1,482,756 → 실제 157,128 = **89.4% 컬링**. 메시 47개 중 draw call 7개(85% 컬링). 리전 배칭이 의도대로 작동.

**② 그림자가 최대 비용** — 프레임 시간 8.6ms → 18.9ms. **그림자만 10.3ms로 나머지 전부(8.6ms)보다 크다.** 그림자 맵은 빛 시점 렌더라 **카메라 컬링이 안 먹는다**(157K → 623K, 4배).

**③ 판정: 스터드 LOD 불필요 — 카드 접음.** 실제로 그리는 건 157K뿐이다. 스터드가 총량의 86%지만 **그 총량 자체가 89% 안 그려진다.** 압축할 대상이 화면에 없다.

**④ → P3를 앞당긴다.** 실시간 그림자는 프로토타입 기본값이었고, 마인크래프트는 애초에 실시간 그림자가 없다(빛 레벨 전파 + 정점 AO = 전부 구운 값). P3가 들어오면 **10.3ms를 0으로 만들면서 룩은 더 마크다워진다.**
→ 로드맵 조정: **P2-b 스트리밍 → P3 조명(+실시간 그림자 제거) → P2-c/d 가상바닥·시야거리**

**테스트 `world.test.ts` 18/18**(신규) — 증분 인덱스는 틀려도 **에러 없이 브릭이 안 그려진다**. 그래서 불변식 하나를 잠갔다:
> **증분으로 갱신한 결과 == 처음부터 전부 계산한 결과**

무작위 편집 900회(20%는 삭제) 후 가시성·스터드·리전 멤버십·기둥 높이를 전량 재계산/무차별 스캔과 대조 + 리전 중복 없음 + 좌표에 맞는 리전에 담김 + 리비전이 남의 리전을 안 건드림 + 6면이 막힌 브릭은 리전에서 빠짐.
※ 리전 멤버십 갱신을 빠뜨려 보니 **7건이 실패**하는 것을 확인했다.

### 2026-07-29 — ✅ P0 게이트 통과 (폴리곤 예산 실측)

**사용자 측정** (배율 1 · 1920×911):

```
FPS 32 · 브릭 125,000 · 보이는 브릭 16,808 (13%) · draw call 2
삼각형 1,481,696 · 청크 507
```

**검산 — 설계 예측과 정확히 일치했다** (우연이 아니라 구조가 의도대로 돈다는 증거):

| 항목 | 계산 | 실측 |
|---|---|---|
| 총 브릭 | 층당 5,000(=100×50) × 25층 | 125,000 ✅ |
| 보이는 브릭 | 윗면 5,000 + 밑면 5,000 + 옆면 296×23 = 6,808 | 16,808 ✅ |
| 삼각형 | 스터드有 5,000×268 + 無 11,808×12 | 1,481,696 ✅ |

→ **스터드가 남은 건 맨 윗층 5,000개뿐**. 보이는 것의 70%가 스터드를 생략했다.

| 판정 | 결과 |
|---|---|
| 묻힌 브릭 스킵 | **13%** (87% 제거) — 예측 86%와 일치 ✅ |
| 스터드 컬링 | 보이는 것의 70% 생략 ✅ |
| draw call | **2** (12만 브릭인데) ✅ |
| FPS | 32 ⚠️ — 원인은 아래 |

**★ 삼각형의 86%가 스터드다** (5,000 × 256 = 1,280,000). 2×4 브릭 = 박스 12 + 스터드 8×32 = **268 삼각형**.

**그럼에도 스터드 LOD는 지금 하지 않는다 — 진짜 병목은 프러스텀 컬링이 0인 것.**
12만 브릭이 `InstancedMesh` **2개**에 들어 있다. three는 오브젝트 단위로 컬링하므로 오브젝트가 2개뿐이면 **화면 밖 지오메트리도 매 프레임 전부 처리**한다(+그림자 패스로 ×2 ≈ 300만 삼각형/프레임).

| 조치 | 효과 | 성격 |
|---|---|---|
| **리전 배칭 (P2)** | 화면 밖 지오메트리가 **아예 사라짐** | 구조적 ← 먼저 |
| 스터드 LOD | 보이는 지오메트리 86% 압축 | 최적화 ← P2 후 재측정하고 판단 |

실사용 규모(수천~2만 브릭)는 이미 여유롭다(2.5만 기준 보이는 것 ~3,300개·삼각형 ~29만). 12만은 천장을 재본 것.

**→ P0 게이트 ①감각 ②폴리곤 예산 모두 통과.** 다음은 P2.

### 2026-07-29 — P1 저장 (청크 · 바이너리 · dirty만 쓰기)

새로고침해도 만든 것이 남는다. tsc·lint 클린 + 테스트 16,214 + 68 + 라우트 200. **브라우저 확인 대기.**

- **`grid.ts`** — 청크 상수/좌표 (`CHUNK_X/Y/Z = 16/32/16` 셀 = 8×6.4×8m, 세로 512÷32 = **16 청크층**) + `chunkCoordOf`·`chunkKey`.
- **`serialize.ts`(신규)** — 청크 하나를 바이너리로.
  - 좌표를 **청크 상대**로 줄여 1바이트씩. 레코드 **7바이트**(partIdx·rot·x·y·z·matIdx u16).
  - **팔레트**로 (재질군, 색) 중복 제거 → 색을 많이 써도 레코드가 안 커진다.
  - **RLE는 쓰지 않는다.** 설계서엔 "팔레트+RLE"라 적었지만 그건 *셀 배열* 전제였다. 우리는 **레코드 리스트**라 RLE가 맞지 않는다(마인크래프트와 갈리는 지점).
  - `PART_ORDER`/`MAT_ORDER`는 **저장 인덱스**다 — 순서를 바꾸면 기존 저장물이 깨진다(뒤에만 추가할 것).
- **`world.ts`** — 청크 인덱스(`byChunk`) + **dirty 추적**. 소유권은 **앵커 셀** 기준(경계를 넘는 브릭도 청크 하나만 소유). 비워진 청크도 dirty로 남긴다(저장소에서 지워야 하므로).
- **`storage.ts`(신규)** — `BrickStorage` 어댑터. P1은 **localStorage** 구현. 청크당 항목 하나라 "바뀐 청크만 쓰기"가 그대로 성립.
- **스토어/프로토타입** — 마운트 시 복원, 변경 후 600ms 디바운스로 **바뀐 청크만** 저장, `pagehide`에도 flush. 우측 패널에 저장 상태·청크 수 표시.

#### ⚠️ 테스트를 한 번 잘못 만들었다 (기록해 둠)

처음엔 왕복(encode→decode)만 검사했는데, **`PART_ORDER` 순서를 바꿔도 66/66 통과했다.** 인코더와 디코더가 같은 표를 쓰니 왕복은 늘 일치하기 때문이다. 그런데 실제 위험은 **옛 데이터를 새 코드로 읽는 것**이고, 그 경우 에러 없이 다른 브릭으로 복원된다(조용히 틀림).
→ **골든 픽스처**(고정 바이트열 + 기대 결과)를 박았다. 순서를 바꾸면 2건이 실패하는 것을 확인했다.
> ⚠️ 앞으로 이 픽스처가 깨지면 = **저장 포맷이 바뀐 것**이다. 픽스처를 새 값으로 갈아끼워 "고쳤다" 하지 말고, 버전을 올리고 마이그레이션을 붙일 것.

#### P1에서 **하지 않은 것** (의도)

- **`brick_chunks` DB 테이블 / 마이그레이션 0009** — 아직 안 만들었다. 프로토타입엔 씬이 없어(`sceneId` 없음) DB 경로를 **실제로 돌려볼 수가 없다.** 검증할 수 없는 코드를 미리 쓰지 않는다는 원칙에 따라, **브릭을 실제 씬에 붙이는 단계**에서 어댑터만 갈아끼운다(`storage.ts`가 그 자리다).
- **델타 기반 undo** — 체크리스트 D에 있으나 P1 범위 밖으로 미뤘다. 현재 프로토타입엔 undo 자체가 없다.
- **압축(gzip)** — 12만 브릭 ≈ 840KB(base64)로 localStorage 한도 안이라 미적용. 필요해지면 `CompressionStream`.

### 2026-07-29 — ★ 배치 기준 전환: 포인터(점) → 고스트(발자국) — 드롭 모델

> **사용자가 근본 원인을 짚었다.** "초록 고스트가 다른 브릭과 겹치는데, 포인터를 그 브릭 위에 올리면 정상이다.
> 포인터로 이벤트가 발생하는 것 때문 아니냐. 브릭 끝에 못 쌓는 것도 같은 이유 같다. 고스트 기준으로 하면 되지 않나."
> — 맞는 진단이었고, 앞선 두 버그가 **같은 뿌리**였다.

**근본 원인 하나**:
> **포인터는 점 하나인데, 놓는 것은 여러 칸짜리 덩어리다.**

레이가 맞은 한 점이 높이와 위치를 **둘 다** 정하고 있었다. 그래서
- 포인터가 안 짚은 칸이 다른 브릭을 **파고들었다**(겹침). 아무도 그 칸을 확인하지 않았으니까
- 브릭이 갈 수 있는 자리를 **포인터가 갈 수 있는 자리가 잘라먹었다**(끝에 못 쌓음)
- 포인터를 브릭 위에 직접 올리면 정상 동작한 것이 **바로 이 설명의 증거**였다

**바뀐 규칙**:
```
전:  레이가 맞은 점  →  높이 + 위치 둘 다
후:  포인터        →  가로 위치(XZ)만
     고스트 발자국 아래  →  높이(Y)   ← BrickWorld.restY
```
발자국 아래 기둥 중 **가장 높은 것 위에 얹힌다.** 레고를 손에 들고 옮기면 아래 있는 것 위로 타고 올라가는 것과 같다.

**구현**
- `world.ts` — 기둥 높이 캐시 `colTop`(colKey → 꼭대기+1) + `restY()`. 배치 시 `raiseColumns`, 삭제 시 `recomputeColumn`(현재 높이에서 아래로 훑되, 위에 뭐가 남아 있으면 즉시 멈춤). `grid.ts`에 `colKey` 추가.
- `placement.ts` — **`anchorFromPointer()` 하나로 통합.** 브릭 위든 빈 바닥이든 규칙이 하나다.
- **삭제**: `BuildPlane`(어제의 땜질) · `faceOf` · `anchorFromFace` · `anchorFromGround` · `reachRange` · `Face` 타입 · hover의 면 추적. **잔존 참조 0 확인.**
  → 어제 만든 확장 평면은 포인터 기준의 한계를 억지로 넓힌 것이었다. 원인을 고치니 **필요 없어져 통째로 사라졌다.**

**테스트 `placement.test.ts` 16,205/16,205** (`npx tsx src/lib/brick/placement.test.ts`)
불변식 둘을 고정한다 — ①**겹치지 않는다**(restY에선 항상 배치 가능) ②**딱 그 위에 얹힌다**(한 칸만 낮춰도 반드시 막힘 = 공중에 안 뜸).
무작위 지형(브릭 400개, 오버행·빈틈 포함) 위에서 파츠 4종 × 회전 4 × 격자 19×19 전수 + 신고 케이스(끝에 한 칸 걸치기·완전히 벗어나면 바닥) + 기둥 캐시 정합성(지우면 높이 복귀, 위에 남으면 유지).
※ `restY`를 0 고정(옛 포인터 방식)으로 되돌려 **3,666건 실패**하는 것을 확인했다.

**의도적으로 포기한 것(사용자 승인)**: **옆면 붙이기·아래 매달기.** 이제 브릭은 항상 발밑으로 내려앉는다. → 곧바로 아래 "높이 고정 모드"로 보완했다.

#### 🔒 높이 고정 모드 (같은 날, 사용자 요청)

드롭 모델이 못 하는 일 — **받쳐줄 게 없는 자리**(천장·2층 바닥·다리 상판·창문 위 인방) — 을 메우는 보조 모드.

| | 드롭 모드(기본) | 높이 고정 모드 |
|---|---|---|
| 높이 | 발자국 아래에서 자동(`restY`) | **사용자가 지정(`lockY`)** |
| 공중 배치 | 불가 | **가능** |
| 겹침 | 구조적으로 불가 | 겹치면 빨강(막힘) |

- **스키마/스토어**: `lockY: number | null`(null=드롭) + `toggleLock(currentY)`(현재 고스트 높이로 잠금·재누르면 해제) + `nudgeLock(±1)`(한 단 = 브릭 높이 3칸). `clampLock`으로 0~`Y_MAX` 안에 가둔다.
- **배치 규칙**: `anchorFromPointer(..., lockY)` 한 곳에서 `lockY ?? restY(...)`로 분기. **규칙이 여전히 한 곳에만 있다.**
- **작업 평면**: 고정 시 그 높이에 투명 레이캐스트 평면(400×400) + 파란 격자를 띄운다. **고정 모드에선 XZ를 이 평면에서만 받는다** — 브릭 표면에서 받으면 높이가 달라 커서와 고스트가 시차로 어긋난다(브릭·바닥 hover 핸들러를 `locked`로 게이트).
- **조작**: `L` 토글 · `PageUp/PageDown` 층 이동 · 좌측 패널에 `N층 · N.Nm` 표시와 ± 버튼.
- **테스트 +9** (총 16,214) — 기둥 두 개 사이 빈 공간에서 드롭은 바닥(0)으로 떨어지고 고정은 지정 높이 유지 · XZ 불변 · 겹치면 배치 불가 · 지형이 달라도 고정 높이 유지.

### 2026-07-29 — 🐛 확장 배치 평면 (커서 도달 범위 = 배치 가능 범위)

**사용자 보고**: "2×4를 바닥에 놓고 그 **돌기 끝부분**에 2×4를 끼우려 하면, 마우스 포인터가 아래 브릭을 벗어나면서 끼울 수 없다."

**원인**: 자유 축 좌표를 아래 브릭 **표면의 레이캐스트 지점**에서만 얻고 있었다. 여러 칸짜리 브릭은 아래 브릭에 **한 칸만 걸치는 위치**까지 유효한데, 커서는 아래 브릭 표면 위에만 머물 수 있어 바깥쪽 자리에 도달을 못 했다. 커서가 브릭을 벗어나는 순간 레이가 바닥 평면에 맞아 y=0으로 떨어졌다.
→ **커서가 갈 수 있는 범위가 놓을 수 있는 범위를 잘라먹고 있었다.** 1×1 블록(마인크래프트)에선 생기지 않는, 다중 칸 파츠 고유의 문제.

**수정**:
1. **`BuildPlane`**(신규, `BrickPrototype.tsx`) — 접촉면을 **닿을 수 있는 모든 위치만큼**(각 축 ±(새 브릭 길이−1)칸) 넓힌 투명 평면. 브릭 표면보다 **0.002m 안쪽**에 둬서 브릭 위에서는 브릭이 먼저 잡힌다(면 판정 유지). 살짝 보이게(opacity 0.07) 해서 "여기까지 놓을 수 있다"가 눈에 보인다.
2. **`face`를 hover 상태에 고정** — 확장 평면 위의 좌표는 브릭 AABB **바깥**이라, 거기서 `faceOf`를 다시 부르면 엉뚱한 면이 나온다. 브릭에 처음 닿은 순간 확정해 들고 다닌다.
3. 브릭 `onPointerOut`의 `null`을 무시 — 바닥/확장 평면 이벤트가 곧바로 갱신하므로 깜빡임 방지.

#### 🐛 후속 — 확장 평면이 "반쪽만" 되던 문제 (같은 날)

**사용자 보고**: "위는 되는데 아래는 안 되고, 오른쪽은 되는데 왼쪽은 안 된다. 반쪽만 된다."

**원인 — 규칙이 두 군데에 따로 적혀 있었다.** `placement.ts`의 `centered()`는 가리킨 칸에서 `floor((길이−1)/2)`만큼 당겨 앵커를 잡는데, `BuildPlane`은 그 보정을 모른 채 평면을 깔았다. 그래서 도달 범위가 **한 칸씩 밀려서** 한쪽 끝은 못 가고, 반대쪽은 **아예 닿지도 않는 자리(공중 배치)까지** 갈 수 있었다.
- 2×4 기준: 필요한 앵커 [−3…+3] ↔ 실제 도달 [−4…+2]
- 짧은 축(길이 2)은 보정이 0이라 멀쩡했다 → **긴 축에서만 티가 나는 비대칭**

**수정**: `placement.ts`에 **`centerOffset()`·`reachRange()`를 단일 기준점으로 export**하고 `BuildPlane`이 그걸 쓰게 했다. 이제 규칙이 한 곳에만 있다.

**테스트 `placement.test.ts` 1544/1544** (`npx tsx src/lib/brick/placement.test.ts`)
불변식 하나를 고정한다 — **커서가 갈 수 있는 범위 == 아래 브릭에 닿을 수 있는 앵커 범위.**
파츠 4종 × 회전 4 × 파츠 4종 × 회전 4 × 좌표 3곳 × 2축 전수 + 신고 케이스(2×4 위 2×4의 ±3 끝자리 도달, ±4 공중자리 차단).
※ 옛 공식으로 되돌려 **184건 실패**하는 것을 확인했다(테스트가 실제로 이 버그를 잡는다).

**미해결(후속)**: **옆면(±x/±z)은 확장 평면이 없다.** 벽을 따라 어긋쌓기할 때 같은 제약이 남는다(브릭 자체 길이 안에서만 어긋낼 수 있음). 수직 평면이라 카메라 방향에 따라 epsilon 부호가 갈려서 P0 범위에서 제외했다. 벽 쌓기에서 불편하면 그때 추가.

**덤**: Canvas `shadows` → `shadows="percentage"`(three가 PCFSoftShadowMap을 deprecate — 기존 `ViewerCanvas`와 동일하게 맞춤).

### 2026-07-28 — 방향 결정 (대화 전문 요약)

**출발점(사용자)**: 에디터에서 테이블을 만들어봤는데 너무 불편했다. 박스로 상판을 만들고 다리를 놓으려니 기즈모를 이리저리 움직이고 카메라를 돌려야 했다. 마인크래프트처럼 복셀 방식으로 바꿔야 할 것 같고, 기본 오브젝트(프리셋)도 많이 필요하다.

**진단**: 원인은 복셀 부재가 아니라 **상대 배치 개념의 부재**였다. 코드로 확인:
- `EditorCanvas.tsx:922` — 배치가 `groundPointFromEvent`로 **항상 y=0 평면에만** 떨어짐. 오브젝트 위에 놓는 경로가 없음
- `sceneStore.ts:632,635` — 그리드 스냅·오브젝트 스냅 **둘 다 기본 꺼짐**. 오브젝트 스냅은 가이드선도 없고 그룹도 제외
- `alignSelected` — min/center/max 맞추기뿐, **"A 위에 B 올리기"가 없음**
- 프리셋 8종이 전부 메커니즘 데모(로봇팔·문·엘리베이터·회전문·기어·바퀴·동전). **가구/건축이 0개**

**마인크래프트 사실 확인** (사용자가 영상으로 관찰한 6가지):

| # | 관찰 | 판정 |
|---|---|---|
| 1 | 사각박스 하나가 기본단위 | ✅ 맞음 |
| 2 | L자형이어도 1×1×1 안에 들어간다 | ✅ 맞음 — **격자는 배치의 단위이지 형태의 단위가 아니다.** 계단·반블록·울타리가 그 예. 레고 파츠로 가는 다리 |
| 3 | 각 재질의 박스가 존재 | ⚠️ 맞지만 마크는 형태×재질이 **곱해진 카탈로그**(~1000종). 브릭은 직교 분해 가능 |
| 4 | 재질에 조명·불·물·눈·식물 포함 | ⚠️ 화면상 맞음. 구현으로는 각각 **별개 시스템**(광원 전파 / 유체 시뮬 / 십자판 렌더 / 번짐 로직) |
| 5 | 바닥을 팔 수 있다 | ✅ 맞음. 단 **세로는 유한**(1.18+ 기준 −64~319, 384칸). 가로만 무한 |
| 6 | 십자 커서가 면에 반응 — 마우스로 가능? | ✅ **둘 다 이미 구현됨.** 마우스=기본 R3F / 십자=`ViewerCanvas.tsx:336-343` `centerPointer` + `HoverUpdater` |

**사용자가 나열한 엔진 구성요소 평가**: 대체로 정확했으나 ①**Greedy Meshing과 GPU Instancing이 서로 배타**(둘 중 하나) ②**면 제거(hidden face removal)가 빠짐** — 가장 큰 항목 ③조명 전파/AO, 청크 경계 처리, 델타 undo, 팔레트 저장, 반투명 정렬이 누락.

**결정 경로**:
1. 복셀 월드 전환은 **재질/물리/관절/계층/기즈모/undo/저장을 거의 재사용 못 함** → 로그인만 공유하는 두 번째 제품에 가까움
2. 사용자 요구(비큐브 형태 + 파츠별 색)를 만족하려면 **그리디 메싱이 무력화**됨 → 인스턴싱이 정답
3. 사용자가 **레고 형태 + 마크 시스템 + 무한 맵**을 확정 → 청크는 필수로 복귀(내 이전 조언 정정)
4. 우리 무한 ≠ 마크 무한: 마크는 **절차 생성**, 우리는 **희소 저장 + 가상 바닥** → 훨씬 저렴

**확정된 4가지**: 세로 512칸 / 걸어다니는 크기(스터드 0.5m) / 레고 비율(셀 Y 0.2m, 브릭 3칸) / 파츠 4종 / 재질군 3종+색.

**작성물**: `doc/BRICK_SYSTEM.md`(설계서), `doc/BRICK_PROGRESS.md`(이 문서).
