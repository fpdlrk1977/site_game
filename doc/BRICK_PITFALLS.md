# BRICK_PITFALLS — 브릭 작업 전 필독 (반복해서 터진 함정)

> 브릭 관련 코드를 건드리기 전에 이 파일을 읽는다. 작업이 끝나면 **이번에 배운 함정을 여기에 추가**한다.
> 진행 기록은 `BRICK_PROGRESS.md`, 설계는 `BRICK_SYSTEM.md`, 계획은 `BRICK_PLAN.md`.

---

# ⚠️ 작업 전 필독 — 반복해서 터진 함정

> **이 절을 먼저 읽고 시작할 것.** 아래는 전부 **실제로 두 번 이상 났거나, 한 번 났는데 원인이 구조적이라 또 날** 것들이다.
> 새 코드를 짤 때가 아니라 **이미 있는 것을 건드릴 때** 터진다. 새 항목이 생기면 여기에 추가한다.

## A. 청크·좌표 — 가장 많이 터진 곳

**A-1. 청크는 세로로도 나뉜다 (`CHUNK_Y = 32`).**
지형(y −3..−1)은 `cy=1`, 지면에 놓은 브릭(y 0..2)은 **`cy=2`** 다. 청크를 다룰 때 `cx,cz`만 보면 **다른 층의 청크를 같은 것으로 착각**한다. → `provesTerrainGenerated()` 참고. 이것 때문에 "브릭 하나 놓고 새로고침하면 땅이 8m×8m 패임"이 났다.

**A-2. 같은 값이라도 용도가 다르면 기본값이 다르다.**
`topOfColumn`의 기본값은 **0**(빈 곳에 놓으면 지면 높이)인데, 빛의 `isOpenSky`는 **`Y_MIN`** 이어야 한다(땅이 없는 곳은 지하가 아니라 허공). 하나로 합치면 빛 BFS가 빈 지하로 흘러넘친다(실측 43만 칸).

**A-3. 지형은 "한 겹"이고 그 아래는 데이터가 없다.**
아래를 그냥 빈 칸으로 보면 빛·탐색이 땅 밑 전체로 샌다. **안 판 땅은 `solidBelow`로 막는다.**

**A-4. 좌표를 새로 만들 땐 음수를 먼저 생각한다.**
파기가 들어오면서 `y`가 음수가 됐다. `?? 0` / `top > 0` / `let top = 0` 같은 코드가 전부 구멍 안 배치를 깨뜨렸다.

**A-5. ★★★ 칸은 정육면체가 아니다 — 세로축이 걸린 회전은 격자에 안 앉는다.** (2026-08-04)
`CELL_X = CELL_Z = 0.5m`인데 **`CELL_Y = 0.2m`** 다(2.5 : 1). 그래서 **X·Z끼리 도는 회전(지금까지의 rot 0~3)만 안전**하고,
**세로가 가로로 가는 회전(옆으로 눕히기)은 어떤 파츠도 정수 칸에 안 떨어진다**:

```
브릭 높이 0.6m → 가로 칸  0.6 / 0.5 = 1.2칸    (정수 아님)
스터드 폭 0.5m → 세로 칸  0.5 / 0.2 = 2.5칸    (정수 아님)
```

- **칸 수를 축 교환(permutation)으로 돌리는 것과 실제로 회전시키는 것은 다르다.** 칸이 정육면체일 때만 같다.
  1×4를 옆으로 눕히면 **그린 것 0.60×0.50×2.00 · 점유 1.50×0.20×2.00** — 브릭이 제 상자를 뚫고 나오고 옆에 붙이면 틈이 뜬다.
- **판별식**: `rotMatrixPlace(rot)[4] !== 0`(월드 Y가 로컬 Y에서 오는가) → `rotation.ts`의 `isGridExact`.
  지금 쓸 수 있는 눕히기는 **그대로(0)·뒤집기(5)** 둘뿐이고, UI는 `nextExactTip`으로 그 밖을 못 나간다.
- ⚠️ **테스트를 축 배치로 쓰면 못 잡는다.** 나는 "화면 기준과 배치 기준의 축이 같다"를 통과시켜 놓고
  **안전하다고 결론냈다** — 축은 맞고 **크기(m)** 가 틀린 것이었다. 반드시 **미터 단위로** 비교할 것(`rotation.test.ts`).
- 결국 **화면을 보고 잡았다.** 1분 만에 눈에 띄었고, 순수 함수 테스트 1,900개가 전부 통과하고 있었다(T-2와 같은 계열).
- 실제 레고도 같은 제약이다(브릭 9.6mm : 간격 8mm) — 그래서 옆으로 붙이는 **전용 브래킷 부품**이 따로 있다.

## B. 저장 — "비어 있음"에는 두 가지 뜻이 있다

**B-0a. sceneId ≠ projectId.** 에디터 주소는 `/editor/<projectId>`이고, **씬 id는 미리보기(👁)가 여는 `/space/<sceneId>`에 있다.** projectId를 씬 자리에 넣으면 RLS가 `new row violates row-level security policy`로 막는다 — 정책 조건(그 씬이 내 프로젝트인가)이 거짓이 되기 때문. **RLS 오류를 보면 먼저 id 종류와 로그인 여부를 의심할 것.** (`/test/*`는 보호 경로가 아니라 **로그인 없이도 열린다** → `auth.uid()`가 null이면 같은 오류가 난다)

**B-0. 스키마를 짐작하지 말 것 — 기존 마이그레이션을 읽고 그 패턴을 따라라.**
`0009_bricks.sql`에서 `scenes.is_published`를 썼는데 **그 컬럼은 `projects`에 있다**(`scenes`엔 없다). `scenes`의 공개 읽기 정책도 프로젝트를 조인해 판정한다(`0001_init.sql`) — **그걸 먼저 읽었으면 없을 실수**였다.
또한 **정책엔 `create policy if not exists`가 없다.** 실행이 중간에 실패하면 재실행이 막히므로 **`drop policy if exists`를 먼저** 쓴다.

**B-0b. ★★ "메모리에 올라와 있는 것"과 "저장소에 있는 것"은 다른 목록이다.**
언로드 판정을 **저장소 목록**으로 하면 **생성한 지형이 영원히 안 내려간다**(저장하지 않으므로 그 목록에 없다). 실측 8,215청크 / 525,760브릭. → 언로드는 `world.loadedChunks()`로.
> **리전 컬링이 FPS를 지켜 줘서 증상이 안 보였다**(142 FPS). **메모리만 조용히 무한 증가.** 이런 종류는 눈으로 못 찾는다 — **통계 숫자를 읽어야** 잡힌다(`청크 로드/전체`가 딱 알려줬다).
> 그리고 **내릴 때는 딸린 기록도 같이 잊어야** 한다(`terrainDone`·`filledTo`·`solidBelow`). 안 그러면 그것들이 무한히 커지고, 되돌아왔을 때 "이미 했다"며 다시 안 만든다.

**B-1. 빈 청크 = 삭제 ≠ 빈 청크 = 다 파냄.**
- **지면층**은 비어도 **남긴다** — 지우면 "다 파냈다"는 사실이 사라져 **판 구덩이가 되메워진다**.
- **전체 지우기**는 반대다. 저장소를 **통째로 비워야** 한다. 안 그러면 빈 지면 청크가 "다 파냈다"로 읽혀 **새로고침 후 바닥이 영영 안 깔린다**.
- → 한 규칙을 고칠 때 **반대쪽도 같이 확인할 것.** 실제로 B의 앞 절반을 고치자마자 뒤 절반이 터졌다.

**B-2. `PART_ORDER`는 append-only.**
순서를 바꾸면 기존 저장물이 **다른 파츠로 디코드**된다. 골든 픽스처가 지키지만, 그 앞에서 멈춰 생각할 것.

**B-4. ★★ 저장은 "쓰기"만 확인하면 안 된다 — *다시 읽어 넣는 곳*까지 봐야 한다.**
새로고침하면 **모든 브릭이 하얗게** 변하고 있었다(2026-08-02 발견, 무늬 기능이 생긴 07-31부터 쭉).
원인은 한 줄 — 청크를 불러와 월드에 넣을 때 **`tex`(무늬) 인자를 안 넘겨서** `placeFast`의 기본값 `0`(민짜)이 들어갔다.

```ts
world.placeFast(d.part, d.x, d.y, d.z, d.rot, d.color, d.mat);        // ← 무늬가 사라진다
world.placeFast(d.part, d.x, d.y, d.z, d.rot, d.color, d.mat, d.tex); // ← 맞다
```

- **저장 쪽은 처음부터 멀쩡했다.** 바이너리에도, 디코드 결과(`d.tex`)에도 값이 있었다. **마지막 한 칸에서만** 떨어졌다.
- **왜 오래 안 들켰나**: ①놓은 직후엔 정상이라 **새로고침을 해야만** 보인다 ②에러가 없다
  ③바로 옆 `d.color`는 넘기고 있어서 **코드를 봐도 눈에 안 띈다** ④기본값이 있는 선택 인자라 타입 검사도 통과한다.
- **판별법**: "새로고침하면 뭔가 초기값으로 돌아간다"는 증상이면 **저장 포맷이 아니라 복원 호출부의 인자**를 먼저 볼 것.
  저장 데이터를 직접 까 보면(브라우저에서 `localStorage` 디코드) **데이터는 멀쩡한데 화면만 다르다**는 게 바로 갈린다 — 그러면 범인은 복원 쪽이다.
- **막는 법**: 테스트는 "저장 → **새로고침** → 값이 살아 있나"로 **왕복**을 봐야 한다(`brickStore.test.ts`).
  인코딩만 검사하는 테스트는 이 결함을 **영원히 통과시킨다**(실제로 저장 테스트 254개가 전부 통과하고 있었다).
- **새 필드를 브릭에 추가할 때마다** 이 왕복 테스트에 항목을 하나 더할 것. 인자 목록 끝에 붙는 선택 인자가 특히 위험하다.

**B-3. 저장은 dirty만 쓴다.** 손 안 댄 지형까지 저장하면 방문한 청크마다 64블록이 쌓인다(`clearDirty`).

## C. 파생 인덱스 — 틀려도 에러가 안 난다

**C-1. `placeFast`는 가시성을 안 잡는다.** 끝나면 반드시 `refreshPending()`. 안 부르면 **브릭이 조용히 안 그려진다.**

**C-2. 증분 갱신 == 전량 재계산.** 이게 유일한 안전장치다. 새 증분 경로를 만들면 **지문 비교 테스트를 같이** 만들 것(`world.test.ts` 참고).

**C-3. 언로드도 이웃을 다시 잡아야 한다.** 가리던 브릭이 내려가면 이웃 면이 열린다.

## D. 렌더 — 구운 값 위에 실시간 효과를 겹치지 말 것

**D-1. 방향광·실시간 그림자는 쓰지 않는다.** 마인크래프트엔 태양 방향이 없다. 면 밝기는 **하늘빛 × `FACE_TONE`(고정 배율)**. 방향광을 되살리면 "블록에 그림자가 진다"·"카메라 돌리면 번쩍인다"가 즉시 재발한다.

**D-2. 반사광(specular)은 보는 방향에 따라 변한다.** 거칠기를 낮추면 구운 값과 싸운다. 지형은 무광(0.98), 브릭은 0.72.

**D-2b. ★ 환경광은 `π`다 — 낮추면 그늘이 안 보인다.** three는 환경광을 **`intensity / π`** 로 쓴다(램버트 BRDF에 `1/π`가 들어 있다).
따라서 **`Math.PI`여야 알베도가 1:1**로 나온다 — 고른 색이 그 색 그대로, 흰색이 진짜 255. 그 위에서 면 배율·AO는 **깎기만** 하므로 잘리지도 않는다.
`π`보다 낮추면 바닥이 어두워지고, **어두운 색 위에서는 같은 배율이라도 절대 차이가 작아 그늘이 안 보인다**(실측: 환경광 1.0 → 갈색 바닥 61, 그늘 46. `π` → 107 / 88).
**검증법: 흰 브릭이 화면에서 255로 나오는가.** 1.0일 땐 153이었다.

> ⚠️ **여기서 크게 헛돌았다(2026-07-31)**: "1.0을 넘으면 잘린다"고 단정하고 1.6 → 1.0으로 **낮췄다.**
> `1/π` 나눗셈을 빼먹은 계산이었고(1.6은 실제로 0.51이라 잘림과 무관), **화면만 더 어두워져 그늘이 더 안 보이게** 됐다.
> 원인을 세 번 잘못 짚는 동안 공통점은 하나 — **화면 값을 재지 않고 추측했다.** 아래 D-2c가 그 해법이다.

**D-2c. ★ 룩 문제는 추측하지 말고 화면 값을 재라.** `BrickInstances.tsx`의 **`DEBUG_BAKED_LIGHT`** 를 켜면 색을 무시하고 **구운 값을 흑백으로** 그린다.
거기에 **스크린샷 픽셀을 직접 읽으면**(PNG는 zlib으로 60줄이면 디코드된다) "값이 안 오는가 / 색에 묻혔는가"가 한 번에 갈린다.
이번에 AO를 두 번 헛고친 뒤 이 방법으로 **5분 만에** 결론이 났다 — 열린 바닥 153(=1/π, 환경광 정상) · 밑동 96~141(=AO 정상 동작) · 색 화면 61 vs 46(=너무 어두워 안 보였을 뿐).

**D-6. ★★ 회전 기준이 두 개다 — 화면과 배치가 서로 반대다. 합치지 말 것.**
2026-08-02에 숫자로 확인했다. **원래부터 그랬다**:

| | 어디 | 방향 |
|---|---|---|
| **화면 기준** | 메시 회전(`setFromAxisAngle`) · 밝기표(`CORNER_MAP`) | Y축 **+90°** |
| **배치 기준** | 기준 칸(`parts.pivotOffset`) | Y축 **−90°** |

`rot 1`과 `rot 3`이 서로 뒤바뀌어 있다(0·2는 같다).

- **왜 안 들켰나**: 브릭은 상자라 **점유 범위가 어느 쪽이든 같다**(90°든 −90°든 X/Z 크기만 맞바뀜).
  차이가 드러나는 건 **경사처럼 앞뒤가 있는 파츠**와 **R을 눌렀을 때 몸통이 커서 어느 쪽으로 도는가**뿐이다.
- **왜 그냥 두나**: 각자 제 일에는 일관돼 있고 **사용자 확인까지 끝난 동작**이다.
  한쪽으로 "정리"하면 **이미 지어 둔 경사 지붕 방향이나 R 조작감이 뒤집힌다.** 얻는 것 없이 회귀만 난다.
- **24방향으로 늘릴 때도 각자의 기준에서 각자 늘린다** — `rotation.ts`의 `rotMatrixVisual` / `rotMatrixPlace`.
  둘을 섞으면 **에러 없이 명암만 조용히 뒤집힌다**(사보타주로 확인: 밝기표를 배치 기준에 물리니 4개 중 2개만 일치).
- ⚠️ **회전 방향을 손으로 유도하지 말 것.** 내가 유도했을 때 반대로 나왔다.
  `rotation.test.ts`가 rot 0~3에서 기존 값과 대조하니 **거기에 맞춰 고정**할 것.

**D-3. 면 밝기는 면당 하나다.** 평균에 **막힌 칸을 넣으면 안 된다** — 2×2 지형 타일에 브릭이 걸치면 타일 **전체**가 어두워져 그림자처럼 보인다.

**D-4. 인스턴스 속성을 붙일 땐 지오메트리를 공유하지 말 것.** 캐시된 지오메트리에 직접 붙이면 그룹끼리 덮어쓴다(껍데기만 새로 만들어 정점 버퍼는 공유).

**D-5. ★ 조준 표시는 반드시 표면과 같은 평면에 그린다.**
공중에 뜬 것을 그리면 **시차(parallax)** 로 어긋나 보인다. 브릭은 0.6m, 셀은 0.5m — 카메라가 45°면 화면에서 **한 칸보다 크게** 밀린다. "격자에 맞춰 클릭했는데 1칸 어긋난다"가 정확히 이것이었고, **배치는 맞고 보이는 게 틀린** 경우였다.
> **판별법: 수직으로 내려다보고 클릭해 본다.** 거기서 정확하면 시차다(90°에서 시차는 0). 로직을 뜯기 전에 이걸 먼저 해 볼 것 — 나는 이걸 안 해서 배치 계산을 두 번 헛고쳤다.

**D-7. ★★ 배경(하늘·산·구름)에서 세 번 밟은 것.** (2026-08-04, §16 작업)

1. **프로토타입과 에디터의 배경·조명이 갈라져 있었다.** `BrickEnvironment`를 만들어 뒀는데
   `/test/brick`은 그걸 **안 쓰고 같은 내용을 손으로 복사**해 두고 있었다 — 그래서 환경광이
   에디터는 `π`, 프로토타입은 옛 `1.6`이었고, 배경을 붙였더니 **프로토타입에만 안 나왔다.**
   → **"공용 컴포넌트를 만들었다"와 "다들 그걸 쓴다"는 다른 얘기다.** 만든 뒤 `import` 개수를 세어 볼 것.
   (브릭 렌더·도구 패널·카메라는 이미 공용인데 배경만 빠져 있었다)

2. **배경 크기는 미터가 아니라 *화면에서 차지하는 각도*로 정해야 한다.**
   참고 수치(650m 거리 / 380m 높이)를 그대로 넣었더니 `atan(380/650) = 30°`,
   시야각이 ±25°라 **산이 하늘을 통째로 덮었다.** 지금은 950m / 280m ≈ 16°.
   → 먼 물체를 놓을 땐 **거리와 크기를 따로 보지 말고 비율을 계산**할 것.

3. **`BackSide`는 삼각형을 엮은 순서에 달려 있다.** 링 안쪽에서 보니 `BackSide`가 맞을 것 같지만
   winding이 반대면 **보고 싶은 면이 정확히 잘려 나가** 산이 통째로 사라진다(실제로 그랬다).
   메시 하나짜리 배경은 그냥 `DoubleSide`가 안전하다.

4. 🔴 **배경 판이 지면 높이에 있으면 파낸 구덩이를 덮는다.** 사용자 보고: *"바닥을 파면 녹색면이 보인다.
   즉 바닥이 안 파이는 거 같다."* — 데이터는 멀쩡했고 **배경이 앞을 가린 것**이었다(먼 땅 판 −0.02m,
   구멍 바닥 −4.8m → 판이 위라서 먼저 그려진다).
   → 먼 땅은 **가운데가 뚫린 고리**로. 구멍은 **스트리밍 반경보다 작아야** 한다 —
   크면 이번엔 진짜 지형과의 사이에 **틈**이 생겨 하늘이 비친다. 테스트가 그 관계를 잠근다.
   > **"안 파인다"류 신고는 데이터부터 의심하지 말 것.** 파생 인덱스가 아니라 **가리는 것**이 원인일 수 있다.

> 다섯째, **능선 모양**: `abs(sin)`은 꼭대기가 둥글고 골이 뾰족해 **언덕**으로 보인다.
> 산은 반대라 `1 - abs(sin)`이어야 한다. 순수 함수 테스트는 이걸 못 잡는다(값 범위는 똑같다) — 화면을 봐야 한다.
>
> 여섯째, **같은 색을 두 군데 적지 말 것**: `SKY_ZENITH` 상수를 만들어 놓고 함수 안에 값을 또 박아 뒀더니
> **상수를 고쳐도 화면이 안 바뀌었다.** 양 끝 색을 상수와 대조하는 테스트로 잠갔다(T-3·D-7과 같은 계열).

**D-8. ★★★ 접촉 그늘 — 세 번 연속으로 원인을 잘못 짚었다.** (2026-08-04)

사용자 신고: *"바닥에 브릭을 놓으면 맞닿은 경계 그림자가 어색하다. 포토샵으로 작은 이미지에 blur를 많이 준 것처럼 사방으로 퍼진다."*

| 시도 | 내가 짚은 원인 | 실제 | 결과 |
|---|---|---|---|
| 1 | 그라데이션이 브릭 길이만큼 늘어난다 | **포화**였다(막힌 칸 2·3·4가 전부 같은 값) | 고치긴 했으나 **화면은 그대로** |
| 2 | 벽면이 과대평가된다 | 맞지만 **주범이 아니었다** | "변한 게 없다" |
| 3 | 폭이 넓다 → 25cm로 조임 | **마인크래프트도 폭은 1m다** | "그림자가 거의 없다" |

**교훈 셋**

1. **폭을 좁히는 게 답이 아니다.** 참고 이미지를 재 보니 마크도 그늘이 **블록 한 칸(1m)** 에 걸쳐 퍼진다.
   답은 같은 폭 안에서 **접촉면 쪽으로 쏠리게** 하는 것(곡선), 그리고 **포화하지 않게** 하는 것이다.
2. **"어둡다/밝다"만 보는 테스트로는 이 계열을 못 잡는다.** 값은 내내 맞았고 **모양**이 틀렸다.
   → `shading.test.ts`가 **거리별 프로파일**과 **거리당 회복 속도**를 잰다.
   ⚠️ 총량으로 비교하면 안 된다 — 뒤 구간이 3배 길어 아무리 쏠려 있어도 뒤가 이긴다(그렇게 썼다가 헛failed).
3. **같이 맞춰야 하는 상수를 두 파일에 두지 말 것.** 세기는 `world.ts`, 곡선은 셰이더에 있어서
   한쪽만 만지다 두 번 놓쳤다. → **`lib/brick/shading.ts` 한 파일로 모았다.**

> **아직 남은 구조적 격차**(이걸 고치기 전엔 큰 브릭에서 마크보다 못하다): 아래 D-9.

**D-9. ⬜ 조명 해상도가 **칸이 아니라 브릭**이다 — 마크와의 진짜 격차.**

밝기 값은 브릭의 **꼭짓점 여덟 개**뿐이고, 그 사이는 GPU가 직선으로 채운다.
→ **2×4 브릭의 면은 2m짜리 그라데이션 한 장**이 된다. 마인크래프트는 면이 절대 1m를 안 넘는다.
게다가 그 여덟 값을 **여섯 면이 공유**해서, 같은 평면에 나란히 붙은 이웃까지 가림막으로 세어진다
(평평한 벽 밑동이 한 단계 더 어두워진다 — 지금은 `AO_WALL_RELIEF`로 뭉뚱그려 상쇄 중).

**고치는 법**: 프래그먼트에서 **월드 좌표로 칸 단위 빛 볼륨을 샘플링**한다(복셀 엔진 표준).
지오메트리·정점 수 무변경, 텍스처 조회 1회. 계획서 §18.

## E. 입력 — 버튼 하나에 두 뜻을 주지 말 것

**E-00. ★★★ 사용자가 겨눈 것을 코드가 "알아서" 고치지 말 것.**
배치가 어긋난다는 말이 나오면 **공식을 고치기 전에 자동 보정을 찾아 지워라.** 원인은 매번 보정이었다:
`snapCentered`(커서로 좌우 위치 계산) · `seatAlong`(걸리면 한 칸 밀어냄) · 옆면 `b.y` 정렬(높이 자동 맞춤). 하나하나는 "친절"인데, 합쳐지면 **겨눈 자리와 놓인 자리가 달라진다**.
> **마인크래프트가 정확하게 느껴지는 진짜 이유**: 블록이 전부 1칸이라 "어느 면"이 정해지면 들어갈 칸이 **딱 하나** — 고를 여지가 없어 보정할 것도 없다. 그리고 **막히면 그냥 안 놓인다.**
> 우리 브릭은 여러 칸이라 "그 면 위 어디에?"가 남지만, **그 답을 코드가 채우면 안 된다.** 규칙은 **"겨눈 칸의 옆 칸"** 하나. 막히면 **빨강으로 알리고 안 놓는다.** 뻗는 방향은 사용자가 **회전(R)** 으로 정한다.
> **확정 규칙**: 겨눈 **칸**을 표면에 강조 → 그 옆 칸에 브릭 **첫 칸**. 크기·회전이 첫 칸을 바꾸지 않는다(테스트로 잠금).
> 나는 이걸 못 알아듣고 **네 번** 보정 공식을 고쳤다(낡은 상태 → 시작 칸 → 시차 → 중앙 맞춤). 사용자가 "다른 대안을 찾아라"고 **두 번** 말했는데도 계속 공식만 만졌다.

**E-1. 액션은 왼쪽 버튼 하나.** 우클릭은 카메라 회전과 겹치고, **모바일·트랙패드엔 아예 없다**.

**E-2. R3F `onClick`·`contextmenu`는 얼마나 끌었든 뗄 때 발동한다.** 그래서 액션은 **누를 때** 실행한다.

**E-2b. ★★ 드래그용 투영 평면은 **손이 닿는 그 표면**이어야 한다. 그리고 움직이기 전엔 아무것도 옮기지 말 것.**
평면이 표면보다 떠 있으면 비스듬히 볼 때 투영 지점이 **`높이 × tan(기울기)`** 만큼 어긋난다. 브릭 **중심**(0.3m)으로 잡았더니 45°에서 0.3m → 반올림하면 **한 칸**. 게다가 `useFrame`이 마운트 첫 프레임에 돌아서 **그냥 클릭만 해도 자리가 바뀌었다.**
> 증상이 특이했다: **호버 중에는 숫자가 맞는데 클릭하는 순간 바뀐다**(−6,0,−1 → −5,0,0), 그리고 위에서 볼 때는 정확하다.
> **"호버할 때 숫자냐 클릭할 때 숫자냐"** 는 사용자 질문 하나로 원인이 드러났다. 배치 문제는 **호버 값과 클릭 후 값을 따로** 봐야 한다 — 나는 계속 호버 계산만 들여다봤다.

**E-3. 뷰포트 평면 좌표는 R3F 이벤트가 아니라 직접 레이캐스트로.** 방금 놓은 브릭이 뒤의 평면을 가려 이벤트가 끊기고, 브릭 표면에서 받으면 높이가 달라 커서와 어긋난다. 안전장치 2개 필수: **시선이 평면과 거의 나란하면 무시** + **거리 상한**.

**E-4. 대체한 것은 남기지 말 것.** y=0 투명 판을 "backdrop용"이라며 남겼더니 그 판이 **구멍 안 배치를 막고 있었다.**

**E-5. ★ 놓는 자리는 "최신 값"이 아니라 **화면에 보여준 값**이다 (WYSIWYG).**
미리보기와 클릭이 **서로 다른 값**을 읽으면 어긋난다. 한때 "상태는 한 프레임 낡으니 클릭은 ref로 최신 값을 읽자"고 고쳤는데, 그게 **미리보기와 배치를 갈라놨다** — 움직이면서 누르면 마커가 있던 곳이 아니라 다음 칸에 놓였다. ref는 **화면에 안 보이는 값**(드래그 세션 상태 등)에만 쓴다.

**E-6. 커서와 도형의 관계는 "수치"로 물어라.** "어긋난다"는 느낌만 듣고 **네 번**을 헛고쳤다. **"커서가 사각형의 어디에 있나"** 한마디에 원인이 드러났다(모서리에 있었다 = 보정이 밀고 있었다). 배치 문제는 **커서·마커·놓인 브릭 셋의 관계**를 물어 좁힐 것.

## F. 측정·테스트

**F-1. 숫자가 상식과 어긋나면 코드보다 계기판을 먼저 의심한다.** (`gl.info.render`에 그림자 패스가 섞여 "실제 > 전체"가 나왔다.)

**F-2. 실패하는 걸 본 적 없는 테스트는 아직 아무것도 안 지킨다.** 새 테스트는 **반드시 사보타주로 실패를 확인**할 것. 실제로 두 번, 통과하는데 버그를 못 잡는 테스트를 썼다.

**F-3. 무작위 월드에 기댄 조건부 단언은 조용히 사라진다.** 파츠를 늘리자 분포가 바뀌어 `if`가 안 걸리며 테스트 하나가 소리 없이 없어졌다. **확정적으로** 만들 것.

**F-4. 결과를 확인하지 않는 insert는 실패해도 아무도 모른다.** (상호작용 이벤트가 몇 달간 한 건도 저장되지 않았다.)

**F-6. ★ 사보타주가 *통과*하면 코드가 아니라 테스트가 틀린 것이다 — 무엇과 무엇을 비교했는지 다시 보라.** (2026-08-04)
사암 옆면의 위아래를 잠근다며 **맨 윗줄 vs 맨 아랫줄**을 비교했는데, 띠를 통째로 아래로 옮기는 사보타주가 **그대로 통과**했다
— 옮긴 띠의 **어두운 경계선**이 하필 맨 아랫줄에 걸려 밝기 대소가 유지됐기 때문이다.
- 고친 방법: **띠(0~1줄) vs 몸통(4줄 이하)** 을 평균으로 비교하고 **여유(> 20)** 를 요구한다.
  "한 줄"이 아니라 **덩어리**를 비교해야 우연에 안 걸린다.
- 겸사겸사 **그림도 고쳤다** — 원래 띠가 너무 옅어서 테스트가 잡을 여지 자체가 없었다.
  **잠글 수 없을 만큼 미묘한 차이는 애초에 화면에서도 안 보인다**는 신호다.
- 같은 날 A-5도 같은 병이었다(축만 보고 크기를 안 봄). **F-2는 "실패를 봤나"이고, 이건 "무엇을 봤나"다.**

**F-5. ★ 자동화 브라우저에서 잰 FPS를 믿지 말 것.** 탭이 화면 앞에 없으면 Chrome이 `requestAnimationFrame`을
**초당 1회 수준으로 throttle**한다 → FPS가 121에서 **1~6으로** 떨어지고 카메라도 안 움직인다. 방금 넣은 변경 탓으로
착각하기 딱 좋다(2026-08-02에 실제로 그랬다).
- 판별: `document.hidden` / `visibilityState`를 읽어 본다. `hidden`이면 그 FPS 숫자는 **의미가 없다.**
- 대신 **draw call·삼각형 수**를 보라 — 이건 throttle과 무관하고, 지오메트리·배칭 회귀를 실제로 잡아 준다.
- `Runtime.evaluate`가 타임아웃 나는 것도 같은 원인일 수 있다(렌더러가 얼어 있는 것처럼 보인다).

## G. 도구 사용

**G-1. 큰 파일에서 정규식·sed로 코드 블록을 자르지 말 것.** 265줄이 날아간 적 있다. **Edit 도구**를 쓴다.

**G-2. 작업 전 컨펌.** "어떻게 할 거냐"는 질문이지 지시가 아니다.

**G-5. `git checkout <file>`은 되돌리기 도구가 아니다.** 커밋 안 된 작업을 조용히 지운다 — 확인까지 끝난 기능을 그렇게 날렸다(2026-07-31). 스크립트 편집이 꼬이면 **되돌릴 게 아니라 그 편집을 손으로 고친다.** 정말 되돌려야 하면 먼저 `git stash`.

**G-4. `node -e "..."`에 백틱을 담지 말 것.** 셸이 명령 치환으로 먹어서 **문장이 조용히 비어 버린다**(문서에서 `recentColors` 같은 단어가 통째로 사라졌다). 두 번 겪었다. 문서에 코드 표기가 들어가면 **Write/Edit 도구로 쓰거나 파일로 만들어 붙일 것.**

**G-3. 이 저장소엔 CRLF 파일이 섞여 있다.** `\n`을 붙인 문자열 치환은 그런 파일에서 **전부 MISS하고도 성공한 것처럼 끝난다**(`EditorCanvas.tsx`에서 11개 전부 놓쳤다). 스크립트로 손대야 하면 **줄 단위로 나눠 정규식으로 찾고, 원본 EOL로 되붙일 것.** 치환 결과는 반드시 개수로 확인한다.

> **또 밟았다(2026-07-31)**: 되돌리기 사보타주 검증에서 여러 줄짜리 치환이 조용히 안 먹어,
> "사보타주해도 테스트가 통과한다"는 **잘못된 결론**을 낼 뻔했다. 코드가 아니라 **검증 도구가 거짓말한 것**이다.
> 사보타주는 반드시 **적용됐는지 먼저 확인**하고(치환 개수 출력), 줄 단위로 할 것.

**G-6. 다시 계산했으면 *알려야* 한다 — 렌더러는 리비전만 본다.** `recomputeLight()`가 빛 지도를
다시 채우고도 **리전 리비전을 안 올려서**, 값은 맞는데 화면은 옛 값이었다(2026-07-31).
증상이 **"어떤 건 바뀌고 어떤 건 안 바뀐다"** 로 나타나는 게 이 계열의 특징이다 —
다른 이유로 리빌드된 리전만 우연히 갱신되기 때문. 데이터가 틀렸다고 착각하기 딱 좋다.

- **파생물을 다시 만드는 함수는 끝에서 반드시 무효화까지 한다.** `recomputeAll()`은 하고 있었고
  `recomputeLight()`만 빠져 있었다 — **짝이 되는 함수끼리 비교**하면 금방 드러난다.
- **중간에 빠져나가는 길(예산 초과 `return`)이 있으면 무효화가 건너뛰어진다.** 굽기와 무효화를
  **다른 함수로 나눠서** 어느 경로로 끝나도 무효화가 돌게 할 것.
- 테스트는 값이 아니라 **리비전이 올랐는지**를 본다(`regionSnapshot()` 전후 비교). 값만 보는
  테스트는 이 결함을 **영원히 통과시킨다**.

**T-1. ★ 캔버스 텍스처는 `flipY = false`를 꼭 켤 것.** three는 `flipY = true`가 기본이라(GL 텍스처 원점이 좌하단)
**캔버스 맨 윗줄이 `v = 1`** 이 된다. 아틀라스 좌표를 `v = (행 + f) / 행수`(안 뒤집힌 기준)로 계산하면
**엉뚱한 행을 샘플링**한다 — 우리 경우 0~3번 칸이 3행을, 4~7번 칸이 2행을 읽어 **안 그린 빈 영역(흰색)** 이 나왔다(2026-07-31).
화면엔 "무늬가 아예 안 먹는다"로 보여서 셰이더 배선 문제로 착각하기 쉽다.

- 판별법: **그림 데이터를 순수 함수 테스트로 먼저 자를 것**(`textures.test.ts`). 그게 통과하면 남은 건 좌표계다.
  이번에 그렇게 범위를 좁히니 바로 나왔다 — 추측했으면 또 한참 돌았다(G-6·D-2c와 같은 교훈).

**T-2. ★ 그림에 위아래가 생기는 순간 `v`를 뒤집어야 한다.** T-1의 나머지 절반이고, **몇 달간 잠복해 있었다**(2026-08-02).
아틀라스가 `flipY = false`로 올라가므로 **`v = 0`이 캔버스 맨 윗줄**인데, 상자 옆면의 `v = 0`은 **아래쪽**이다.
→ 그림이 화면에서 **위아래로 뒤집혀** 붙는다. 지금까지 무늬가 전부 잡티·주기적 가로줄(흙·돌·벽돌)이라
**위아래가 없어서 아무도 몰랐다.** 잔디 옆면(풀 띠가 위에 있어야 한다)을 넣자마자 드러났다.

- 해법은 셰이더에서 `f.y = 1.0 - f.y` 한 줄(`injectTexture`). 그림 쪽 규약은 **"y=0 줄이 면의 위쪽"** 으로 고정했다.
- **순수 함수 테스트로는 절대 안 잡힌다** — 그림 데이터는 맞기 때문이다(G-7과 같은 계열). 테스트는 "풀 띠가 그림 위쪽에 있다"까지만 잠글 수 있고,
  **그게 화면 어디에 가 붙는지는 눈으로 봐야 한다.** 브릭 하나 놓고 옆면을 확대해 보는 데 1분이면 된다.
- 앞으로 **위아래가 있는 그림**(원목 마구리·화로 앞면·사다리)을 넣을 때마다 이 규약을 다시 확인할 것.

**T-4. ★ UV가 두 벌이다 — 무늬는 `aTexUV`, 경계선은 `uv`.** 무늬 크기를 파츠 크기와 무관하게 만들려면
UV를 **칸 수만큼 반복**시켜야 하는데(1×1과 1×2의 벽돌 줄눈이 2배 차이 났다), 경계선 셰이더는 정반대로
**면마다 0~1**이어야 가장자리를 찾는다(`min(vUv.x, 1-vUv.x)…`). 한 벌로 겸용하면 **타일마다 선이 그어져** 브릭이 잘게 쪼개진다.
- `aTexUV`는 **법선의 지배축으로 평면 투영**해 한 함수(`addTexUV`)가 상자·모따기·쐐기·돌기를 전부 처리한다.
  파츠마다 손으로 적으면 반드시 어긋난다 — 실제로 **쐐기(경사)는 원래 `uv` 자체가 없었다.**
- 세로는 **위쪽 기준**이다(브릭 한 장, 플레이트는 위쪽 ⅓). 아래 기준으로 잡으면 T-2의 잔디 띠가 밑으로 밀린다.
- 새 파츠를 추가하면 `addTexUV`를 **반드시 통과시킬 것.** 빠뜨리면 그 파츠만 무늬가 뭉개지는데, 다른 파츠가 멀쩡해서 늦게 발견된다.

**T-5. ★ `uv`가 없는 지오메트리는 경계선 셰이더에 물리면 *면 전체가 어두워진다*.** ✅ 해결(2026-08-02)
`injectEdgeLines`는 `vUv`로 가장자리까지의 거리를 재는데, 지오메트리에 `uv` 속성이 없으면 WebGL이 **(0,0)을 기본값으로** 준다
→ 모든 프래그먼트가 `d = 0` = "선 위"로 판정돼 **면 전체에 `EDGE_DARKEN`이 곱해진다.**
쐐기(경사)엔 `uv`가 아예 없어서 **경사면만 조용히 어두웠다**(A/B 실측 6.6~8.2%).

- 증상이 "약간 어둡다"라서 **조명 문제로 착각하기 딱 좋다.** 실제로 구운 빛·AO를 먼저 의심했다.
- 판별: 그 파츠의 지오메트리에 `uv`가 있는지 본다. `position`·`normal`만 손으로 만든 지오메트리가 후보다.
- **새 지오메트리를 손으로 만들면 `uv`(면당 0~1)를 반드시 같이 넣을 것.** 테스트가 전 파츠에 대해 지킨다(`brickGeometry.test.ts`).
- 크기가 아니라 **밝기**로 나타나므로 T-4(무늬 크기)와는 다른 증상이다 — 둘 다 UV 문제지만 보이는 곳이 다르다.

> **실측이 결론을 냈다**: 고친 뒤/전 스크린샷을 픽셀로 비교해 **밝아진 픽셀 11,993 · 어두워진 픽셀 0**.
> "어두워진 게 0"이라는 게 핵심 증거다 — 상자는 한 픽셀도 안 변하고 **경사면만** 밝아졌다는 뜻이다.
> 참고로 내 사전 추정은 14%였는데 실측은 6.6~8.2%였다. `smoothstep(0, 0, 0)`은 **GLSL 명세상 결과가 정의되지 않아** 추정이 빗나갔다 — **셰이더 수치는 재야 한다.**

**T-3. 재료 하나가 아틀라스 칸 하나라는 가정이 깨졌다.** 면별 무늬가 들어오면서 **재료 1 : 칸 N** 이 됐다(잔디 = 잔디·잔디옆면·흙 세 칸).
아틀라스를 구울 때 `BRICK_TEXTURES`를 돌면 **옆면 전용 그림이 아예 안 구워진다** — 화면엔 흰 면으로 나오고,
원인은 셰이더처럼 보인다. **굽는 루프는 반드시 `ART_ORDER`(그림 목록)를 돈다.**
> **저장값은 재료 id**이고 아틀라스 칸은 **내부 구현**이다. 이 둘을 다시 합치지 말 것 — 합치는 순간 그림을 추가할 때마다 저장 포맷이 흔들린다.

**G-7. 포인터 이벤트로만 갱신되는 상태는 키보드 조작에서 멈춘다.** 놓일 자리(`spot`)를 `onHover`에서만 계산해서,
**마우스를 안 움직이고 `R`만 누르면 앵커가 옛 회전에 남았다**(2026-07-31). 크기는 최신이라 고스트가 겨눈 칸을 벗어난다.
→ **마지막 겨눈 지점을 ref에 남기고, 파츠·회전이 바뀌면 거기서 다시 계산**한다.

- **G-6과 같은 계열이다** — "계산은 맞는데 화면에 안 갔다". 하루에 두 번 나왔다.
- 판별법: **입력 없이 상태만 바꿔 보라**(R·단축키·패널 클릭). 그때 화면이 안 따라오면 이 계열이다.
- 순수 함수 테스트로는 **절대 안 잡힌다**(계산은 맞으므로). 배선을 바꿀 땐 "이 값을 누가 다시 계산하나"를 먼저 적을 것.
